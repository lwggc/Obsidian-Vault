# 计算机网络 — 卡片素材

> 使用说明：每张卡片我已取好标题和整理好内容。你需要在 Markji 中手动添加 [F##...] 挖空标记。
> 建议：每张卡片挖 2~4 个空，别挖太多，否则记不住。
> 📷 标注表示原笔记该部分有配图，请对照你的原笔记插入对应图片。

---

## 卡1 — 标题：计算机网络概述

**网络、互联网与因特网**：
- 若干节点和链路互连形成网络
- 若干网络通过路由器互连形成互连网（互联网）
- 因特网是当今世界上最大的互联网

**因特网的组成**：
- 从组成部分：硬件、软件、协议
- 从工作方式：边缘部分、核心部分
- 从功能组成：通信子网、资源子网

**计算机网络的功能**：数据通信（最重要最基础）、资源共享、分布式处理、提高可靠性、负载均衡。

> 📷 原笔记该部分有配图

---

## 卡2 — 标题：三种交换方式

**电路交换**：建立连接→通话→释放连接。通信时延小、有序传输、无冲突，但建立连接时间长、线路利用率低。

**报文交换**：报文被整个发送，转发时延长，需较大缓存空间。

**分组交换**：报文分组后独立转发。传输效率高，减少了出错概率和重传代价。但存在存储转发时延，可能失序、丢失。

**分组交换的优缺点**：
- 优点：交换节点可改变转发路由，不会引起全网瘫痪
- 缺点：存在存储转发时延，首部带来额外开销

> 📷 原笔记该部分有配图

---

## 卡3 — 标题：计算机网络分类

**按分布范围**：广域网WAN（几十到几千千米）、城域网MAN（5-50km）、局域网LAN（1km左右）、个域网PAN（10m）

**按拓扑结构**：总线形、星形、环形、网状

**按传输技术**：广播式网络、点对点网络

**按使用者**：公用网（因特网）、专用网（军队、铁路等）

> 📷 原笔记该部分有配图

---

## 卡4 — 标题：性能指标

**速率**：数据率或比特率，基本单位bit/s（bps）
**带宽**：单位时间内某点到另一点所能通过的最高数据率
**吞吐量**：单位时间内通过某个网络或接口的实际数据量
**时延**：发送时延 + 传播时延 + 处理时延 + 排队时延
**时延带宽积** = 传播时延 × 带宽（以比特为单位的链路长度）
**往返时间RTT**
**信道利用率** = 有数据通过的时间 / 总时间

> 📷 原笔记该部分有配图

---

## 卡5 — 标题：OSI模型与TCP/IP模型

| OSI七层 | TCP/IP四层 | PDU |
|:---:|:---:|:---:|
| 应用层 | 应用层 | 报文 |
| 表示层 | ↑ | ↑ |
| 会话层 | ↑ | ↑ |
| 传输层 | 传输层 | 报文段 |
| 网络层 | 网际层 | IP数据报/分组 |
| 数据链路层 | 网络接口层 | 帧 |
| 物理层 | ↑ | 比特 |

- OSI上三层（应用、表示、会话）为**资源子网**，下三层（物理、数据链路、网络）为**通信子网**，传输层承上启下
- **端到端**：传输层（进程到进程）
- **主机到主机**：网络层
- **点到点**：数据链路层（节点到节点）

ISO设计了OSI/RM，但实际执行的是TCP/IP协议标准。

> 📷 原笔记该部分有配图

---

## 卡6 — 标题：OSI各层功能

**应用层**：用户能产生网络流量的程序。FTP、SMTP、HTTP
**表示层**：数据格式变换、数据加密解密、数据压缩和恢复
**会话层**：建立管理终止会话，视频流语音流同步（声画统一），校验点/同步点恢复通信
**传输层**：端到端通信（进程到进程）。可靠传输TCP、不可靠UDP。复用分用、端到端流量控制
**网络层**：路由选择、整个网络流量控制
**数据链路层**：成帧、访问控制、相邻节点流量控制
**物理层**：定义接口特性、传输模式（单工/半双工/全双工）、速率、比特同步、比特编码

**协议三要素**：语法（格式）、语义（操作）、同步（时序）

> 📷 原笔记该部分有配图

---

## 卡7 — 标题：物理层通信基础

**码元**：一个信号周期内，1码元携带log₂K比特（K为状态数）。码元速率即调制速率。

**奈奎斯特定理**（无噪声理想信道）：
- 极限波特率 = 2W（Baud）
- 极限比特率 = 2W × log₂K（b/s）

**香农定理**（有噪声信道）：
- 极限比特率 = W × log₂(1 + S/N)（b/s）
- dB = 10 × log₁₀(S/N)

> 当题目同时给出码元和比特关系时，需要受两个公式共同限制，取二者最小值。

> 📷 原笔记该部分有配图

---

## 卡8 — 标题：编码与调制

**数字信号→数字信号（基带调制）**：
- 曼彻斯特编码：上0下1（IEEE标准），**中间必跳变**，有自同步能力。以太网默认采用
- 差分曼彻斯特编码：中间跳变，0表示开始边界有跳变，1表示无跳变

**数字数据→模拟信号（调制）**：
- 调幅（ASK）、调频（FSK）、调相（PSK）
- **正交幅度调制QAM**：m种振幅×n种相位，1码元=log₂(mn) bit

> 📷 原笔记该部分有配图

---

## 卡9 — 标题：传输介质

**导向型**：
- **双绞线**：屏蔽STP / 非屏蔽UTP
- **同轴电缆**：内导体（信号）+外导体屏蔽层
- **光纤**：纤芯（高折射率）+包层（低折射率），利用全反射。单模（长距离）、多模（近距离易失真）
- 抗干扰能力：光纤 > 同轴 > 双绞线

**非导向型**（无线）：电磁波。短波指向性强，长波绕射性好。

**物理层特性**：机械特性（引脚尺寸）、电气特性（电压范围）、功能特性（哪些线的功能）、过程特性（事件顺序）

> 📷 原笔记该部分有配图

---

## 卡10 — 标题：物理层设备

**中继器**：对数字信号进行再生（整形+放大）。不能无限串联。
**集线器（多端口的中继器）**：将收到信号整形放大后转发到所有其他端口。各端口连接的节点不可同时发送数据，会导致冲突。

中继器和集线器的**5-4-3规则**：最多5段介质、4个中继器、最多3个主机段。

> 放大器是对**模拟信号**进行放大，中继器是对**数字信号**进行再生。

> 📷 原笔记该部分有配图

---

## 卡11 — 标题：数据链路层功能

数据链路层提供**不可靠的无连接服务**。主要使用点对点信道和广播信道。

**三大功能**：封装成帧、差错检测、可靠传输

**四种组帧方法**：
1. **字符计数法**：帧首定长计数字段表示帧长。一个出错后续全乱
2. **字节填充法**：SOH/EOT定界，特殊字符前加ESC
3. **零比特填充法**（HDLC）：01111110定界，遇到连续5个1就填充0
4. **违规编码法**：利用物理层违规信号定界（如曼彻斯特中间不跳变）

> 📷 原笔记该部分有配图

---

## 卡12 — 标题：差错控制

**位错**：CRC循环冗余校验发现（可检测所有单比特、奇数位、双比特、小于等于校验位长度的连续错误）
**帧错**：帧丢失、重复、失序

**CRC校验**：数据发送和接收方约定一个除数，余数为0表示没错，非0表示出错。

**海明码**：分组偶校验，多个校验位能反映出错位置。有1位纠错能力、2位检错能力。

> 📷 原笔记该部分有配图

---

## 卡13 — 标题：可靠传输与滑动窗口

**可靠传输**：解决帧丢失、帧重复、帧失序
**流量控制**：控制发送方速率，让接收方来得及接收

三种协议：
- **S-W（停等协议）**：发送窗口=1，接收窗口=1
- **GBN（后退N帧）**：发送窗口>1，接收窗口=1。接收方只按序接收，丢弃失序帧。累计确认
- **SR（选择重传）**：发送窗口>1，接收窗口>1。接收方对每个正确接收的分组逐一确认。W_R ≤ W_T

**累计确认**：收到3号帧确认，则0、1、2、3都默认已收到。

> 📷 原笔记该部分有配图

---

## 卡14 — 标题：PPP协议

PPP是目前使用最广泛的点对点数据链路层协议。

特点：点对点、面向字节流。**没有纠错，没有序号和确认机制，不可靠。**
- 异步链路→字节填充；同步链路→零比特填充
- 同一物理链路上支持多种网络层协议

PPP很菜，啥都没有，必须靠上层协议保证数据传输正确无误。

三部分组成：**LCP**（建立配置测试链路）+ **NCP**（配置网络层协议）+ 网络层PDU封装到串行链路的方法

> 📷 原笔记该部分有配图

---

## 卡15 — 标题：介质访问控制

广播信道需要实现介质访问控制（点对点信道通常不用）。

三种方式：
- **信道划分**（静态）：时分TDM、频分FDM、波分WDM、码分CDM
- **随机访问**（动态）：ALOHA、CSMA、CSMA/CD、CSMA/CA
- **轮询访问**：令牌传递协议（适用于负载高的网络）

**码分复用CDMA**：各节点有专属码片序列。与码片相同表示1，相反表示0。不同站的码片正交（规格化内积为0）。

> 📷 原笔记该部分有配图

---

## 卡16 — 标题：CSMA/CD协议

用于**有线以太网**（半双工）。

**先听后发，边听边发，冲突停发，随机重发。**

**随机重发**：截断二进制指数退避算法。
- 第10次冲突是分水岭
- 第16次冲突直接放弃，报告网络层

**最短帧长** = 争用期 × 数据传播速率。以太网默认最短帧长64B，最长1518B。
**争用期** = 端到端往返传播时延。发送帧时间 ≥ 争用期时间。

> 📷 原笔记该部分有配图

---

## 卡17 — 标题：CSMA/CA协议

用于**IEEE 802.11无线局域网**。

三种机制：RTS/CTS帧（预约信道，可选）、ACK帧确认。

- CA不存在冲突检测（无线环境无法检测）
- **帧间间隔优先级**：SIFS > PIFS > DIFS（时间越短优先级越高）
- SIFS：最高优先级，用于ACK、CTS帧
- DIFS：最长的ifs，用于数据帧和管理帧

**RTS/CTS只能减少隐蔽站问题，无法完全避免。**

> 📷 原笔记该部分有配图

---

## 卡18 — 标题：局域网

IEEE 802.3（以太网）：逻辑拓扑是总线形，物理拓扑是星形。
IEEE 802.11（无线网）
IEEE 802.1Q（VLAN技术）

**以太网交换机**属于数据链路层。最大优点：用户通信时独占带宽。
- 直通式交换：只要目的地址
- 存储转发交换：要完整的帧

交换机总带宽 = N个接口 × 每个接口带宽，若**全双工则再乘2**。

**VLAN**：通过软件实现，把大局域网分割成多个逻辑VLAN，每个VLAN相当于一个小广播域。
802.1Q帧最大1522B。

> 📷 原笔记该部分有配图

---

## 卡19 — 标题：以太网MAC帧

V2标准：**662N4，收发协数验**
- 6B目的地址 + 6B源地址 + 2B类型 + 4B校验
- 传输到物理层时，物理层会在MAC帧前添加8B前导码（7B同步+1B定界）

**以太网帧不需要帧结束定界符**。各帧之间必须有间隙，接收方找到帧开始定界符后连续到达的都属于同一帧。

**冲突域**：两个节点同时发送会冲突则在同一冲突域
**广播域**：一个节点发广播帧可被另一个节点接收则在同一广播域

> 📷 原笔记该部分有配图

---

## 卡20 — 标题：无线局域网802.11

结构：星形（1个AP+N台移动设备）
协议：CSMA/CA

802.11数据帧MAC首部共30字节。地址1、2、3看AP字段。
口诀：
- **30 N 4首数验，首部3+1地址**
- **九十比特表去来，帧的中转靠AP**
- **去往AP中起止，来自AP止中起**

两个移动站不能直接通信，必须通过AP转发。

> 📷 原笔记该部分有配图

---

## 卡21 — 标题：网络层功能

网络层主要任务：把分组从源端传到目的端，为分组交换网上的不同主机提供通信服务。

**功能**：异构网络互联、路由和转发、拥塞控制

**网络层提供的两种服务**：
- **虚电路服务**（面向连接）：固定路径不固定带宽，可实现差错控制
- **数据报服务**（无连接）：网络造价低，灵活适应多种应用

**SDN（软件定义网络）**：把控制层面和数据层面分离。远程控制器掌握网络状态，为每个分组计算最佳路由。路由器仅进行查表转发。
- OpenFlow协议：控制层面与数据层面之间的通信接口

> 📷 原笔记该部分有配图

---

## 卡22 — 标题：IPv4数据报格式

**418，首总偏**
- 首部长度：以4B为单位
- 总长度：整个IP数据报长度
- 标识：16bit，判断分片属于哪个数据报
- 标志：MF=1还有分片，MF=0最后分片；DF=1不允许分片
- **片偏移**：以8B为单位，标识数据部分在被分片前的位置
- TTL：8bit，每经过一个路由器先TTL-1再判断
- 协议：TCP=6，UDP=17
- 首部检验和：16bit，**仅检验首部不检验数据部分**

分片：可在源主机或任何路由器中发生，只有目的主机才重组。
除了最后一个分片，其他分片的数据部分必须是8B的整数倍。

> 📷 原笔记该部分有配图

---

## 卡23 — 标题：IP地址与子网划分

**IP地址分类**：A/B/C/D/E类。根据前几位比特判断类别。
- 网络号全0表示本网络，全1表示广播
- 主机号全0表示网络本身，全1表示广播地址
- 127.x.x.x：环回自检地址
- **一个网络中主机号占n位则最多支持2^n-2台主机**

**子网划分**：<网络号,主机号> → <网络号,子网号,主机号>
- 子网地址中主机号不能为全0/全1

**CIDR**：IP/前缀长度。主要作用：有效分配IP地址空间，减少路由表大小。
- 子网划分是变长子网划分
- 遵循**最长前缀匹配原则**

**NAT**：私有IP不能直接出现在因特网上，必须通过NAT转换为全球IP地址。

> 📷 原笔记该部分有配图

---

## 卡24 — 标题：ARP、DHCP、ICMP

**ARP**：IP地址→MAC地址。作用在局域网内部，不能跨网络使用。
- 每台主机/路由器维护ARP缓存表
- 收到ARP报文（无论请求还是回答）时更新表

**DHCP**：给刚接入网络的主机动态分配IP地址、配置默认网关、子网掩码。应用层协议，基于UDP。
- 客户端口68，服务器端口67
- 四个阶段：发现（广播）→提供（广播）→请求（广播）→确认（广播）

**ICMP**：网络层协议，封装在IP数据报中发送。分为差错报告报文和询问报文。
- 五种差错报文：终点不可达、源点抑制（已废弃）、超时、参数问题、路由重定向
- ping（应用层）使用ICMP回送请求与回答报文

> ICMPv6合并了ARP和IGMP的功能。IPv6配套网际层协议只需ICMPv6一个。

> 📷 原笔记该部分有配图

---

## 卡25 — 标题：IPv6

128位地址。**解决IPv4地址耗尽的根本措施。**

IPv6与IPv4区别：
- 取消了首部长度字段，固定40B
- TTL改为跳数限制
- 取消检验和字段（加速路由器处理）
- 取消选项字段，用扩展首部实现
- **只有源主机可以分片（IPv4中间路由器也可分片）**
- **不需要ARP**
- 过渡技术：**双协议栈**和**隧道技术**

IPv6地址压缩：左侧零省略、连续零压缩（只能使用一次）。CIDR斜线表示法仍可用。

三种基本地址类型：单播、多播、任播。

> 📷 原笔记该部分有配图

---

## 卡26 — 标题：路由算法与协议

**静态路由**：人工配置，简单开销小，不能及时适应网络变化。
**动态路由**：路由器通过路由选择协议自动获取，可适应网络变化。

**RIP（应用层/UDP/520）**：距离向量算法（Bellman-Ford）。
- 最大跳数15，16表示不可达。适用于小型自治系统
- 只能和相邻路由器交换信息，交换的是自己的路由表
- 好消息传播得快，坏消息传播得慢（慢收敛）

**OSPF（网络层/IP/89）**：链路状态算法（Dijkstra）。
- 每一台路由器建立整个网络的拓扑图
- 支持等价多路径转发、鉴别功能、CIDR
- 划分为主干区域和非主干区域

**BGP（应用层/TCP/179）**：路径向量协议，自治系统之间。

> 📷 原笔记该部分有配图

---

## 卡27 — 标题：IP多播

D类地址（224.0.0.0~239.255.255.255）称为多播地址。
- 只能作为目的地址，不能作为源地址
- 多播组成员可随时变动
- 非成员也可向多播组发送数据报

**IGMP**：让本地多播路由器知道局域网内主机是否参加了多播组。
- 封装在IP数据报中传送，视为IP的组成部分
- IGMP仅作用于本地局域网

**多播路由选择协议**：在多播路由器之间为每个多播组建立多播转发树。

> 📷 原笔记该部分有配图

---

## 卡28 — 标题：路由器和三层设备

**路由器**：专用计算机，任务是转发分组。
- 路由选择部分：核心是路由选择处理机，构建路由表
- 分组转发部分：输入端口 + 交换结构 + 输出端口

**路由表（软件实现）vs 转发表（软硬件均可）**：
- 路由表由路由选择算法得出，用软件实现
- 转发表由路由表得来，可用硬件（如TCAM）实现

**三层设备区别**：
- 路由器：可互联两个不同网络层协议的网段
- 网桥（交换机）：可互联物理层和链路层不同的网段
- 集线器：不能互联两个物理层不同的网段

> 📷 原笔记该部分有配图

---

## 卡29 — 标题：传输层概述

传输层提供**应用进程之间的逻辑通信**（端到端）。

**socket套接字 = {IP地址:端口号}**，唯一标识网络中的一台主机上的一个应用进程。

**服务端**：被动通信，**客户端**：主动发起通信

**熟知端口号**：FTP=21，TELNET=23，SMTP=25，DNS=53，TFTP=69，HTTP=80，SNMP=161

**分用复用**：
- 发送方复用（传输层用端口号区分不同应用）
- 接收方分用（根据目的端口号上交）

> 📷 原笔记该部分有配图

---

## 卡30 — 标题：UDP

**无连接、不保证可靠交付、面向报文（一次发送一个报文）。**

特点：
- 没有拥塞控制，网络拥塞不影响发送速率
- 支持一对一、一对多、多对多
- 首部仅8B
- 只在IP基础上添加了分用复用和有限差错控制

UDP检验：
- 计算时增加12B伪首部（包含源IP、目的IP、协议字段）
- 检验和不是必需的（为0表示不计算）
- 计算结果为0时设置为全1（区分）
- 伪首部和全零填充字节都不发送

> 📷 原笔记该部分有配图

---

## 卡31 — 标题：TCP特点与段格式

**面向连接、点对点（一对一）、可靠交付、全双工、面向字节流。**

TCP首部最短20B，最长60B。

**关键字段**：
- **序号seq**：标记数据部分第一个字节在原始字节流中的位置
- **确认号ack**：在该确认号之前的所有字节都已确认收到（累计确认）
- 数据偏移：以4B为单位，表示TCP首部长度
- 标志位：SYN（同步）、FIN（结束）、ACK（确认=1时ack有效）
- 窗口：告诉对方从确认号算起还能接收多少字节

**只有第一次握手中ACK=0**，其他所有TCP报文段ACK=1。

> 📷 原笔记该部分有配图

---

## 卡32 — 标题：TCP连接管理

**三次握手**：
1. 客户端→服务器：SYN=1, seq=x
2. 服务器→客户端：SYN=1, ACK=1, seq=y, ack=x+1
3. 客户端→服务器：ACK=1, seq=x+1, ack=y+1
（握手①②SYN段不能携带数据，消耗1个序号；握手③可携带数据）

**四次挥手**：
1. 客户端→服务器：FIN=1, seq=u
2. 服务器→客户端：ACK=1, seq=v, ack=u+1
3. 服务器→客户端：FIN=1, ACK=1, seq=w, ack=u+1
4. 客户端→服务器：ACK=1, seq=u+1, ack=w+1

挥手②和③之间序号的差值取决于中间要传输的数据。

> 📷 原笔记该部分有配图

---

## 卡33 — 标题：TCP可靠传输与流量控制

**序号**：每个字节对应一个序号
**确认机制**：累计确认，推迟确认（最多不超过0.5s）

**重传机制**：
- **超时重传**：每发一个报文段设计时器，到期未确认则重传
- **快重传**：收到3个冗余ACK（不含第1个原始ACK），不等超时就重传

**流量控制**：接收方根据接收缓存可用空间控制发送方发送速率。
**重传分组数量不能超过滑动窗口大小。**

> 📷 原笔记该部分有配图

---

## 卡34 — 标题：TCP拥塞控制

四种算法：**慢开始、拥塞避免、快重传、快恢复。**

发送窗口上限 = min(rwnd, cwnd)

**超时重传**（严重拥塞）：
- ssthresh = cwnd/2，cwnd = 1，执行慢开始

**收到3个冗余ACK**（轻度拥塞，快重传+快恢复）：
- ssthresh = cwnd/2，**cwnd = ssthresh**，进入拥塞避免

慢开始：每收到一个ACK让cwnd+1（宏观上每RTT翻倍）
拥塞避免：每RTT只让cwnd+1（线性增长）

> 拥塞发生时ssthresh和cwnd会**立刻变化**，不等下一个发送轮次。

> 📷 原笔记该部分有配图

---

## 卡35 — 标题：应用层（DNS、FTP、电子邮件）

**DNS**：域名→IP地址。层次树状结构。域名服务器：根、顶级、权限、本地。
- 递归查询：负担大；迭代查询：更常用

**FTP**：基于C/S，使用TCP。双连接：控制连接（21端口）+数据连接（20端口主动模式）。
- 主动/被动模式针对数据连接而言

**电子邮件**：三个主要构件：用户代理、邮件服务器、协议。
- SMTP（只能传7位ASCII码，发送邮件）
- POP3/IMAP（读取邮件）
- MIME扩展（解决SMTP只能传ASCII的限制）

> 📷 原笔记该部分有配图

---

## 卡36 — 标题：HTTP与万维网

HTTP基于TCP，面向文本，每个字段长度不确定。

**请求报文常用方法**：
- **GET**：获取指定位置的资源
- **HEAD**：只返回状态行和首部行，不返回数据
- **POST**：发送数据创建新资源
- **PUT**：发送数据更新资源

**Connection字段**：close（HTTP/1.0默认，短连接）/ keep-alive（HTTP/1.1默认，长连接）
**Host字段**：指明服务器主机名和端口号
**Cookie**：对无状态的HTTP进行状态化

**HTTP页面请求时间**：
- 非持续连接（1.0）：2RTT + 文档传输时间
- 持续连接（1.1）非流水线：需多次RTT
- 持续连接（1.1）流水线：约1RTT

> 📷 原笔记该部分有配图
