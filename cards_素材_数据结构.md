# 数据结构 — 卡片素材

> 使用说明：每张卡片我已取好标题和整理好内容。你需要在 Markji 中手动添加 [F##...] 挖空标记。
> 建议：每张卡片挖 2~4 个空，别挖太多，否则记不住。
> 📷 标注表示原笔记该部分有配图，请对照你的原笔记插入对应图片。

---

## 卡1 — 标题：数据结构三要素

数据的逻辑结构反映数据元素之间的逻辑关系（前后关系），分为线性（一对一）和非线性（其他）。

抽象数据类型ADT描述了数据的逻辑结构和抽象运算，构成一个完整的数据结构定义。

数据结构三要素：**逻辑结构**、**存储结构**（也称物理结构）和**数据的操作**。

数据项是最小单位，构成数据元素；数据元素是数据的基本元素。

> 数据的逻辑结构独立于其存储结构。

---

## 卡2 — 标题：算法五特性

算法必须满足五个特性：
1. **有穷性**：算法必须能在执行有限步之后终止
2. **确定性**：算法中的每一条指令必须有确切的含义
3. **可行性**：算法中的操作都可以通过基本运算执行有限次实现
4. **输入**：有零个或多个输入
5. **输出**：有一个或多个输出

---

## 卡3 — 标题：时间复杂度排序与空间复杂度

时间复杂度排序（从小到大）：
O(1) < O(log₂n) < O(n) < O(nlog₂n) < O(n²) < O(n³) < O(2ⁿ) < O(n!) < O(nⁿ)

口诀：**常对幂指阶**

空间复杂度：指**相比于输入数据的规模，算法所需的辅助空间大小**。

---

## 卡4 — 标题：顺序表

顺序表是用一组地址连续的存储单元依次存储线性表的数据元素。

**静态分配**：使用静态数组，大小一旦确定无法改变
**动态分配**：使用动态数组，L.data=(ElemType*)malloc(sizeof(ElemType)*size)，满时可用malloc拓展

特点：
1. **随机访问**，能在O(1)时间内找到第i个元素
2. 存储密度高
3. 拓展容量不方便（存储空间要求连续）
4. 插入删除不方便

> 动态分配并不是链式存储，它同样属于顺序存储结构，物理结构没有变化，依旧是随机存取方式。

> 📷 原笔记该部分有配图

---

## 卡5 — 标题：顺序表插入/删除/查找的时间复杂度

**插入操作**：最好O(1)（表尾插入），最坏O(n)（表头插入），平均O(n)

**删除操作**：最好O(1)（删除表尾），最坏O(n)（删除表头），平均O(n)

**按值查找（顺序查找）**：最好O(1)，最坏O(n)，平均O(n)

---

## 卡6 — 标题：单链表

在一个设有头指针和尾指针的单链表中，删除表尾元素的时间复杂度和表长有关（必须从头开始找到表尾结点的前驱）。

带头结点的循环单链表中不存在空指针。

若想用于队列，带头尾指针的非循环单链表即可，循环单链表还需要维护。

**删除给定节点\*p**（非尾节点）：将后继节点的值赋予\*p，后删除后继节点，时间复杂度O(1)。

链表的前插：所有的前插都能转化为后插，后插完后交换两者数据即可。


> 📷 原笔记该部分有配图
---

## 卡7 — 标题：双链表与静态链表

**双链表**：每个节点有两个指针（prior和next），插入删除更方便但空间开销更大。

单链表通过调整指针排序时，平均时间复杂度最低的是——**归并排序**。

**静态链表**：
- 能容纳的最大元素个数在表定义时就确定了，以后不能增加
- 插入、删除不需要移动元素
- 相比动态链表可能浪费较多存储空间
- 需要分配较大连续空间
- 静态链表的结束标志是：next == -1

---

## 卡8 — 标题：顺序表 vs 链表对比

| | 顺序表 | 链表 |
|:---:|:---:|:---:|
| 可扩容性 | 弱 | 强 |
| 增删操作 | 弱 | 强 |
| 查找操作 | 强 | 弱 |

适用场景：
- 表长难以预估、经常增删元素 → 链表
- 表长可预估、查询操作较多 → 顺序表

注意事项：
- 顺序存储和链式存储都可以进行顺序存取
- 链式存储空间可以不连续，但存储单元一定连续
- 线性表的长度不包括头结点
- 链式存储结构比顺序存储结构更能方便地表示各种逻辑结构

---

## 卡9 — 标题：栈的基本概念

栈是一种逻辑结构，可用多种存储结构实现（如数组）。

**栈的操作**：
- `top++`：先赋值，再指针后移（S[top]=x; top++）
- `++top`：先指针后移，再赋值（top++; S[top]=x）

**共享栈**：让两个顺序栈共享一个数组，将两个栈的栈底分别设置在共享空间的两端，两个栈顶向共享空间的中间延伸。能有效利用存储空间。

**卡特兰数**：n个不同元素进栈，出栈元素不同排列的个数为 (1/(n+1))×C(2n,n)。卡特兰数也用于计算n个结点的二叉树不同形态数。


> 📷 原笔记该部分有配图
---

## 卡10 — 标题：出栈序列合法性判断

**降序入栈**（如6,5,4,3,2,1）：
任意数A后面比A大的数都按**升序排列**

**升序入栈**（如1,2,3,4,5,6）：
任意数A后面比A小的数都按**降序排列**

注意：确定了入栈次序并不一定能唯一确定出栈次序（并非单调出入栈时）。

---

## 卡11 — 标题：栈的应用

1. 保存程序运行时的调用过程信息
2. 采用非递归方式重写递归程序时大部分要用栈（反例：斐波那契数列迭代只需循环O(n)）
3. **前缀/后缀表达式**：
   - 前缀：先出栈的是左操作数
   - 后缀：先出栈的是**右操作数**
   - ) 不入栈
4. 括号匹配

---

## 卡12 — 标题：队列基础

队列（Queue）：先进先出（FIFO）的线性表。

**队列应用**：
- 实现二叉树的层次遍历
- 实现缓冲区，解决外设和主机之间速度不匹配问题

**队列的两种存储方式**：
- 入队端（队尾），指针指向当前元素 → 先移指针再入
- 入队端（队尾），指针指向当前元素的下一个 → 先入再移指针
- 入队只动队尾指针，front不变

**双端队列**：允许两端都可以插入和删除，左为前端右为后端。在栈中合法输出的序列在双端队列中必定合法。


> 📷 原笔记该部分有配图
---

## 卡13 — 标题：循环队列

循环队列用模运算将空间在逻辑上变为环状。

> 循环队列指的是**顺序存储**的队列，循环单链表表示的队列不能称为循环队列。

入队：Q.rear = (Q.rear+1) % MaxSize（front不动）
出队：Q.front = (Q.front+1) % MaxSize

**三种区分队空/队满的方法**：

1. **牺牲单元法**（最常用）
   - 队满：(Q.rear+1)%MaxSize == Q.front
   - 队空：Q.front == Q.rear
   - 元素个数：(Q.rear - Q.front + MaxSize) % MaxSize

2. **size变量法**：增设size变量，队空size=0，队满size=MaxSize

3. **tag变量法**：增设tag，出队成功tag=0，入队成功tag=1。刚出队不可能队满，刚入队不可能队空。


> 📷 原笔记该部分有配图
---

## 卡14 — 标题：链式队列

链式队列**不能通过队首队尾指针计算队列长度**，原因：
- 节点在内存中动态分配，节点之间通过指针连接
- 没有固定索引关系，front和rear不是数组索引
- 只能通过遍历来计算长度（类似链表）

---

## 卡15 — 标题：特殊矩阵的压缩存储

**压缩存储**：为多个值相同的元素只分配一个存储空间，对0元素不分配空间。

**对称矩阵**：aij = aji，只需存储上三角或下三角（含对角线）。

**三角矩阵**：除了三角区和对角线外，其余元素都是相同常数。末尾多用一个位置存储常数c。

**三对角矩阵**：非零元素集中在主对角线及其相邻两侧。压缩下标映射：k = 2i + j - 3（下标从0开始）；2i + j - 2（下标从1开始）。

---

## 卡16 — 标题：稀疏矩阵

稀疏矩阵：非零元素很少且分布无规律。

**顺序存储——三元组**：保存<行,列,值>。不仅要保存三元组表，还要保存矩阵的行数、列数和非零元素个数。

**链式存储——十字链表法**：每个节点包含行、列、值和两个指针（指向同行下一个非零元素和同列下一个非零元素）。


> 📷 原笔记该部分有配图
---

## 卡17 — 标题：三维数组存储

三维数组遵循原则：先定一维，再定二维，最后定三维（线→面→体）。

对于 B[i][j][k]：
- 行优先时，**最右下标变化最快**（k先变，然后j，最后i）
- 列优先时，**最左下标变化最快**（i先变，然后j，最后k）

---

## 卡18 — 标题：字符串与朴素模式匹配

字符串简称串，由0个或多个字符组成的有序序列。和线性表逻辑结构类似，区别在于**数据对象只能是字符**。

**朴素模式匹配算法**（暴力法）：
- 主串长n，模式串长m
- 将主串中所有长度为m的子串依次与模式串对比
- 最多对比 n-m+1 个子串，每趟最多m次比较
- 最坏时间复杂度：**O(mn)**
- 若匹配失败，主串指针i指向下一个子串第一个位置，模式串指针j回到第一个位置

---

## 卡19 — 标题：KMP算法

KMP算法利用匹配失败前已扫描字符的匹配信息，避免主串回溯。

- **next数组只和模式串有关，与主串无关**
- next数组值 = 最长相等前后缀长度 + 1
- next[1] = 0，next[2] = 1（开头是0的情况下）
- 最坏时间复杂度：**O(m+n)**
  - 求next数组：O(m)
  - 模式匹配：O(n)
- KMP优化：将next[j]修正为next[next[j]]，直到字符不相等，得到nextval数组
- nextval[1] = 0

> 右滑位数 = j - next/nextval[i]


> 📷 原笔记该部分有配图
---

## 卡20 — 标题：树的基本概念

结点深度 = 结点所在层次（从上往下数）
结点高度 = 以该结点为根的子树高度（从下往上数）
结点度 = 有几个分支（有几个孩子）
树的度 = 结点度的最大值

**基本公式**：
1. 结点数 = 度数之和 + 1
2. 度为m，第i层至多有 m^(i-1) 个结点
3. 高度为h的m叉树，至多有 (m^h - 1)/(m - 1) 个结点
4. n个结点，n-1条边

深度优先搜索：优先访问孩子节点
广度优先搜索：优先访问兄弟节点

---

## 卡21 — 标题：二叉树的性质

**基本公式**：
- 非空二叉树第k层最多有 2^(k-1) 个结点（k≥1）
- 高度为h的二叉树至多有 2^h - 1 个结点
- n₀ = n₂ + 1（叶子结点数 = 度为2的结点数 + 1）
- n个结点，n+1个空指针

**叶结点个数n₀ = (m-1)n_m + 1 推导**：
1. n = n₀ + n_m
2. n = e + 1（顶点数 = 边数 + 1）
3. e = m × n_m
4. m×n_m + 1 = n₀ + n_m → n₀ = (m-1)n_m + 1


> 📷 原笔记该部分有配图
---

## 卡22 — 标题：完全二叉树

完全二叉树允许在满二叉树基础上去掉若干个编号更大的结点。

**常用公式**：
- 高度 h = ⌈log₂(n+1)⌉ 或 ⌊log₂n⌋ + 1
  - 由 2^(h-1) - 1 < n ≤ 2^h - 1 或 2^(h-1) ≤ n < 2^h 得来
- 编号为i的结点所在层次：⌊log₂i⌋ + 1
- 若 i ≤ ⌊n/2⌋，则为分支结点，否则为叶结点
- 叶结点只可能在层次最大的两层出现
- 若有度为1的结点，只可能有一个

---

## 卡23 — 标题：二叉树的遍历

**先序遍历（NLR）**：根 → 左 → 右
**中序遍历（LNR）**：左 → 根 → 右
**后序遍历（LRN）**：左 → 右 → 根
**层序遍历**：根入队 → 队头出队访问 → 左孩子入队 → 右孩子入队，循环至队空

**结论**：
- 交换左右子树后，中序遍历的升序序列会变成降序序列
- 先序序列和后序序列正好相反 → 二叉树只有根节点，或只有左子树/右子树
- 中序遍历最后一个结点一定是从根开始沿右子女指针走到底的结点
- 已知前序+中序或后序+中序 → 可唯一确定二叉树
- 前序+后序 → **不能**唯一确定二叉树

**做题方法**：通过前序/后序/层序找到根结点，再根据中序序列划分左右子树。

---

## 卡24 — 标题：卡特兰树的应用

卡特兰数：Catalan = (1/(n+1)) × C(2n, n)

三种应用（都是同一个数学结构）：
1. n个结点的二叉树的**不同形态数**
2. 1,2,3,...,n 入栈的**出栈序列数**
3. 1,2,3,...,n 为先序遍历序列的二叉树**形态数**

---

## 卡25 — 标题：线索二叉树

n个结点的二叉树有n+1个空指针，利用这些空指针存放前驱/后继指针，就是线索。

线索化的实质：遍历一次二叉树。

**tag标志位**：=0表示指向孩子，=1表示是线索

核心结论：
- **前不能前**：前序线索二叉树没有有效算法通过指针找到前驱
- **后不能后**：后序线索二叉树没有有效算法通过指针找到后继
- 想要查询前驱/后继就必须知道双亲结点
- 后序线索树的遍历需要使用**栈**


> 📷 原笔记该部分有配图
---

## 卡26 — 标题：树/森林的存储结构

1. **双亲表示法**（顺序存储）
   - 在每个结点中增设一个伪指针，指示其双亲在数组中的位置
   - 根结点伪指针为-1
   - 缺点：找孩子不方便，只能从头遍历数组
   - 也可表示森林

2. **孩子表示法**（顺序+链式）
   - 缺点：和双亲表示法相反（找双亲不方便）

3. **兄弟表示法**（二叉链式存储）
   - 也叫二叉树表示法，也可表示森林


> 📷 原笔记该部分有配图
---

## 卡27 — 标题：树/森林的遍历与转换

**树的遍历**（只有两种）：
- 先根遍历：先访问父节点，再访问子节点
- 后根遍历：先访问子节点，再访问父节点

**森林的遍历**本质就是树的遍历。

**WPL（带权路径长度）**：
- 定义：树中所有叶结点的带权路径长度之和
- 另解：树中所有非叶结点的权值之和

**树→二叉树**：左指针挂大儿子，右指针串兄弟
**森林→二叉树**：每棵树先转二叉树，然后根节点右指针串起来
**二叉树→森林**：根节点右指针拆下来作多棵树根，再层序恢复

非终端节点：除叶子节点以外的节点。


> 📷 原笔记该部分有配图
---

## 卡28 — 标题：哈夫曼树

**核心结论**：
1. 结点总数为 2n-1（奇数），n个叶节点，n-1个双分支节点
2. WPL一定最优（**唯一**），但树不唯一（权值重复时高度也可能不唯一）
3. n路平衡归并就是n叉哈夫曼树，需要加虚拟归并段（长度0）
4. 初始结点最终都成为叶子结点
5. 哈夫曼编码：没有一个编码是另一编码的前缀（前缀编码）

**判定某字符集的不等长编码是否具有前缀特性**：任一编码都不是其他任何编码的前缀。

---

## 卡29 — 标题：并查集

用于处理没有重复元素的集合，实现**合并**和**查找**两个功能。

存储结构：数组 + 树（森林）的**双亲表示法**。

**应用**：
1. 判断图的连通分量数
2. 判断图是否存在环
3. 实现克鲁斯卡尔算法

已经连通的子图中，但凡再多出一条边，这个子图就一定有环。

**两种优化**（核心思想：把树变矮）：
1. 改进Union（小树合并到大树）
2. 改进Find（路径压缩，查找路径上的所有结点挂在根节点下）

优化后Find操作复杂度：O(α(n))，α(n)是反阿克曼函数，实用范围α(n) ≤ 4。


> 📷 原笔记该部分有配图
---

## 卡30 — 标题：图的基本概念

**连通**：两个顶点有路径存在
**连通图**：无向图中任意两个顶点都连通
**强连通图**：有向图中任意两个顶点互相都有路径
**连通分量**：无向图中的极大连通子图
**强连通分量**：有向图中的极大连通子图（有环路）
**完全图**：任意两个顶点都存在边
**生成树**：包含全部顶点的一个极小连通子图，n个顶点有n-1条边

> 若一个图有n个顶点且有大于n-1条边，则此图一定有环。
> 图的顶点集一定是非空集，边集可以是空集。

**极小连通子图**：连通且不形成环路所需的最少边数（n-1条）
**极大连通子图**：不被任何别的子图所包含的连通子图

---

## 卡31 — 标题：图的存储结构

**邻接矩阵**：
- 空间复杂度 O(|V|²)
- 唯一（信息在矩阵中有确定位置）
- 适合稠密图
- 若对角线一边全为0，则必不存在环

**邻接表**：
- 无向图 O(|V|+2|E|)，有向图 O(|V|+|E|)
- 不唯一（取决于读入边的顺序和插入算法）
- 适合稀疏图
- 找有向图的入边不方便

**邻接表比邻接矩阵更高效的算法**：拓扑排序、广度优先、深度优先、Prim

**十字链表**：只能存有向图
**邻接多重表**：存储无向图


> 📷 原笔记该部分有配图
---

## 卡32 — 标题：图的遍历（DFS/BFS）

都需要设置visit数组作为标记数组。

**DFS——判断有向图/无向图是否有环**
**BFS——判断无向图是否有环**

**广度优先（BFS）**：
- 类似层序遍历，是非递归算法
- 邻接矩阵：O(V²)；邻接表：O(V+E)
- 连通图→广度优先生成树；非连通图→广度优先生成森林
- 邻接矩阵→生成树唯一；邻接表→不唯一

**深度优先（DFS）**：
- 类似先序遍历，是递归算法（需要回退，靠递归工作栈）
- 空间复杂度来自递归工作栈
- 对连通图→深度优先生成树；否则→生成森林

对于无向图，调用BFS或DFS的次数就是连通分量数。
对于有向图，若起始顶点到所有顶点都有路径，则只需调用一次。

---

## 卡33 — 标题：最小生成树

**Prim算法**（适合边稠密图）：
- 任取一顶点加入树T，每次将代价最小的新顶点纳入，直到所有顶点纳入
- 时间复杂度：O(V²)

**Kruskal算法**（适合边稀疏图）：
- 每次选权值最小的边，使两头相通（已连通则不选），直到所有结点连通
- 步骤：边表排序 + 遍历 + 判环
- 时间复杂度：O(E·log₂E)

**重要结论**：
- 无向连通图各边权值都不同→最小生成树唯一
- 最小生成树的边数 = 顶点数 - 1
- **最小生成树中某顶点到其余各顶点的路径不一定具有最短路径的性质**


> 📷 原笔记该部分有配图
---

## 卡34 — 标题：最短路径

**Dijkstra**（单源最短路径）：
- 设置集合S记录已求最短路径的顶点，每次找距源点最近且未归入的点
- 设置final、dist、path三个数组
- 时间复杂度 O(V²)
- **不能处理负权值的带权图**

**Floyd**（各顶点之间最短路径）：
- 可以处理不带环的负权值
- **不能处理负权回路**的图（路径总权值可以无限减小）
- 时间复杂度 O(V³)
- 新的Path矩阵可能将旧的矩阵中非无穷元素更新为更小值

**求最短路径的算法**：Dijkstra、BFS（要求权值相等或无）、Floyd
> 最短路径一定是简单路径。

---

## 卡35 — 标题：拓扑排序与AOV网

AOV网：有向无环图（DAG），有向边表示活动先后顺序。

拓扑排序：
1. 从AOV网中选择一个没有前驱的顶点输出
2. 删除该顶点和所有以它为起点的有向边
3. 重复直至AOV网为空（若不存在无前驱的顶点→有环）

**逆拓扑排序**：前驱→后继，以它为起点→以它为终点。可用DFS实现。

注意事项：
- 拓扑序列可能不唯一
- 若图中有环，则不存在拓扑排序
- **强连通图和拓扑排序互斥**
- 按拓扑排序重新编号后，邻接矩阵是**三角矩阵**

---

## 卡36 — 标题：关键路径

AOE网中，关键路径是权值之和最大的路径。

**求解步骤**：
1. **事件最早发生时间ve(k)**：正向拓扑序，取前驱ve+权值的最大值（等最慢的人）
2. **事件最迟发生时间vl(k)**：逆向拓扑序，取后继vl-权值的最小值（保证后续按时启动）
3. **活动最早发生时间** = 活动开头结点的最早开始时间
4. **活动最迟发生时间** = 活动末尾结点的最晚开始时间 - 活动长度
5. **时间余量** = 最迟 - 最早，余量为0的活动属于关键路径

> 考试时建议穷举法找关键路径。


> 📷 原笔记该部分有配图
---

## 卡37 — 标题：有向无环图描述表达式

有向无环图是描述含有公共子式的表达式的有效工具，可实现**相同子式的共享**，节省存储空间。

例如表达式 (A+B)×((A+B)/A)：
1. 先画出二叉树
2. 合并重复子表达式（如(A+B)）
3. 相同叶子节点可共用
4. 至少需要5个顶点：×、+、/、A、B各一个

对树进行中序遍历可得中缀表达式，有子表达式则加一层括号，根节点和操作数不加括号。

---

## 卡38 — 标题：折半查找判定树

折半查找只适用于**有序顺序表**。

判定树本质上是**平衡二叉树**。如果高度为h，前h-1层一定是满二叉树，只有最下面一层不满。这个特性可用于判断是否是合法的折半查找判定树。

**查找长度**：
- 成功结点的查找长度 = 自身所在层数
- 失败结点的查找长度 = 其父节点所在层数

树高 h = ⌈log₂(n+1)⌉（查找失败时最多比较次数）
时间复杂度：O(log₂n)

**向上取整 vs 向下取整**：取整方向不同，左右子树节点数量不同，判定树也不一样。

---

## 卡39 — 标题：顺序查找与分块查找

**顺序查找**：第一个元素找1次，第二个找2次，以此类推。查找成功的平均时间与是否有序无关。

**分块查找**：
- 块内不必有序，但块间要求有序（第1块最大<第2块最小<第3块最小...）
- 选取各块中的最大关键字构成索引表

---

## 卡40 — 标题：二叉排序树（BST）

左子树 < 根 < 右子树（递归定义）。

**删除操作**：
1. 叶节点直接删除
2. 有一个子树→子树代替
3. 有两个子树→右子树中序第一个子女填补（右子树最小结点）

**查找效率**取决于树的高度。

**BST vs 二分查找**：
- 平均时间性能差不多
- 二分查找的判定树唯一，BST因插入顺序不同而不唯一
- 静态存储表→二分查找；动态存储表→BST
- BST插入删除O(log₂n)，二分查找插入删除O(n)

---

## 卡41 — 标题：平衡二叉树（AVL）

树上任一结点的左子树和右子树的深度之差不超过1。

最少结点数递推：n_h = n_(h-2) + n_(h-1) + 1

四种调整：
- **LL型**→右旋 | **RR型**→左旋
- **LR型**→先左旋再右旋 | **RL型**→先右旋再左旋

通俗理解：**右旋**（"新大哥右老婆跑路"），**左旋**（"新大哥左老婆跑路"）

**不平衡向上传递**：对最小不平衡子树的旋转可能导致树变矮，从而使上层祖先不平衡。

---

## 卡42 — 标题：红黑树

**口诀：左根右，根叶黑，不红红，黑路同**

性质：
1. 每个结点红色或黑色
2. 根结点是黑色的
3. 叶结点（外部结点/null结点）都是黑色的
4. 不存在两个相邻的红结点
5. 从任一结点到任意叶结点的路径上，黑结点数量相同

**结论**：
- n个内部结点的红黑树高度 h ≤ 2log₂(n+1)
- 最长路径 ≤ 最短路径的2倍
- 插入最多旋转**两次**，删除最多旋转**三次**
- 红黑树适合频繁插入/删除，但查找效率低于平衡二叉树（都是O(log₂n)）

---

## 卡43 — 标题：B树

m阶B树：所有结点的平衡因子均等于0，最多有m个分支的平衡查找树。

**性质**：
- n个关键字的B树必然有 n+1 个叶结点（代表查找失败）
- 算高度不算失败结点（叶结点）
- B树的叶结点对应查找失败情况

**插入**：插入位置一定是最底层的非叶节点。中间元素上移，两边分裂。

**删除**：涉及节点的合并。直接前驱=左子树最大元素，直接后继=右子树最小元素。

B树的最高高度：h ≤ ⌈log_(⌈m/2⌉)⌉((n+1)/2) + 1，同时也是读磁盘最多次数。


> 📷 原笔记该部分有配图
---

## 卡44 — 标题：B+树

**对比B树**：
- B+树的非叶节点仅起**索引作用**
- B树的叶结点关键字和其他结点包含的关键字是**不重复**的
- B+树支持**顺序查找和随机查找、范围查找**，B树只支持随机查找

其他特点：
- 每个分支结点最多有m个子树
- 结点的子树个数和关键字个数相等
- 叶结点才是终点，非叶节点只是索引
- B+树适合做数据库索引和文件索引，磁盘读写代价比B树还低

B树和B+树的叶结点**都必须在同一高度**。


> 📷 原笔记该部分有配图
---

## 卡45 — 标题：散列表

散列表（哈希表）：根据数据元素的关键字计算它在散列表中的存储地址。

散列函数（哈希函数）：把关键字映射成对应地址的函数，记为 Hash(key) = Addr。

**事实：任何散列函数都不可能绝对避免冲突。**

**构造方法**：
1. 直接定址法：取关键字的线性函数值为散列地址
2. **除留余数法★★★**：H(key) = key % p，p是≤表长I的最大质数（注意p不是表长）
3. 数字分析法
4. 平方取中法：取关键字平方值的中间几位


> 📷 原笔记该部分有配图
---

## 卡46 — 标题：散列冲突处理——开放定址法

**线性探测法**（di = 1,2,3,...,m-1）：冲突时顺序查看下一单元。会导致**堆积**（大量元素聚集在相邻地址）。

**平方探测法**（二次探测法，di = 1²,-1²,2²,-2²,...）：可避免堆积。但**不能探测到所有单元，至少能探测一半**。散列表长度m必须是4k+3的素数。

**双散列法**：di = i × Hash₂(key)，H_i = (H(key) + i×Hash₂(key)) % m

**伪随机序列法**：di = 伪随机数序列

> 堆积现象因冲突而产生，**对存储效率、散列函数和装填因子均不会有影响**。
> 散列表的平均查找长度与装填因子直接相关。

---

## 卡47 — 标题：散列冲突处理——拉链法

也称链接法（链地址法）。每个单链表由一个或多个**同义词**构成。

**ASL计算**：
- 查找成功的ASL：分母是**元素个数**
- 查找失败的ASL：分母是**mod后的值**（散列函数能得到的地址数）
- 拉链法查找失败时，和空节点比较**不需要+1**（比较指针，未实际访问节点）

开放定址法中不能随便删除元素（会截断查找路径），要采用**逻辑删除**（加删除标记）。

---

## 卡48 — 标题：排序的基本概念

**内部排序**：元素全部存放在内存中的排序
**外部排序**：元素无法同时存放在内存，需在内、外存之间移动

**稳定性**：经过排序后，能使关键字相同的元素保持原顺序中的相对位置。

---

## 卡49 — 标题：插入排序

**直接插入排序**：
- 第一趟从第二个元素开始排
- 空间O(1)，最好O(n)（有序），最坏O(n²)（逆序），平均O(n²)
- **稳定**，适合顺序和链式存储

**折半插入排序**：
- 用折半查找找插入位置，只适合顺序表
- 当mid元素指向当前元素时，仍需令low=mid+1保证稳定性
- 最终插入low所指位置（即high+1）
- 时间复杂度约O(n·log₂n)，但数据量大时仍为O(n²)

**希尔排序**：
- 按一定增量分组，各组内直接插入排序，再缩小增量重复
- 只可用于顺序表，**不稳定**，空间O(1)


> 📷 原笔记该部分有配图
---

## 卡50 — 标题：交换排序

**冒泡排序**：
- 两两比较相邻元素，逆序则交换
- 最多n-1趟，最少1趟（某趟未出现交换可提前结束）
- **稳定**，适合顺序表和链表

**快速排序**：
- 选枢轴，right从右找小于枢轴的填left坑，left从左找大于枢轴的填right坑
- 只适合顺序存储，**不稳定**
- 最坏O(n²)（初始有序或逆序），平均O(n·log₂n)
- **所有内部排序中最优的排序算法**
- 可以**并行执行**（每趟划分的子序列互不影响）

两趟快排后至少确定几个元素位置：
- 基准在首/尾→至少确定2个
- 基准不在首/尾→至少确定3个


> 📷 原笔记该部分有配图
---

## 卡51 — 标题：选择排序

**简单选择排序**：
- 每轮选最小元素，与第i个位置交换
- **不稳定**，适合顺序表和链表
- 空间O(1)，时间复杂度始终O(n²)

**堆排序**：
- 前提：完全二叉树
- 建堆O(n)，每次调整O(log₂n)，总O(n·log₂n)
- **不稳定**
- 适合关键字较多的情况（如从1万个数中选前100个最大：最小选大根，最大选小根）
- 堆排序可以**并行执行**（左右子树互不影响）
- 堆排序查找效率最低（堆是用来排序的，查找时无序）
- 插入、删除时间复杂度O(log₂n)
- 下坠关键字对比：左右孩子先比较→父节点再和选出的孩子比较


> 📷 原笔记该部分有配图
---

## 卡52 — 标题：归并排序与基数排序

**归并排序**（二路）：
- 空间O(n)，每趟O(n)，共⌈log₂n⌉趟，总O(n·log₂n)
- **稳定**
- 合并两个长m和n的有序表：最坏比较m+n-1次，最好比较min(m,n)次

**基数排序**：
- 不基于比较和移动，基于关键字各位大小排序
- 与序列的初始状态**无关**
- **稳定**
- 适合顺序和链式存储
- 需d趟分配和收集，总O(d(n+r))
- **不能对float和double类型排序**


> 📷 原笔记该部分有配图
---

## 卡53 — 标题：排序口诀汇总

**稳定性**（不稳的）：
"选希堆快"——选择、希尔、堆、快速

**空间复杂度O(1)**：
"简单插入人堆冒个泡"——简单选择、插入、堆、冒泡、希尔

**时间复杂度与初始状态无关**：
"一堆乌龟选基友"——选择、堆、归并、基数

**比较次数与初始状态无关**：选择、基数

**移动次数与初始状态无关**：归并、基数

**排序趟数与初始状态有关**：快速、冒泡

**每步至少将一个放在最终位置**：
"贱猫对快轨"——简单选择、冒泡、堆、快速、归并

**更适合顺序存储的**：
"西巴堆块"——折半插入、希尔、堆、快速

**只适合顺序存储的**：折半插入、希尔、快速
（堆排序顺序和链式都行，但更适合顺序）

---

## 卡54 — 标题：外部排序

外部排序通常采用**归并排序**方法。

**k路平衡归并**：
- 生成r个初始归并段→进行s趟归并，s = ⌈log_k r⌉
- 内存中分配k个输入缓冲区 + 1个输出缓冲区
- 外部排序时间主要消耗在内外存数据交换上

**优化思路**：
1. 增加归并路数k→需增加输入缓冲区，引入**败者树**减少关键字对比次数
2. 减少初始归并段数量r→引入**置换-选择排序**

败者树：
- 作用：多路归并时减少关键字对比次数
- 构造k路败者树需要对比 k-1 次
- 每次找最小关键字最多需要 ⌈log₂k⌉ 次对比
- 败者树是完全二叉树，k个叶结点对应k个归并段

**置换-选择排序**：
- 生成更长的初始归并段
- 得到的初始归并段平均长度为内存工作区大小的**2倍**


> 📷 原笔记该部分有配图
---

## 卡55 — 标题：最佳归并树

本质上是**哈夫曼树的推广**。

每个初始归并段对应一个叶子结点，归并段的块数作为叶子的权值。
**归并过程中磁盘I/O次数 = 归并树的WPL × 2**

**严格k叉树条件**：
- 设n₀为度为0的结点（初始归并段），n_k为度为k的结点
- n₀ = (k-1)n_k + 1 → n_k = (n₀-1)/(k-1)
- 若 (n₀-1) % (k-1) = 0，不需要添加虚段
- 若余数 u ≠ 0，需补充 (k-1)-u 个虚段

虚段是含有0个磁盘块的归并段，不影响I/O次数。
