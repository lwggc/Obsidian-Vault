# 散落知识点汇总 — 做卡片时建议合并

> 同一个知识点在你的笔记中分散在多处，做卡片时建议合并成一张卡。

---

## 📗 数据结构

### 1️⃣ 卡特兰数 — 分散在2处
- **栈章节**：n个元素进栈，出栈排列数 = (1/(n+1))×C(2n,n)
- **二叉树章节**：n个结点的二叉树不同形态数、先序序列确定的二叉树形态数
> 本质是同一个数学公式，可合并为一张卡"卡特兰数的三个应用"

### 2️⃣ 并查集 — 分散在2处
- **"未考过考点"区域**：并查集的三个应用（判断连通分量、判断环、Kruskal）
- **并查集专门章节**：存储结构（数组+树双亲表示法）、Find/Union操作、路径压缩优化
> 建议合并为一张卡，前面写应用场景，后面写实现和优化

### 3️⃣ B树B+树共同性质 — 分散在2处
- **B树章节**：所有叶结点出现在同一层次
- **B+树章节**：也说了叶结点在同一层次（但在B+树中没强调这个）
- **408总结**："b树b+树的叶结点都必须在同一高度"
> 可在B+树卡里补充"和B树共有"这条

### 4️⃣ WPL的两种定义 — 分散在2处
- **树章节定义**：树中所有叶结点的带权路径长度之和
- **408总结另解**：树中所有非叶结点的权值之和
> 两种定义等价，可合并到一张卡

### 5️⃣ 哈夫曼树 → 最佳归并树 — 分散在2处
- **哈夫曼树章节**：构造、WPL、前缀编码
- **外部排序章节**：最佳归并树（本质是哈夫曼树的推广）、虚段计算
> 建议做成关联卡片："最佳归并树本质上是哈夫曼树的推广"

### 6️⃣ 折半查找判定树 — 分散在2处
- **查找章节**：判定树是平衡二叉树、ASL计算
- **408总结**："前h-1层一定是满二叉树"、"只有最下面一层是不满的"
> 这两个特性加在一起才是完整的判定树画像

### 7️⃣ 排序口诀汇总 — 分散在1处但内容多
- **内部排序的比较**段落分散着多句口诀：
  - 不稳定："选希堆快"
  - 空间O(1)："简单插入人堆冒个泡"
  - 时间与初始状态无关："一堆乌龟选基友"
  - 每步确定最终位置："贱猫对快轨"
  - 更适合顺序存储："西巴堆块"
> 建议合并成一张口诀汇总卡

### 8️⃣ 败者树 vs 归并树 — 分散在2处
- **败者树**：减少关键字比较次数，构造需k-1次比较
- **最佳归并树**：哈夫曼树推广，虚段计算

---

## 📕 计算机组成原理

### 1️⃣ 溢出判断 — 分散在2处
- **加减运算章节**：三种溢出判断方法（一位符号位、双符号位、进位位）
- **ALU/标志位章节**：OF、CF的定义和计算
> 注意：你的笔记中Card #63说"双符号位然后左移"是错误的（详见之前对比报告）

### 2️⃣ 标志位OF/SF/ZF/CF — 分散在3处
- **ALU章节**：各标志位的定义（ZF/OF/SF/CF）
- **加减运算章节**：溢出判断方法
- **408总结**："有符号运算只看OF SF ZF，无符号只看ZF CF"
> 建议合并为一张"标志位完整指南"卡

### 3️⃣ 中断 — 分散在3处
- **CPU章节**：中断隐指令（关中断、保存断点、发中断响应信号）
- **I/O章节**：程序中断方式、中断响应条件
- **多重中断**：响应优先级 vs 处理优先级、中断屏蔽字
> 注意与OS中的中断概念有交叉

### 4️⃣ IEEE754舍入 — 分散在2处
- **浮点数章节**：0舍1入法、恒置1法、截断法
- **408总结**："舍入——右规和对阶才需要舍入"
> 你对阶也需要舍入，漏了这个

### 5️⃣ Cache与TLB工作流程 — 分散在2处
- **Cache章节**：Cache映射、替换、写策略
- **虚拟存储器章节**：TLB→页表→Cache→主存的完整流程
> 你的笔记有张完整流程图

### 6️⃣ DMA内部控件 — 分散在2处
- **DMA章节**：DMA工作流程、三种传送方式
- **408总结**：DMA内部控件清单（AR、WC、DR、CR、DMA请求触发器、控制状态逻辑）

### 7️⃣ 有符号乘法vs无符号乘法 — 分散在2处
- **乘法运算章节**：原码乘法、Booth算法
- **408总结**："高n位全为0→不溢出，有符号：高n位全为0/1→不溢出"

---

## 📘 操作系统

### 1️⃣ PV操作经典问题 — 分散在3处
- **信号量章节**：P/V原语定义、互斥和同步的模板
- **生产者消费者**：多组信号量的配合（mutex/empty/full）
- **读者写者**：三种方案（读者优先/写者优先/读写公平）
- **哲学家进餐**：死锁避免方案
> 这四个问题都是PV操作的应用，建议分别做卡但互相关联

### 2️⃣ 页面置换算法 — 分散在2处
- **页面置换算法章节**：OPT/FIFO/LRU/CLOCK的具体实现
- **影响缺页率的因素**：页面大小、物理块数、置换算法、编址方法、写回频率
> 注意你的笔记列了5个因素（该同学只列了4个，漏了写回频率）

### 3️⃣ 死锁 — 分散在5处
- 必要条件（4条）
- 预防（破坏必要条件）
- 避免（银行家算法）
- 检测（资源分配图消边简化）
- 解除（资源剥夺/撤销进程/进程回退）
> 考试常把这几个放一起考，可合并为一张"死锁完整处理策略"卡

### 4️⃣ 进程状态 — 分散在2处
- **进程章节**：五状态模型（创建/就绪/运行/阻塞/终止）
- **调度章节**：七状态模型（加入挂起就绪/挂起阻塞）
> 注意区分：挂起是调入外存，阻塞还在内存

### 5️⃣ 目录结构演进 — 分散在4个子节
- 单级目录（有重名问题，查找慢）
- 两级目录（不同用户可重名，但不能分类）
- 树形目录（解决分类，不能共享）
- 无环图目录（解决共享）
> 你笔记中总结的演进逻辑链非常清晰，适合做一张卡

### 6️⃣ 虚拟内存特征 — 分散在1处但容易漏
- 你笔记写了**四个**特征：多次性、对换性、虚拟性、**非连续性**
- 该同学只写了三个（漏了非连续性）
> 卡上一定要写上四条

---

## 📙 计算机网络

### 1️⃣ TCP拥塞控制 — 分散在4处
- **慢开始**：cwnd从1开始，每RTT翻倍
- **拥塞避免**：每RTT加1
- **快重传**：收到3个冗余ACK立即重传
- **快恢复**：收到3个冗余ACK后cwnd=ssthresh（不降为1）
> 注意区分子**超时重传**（cwnd=1）和**快重传**（cwnd=ssthresh）

### 2️⃣ 路由协议 — 分散在3处
- **RIP**：距离向量，UDP 520，跳数≤15，好消息快坏消息慢
- **OSPF**：链路状态，IP 89，Dijkstra，划分区域
- **BGP**：路径向量，TCP 179，自治系统间
> 可做一张"三大路由协议对比"卡

### 3️⃣ IP地址 — 分散在4处
- **分类编址**：A/B/C/D/E类
- **子网划分**：定长/变长
- **CIDR**：IP/前缀长度，最长前缀匹配
- **NAT**：私有IP→全球IP的转换
> 这是网络层一条线下来的，建议做一张"IP地址演进"卡

### 4️⃣ 三次握手 vs 四次挥手 — 分散在2处
- 三次握手（建立连接）：SYN→SYN+ACK→ACK
- 四次挥手（释放连接）：FIN→ACK→FIN→ACK
> 可以做一张对比卡，标注序号消耗、SYN/FIN是否消耗序号

### 5️⃣ 不发送ICMP的情况 — 分散在1处但内容零散
- IP首部出现错误
- 对ICMP差错报告报文不发送（不叠盒子）
- 对分片的后序报片不发送
- 对多播地址不发送
- 对特殊地址不发送
> 这些是散列在同一段落的多个要点，适合做成一张完整的卡
