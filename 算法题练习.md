

# 代码

[数据结构算法模板](https://www.lucenczz.top/article/af9dbf29-83b5-417a-ac0a-adbb6e721e02)

## 1.线性表的结构体定义

### 1.1顺序表的结构体定义

```c++
#define MAXSIZE 100  // 定义顺序表的最大长度

typedef struct{
    int data[MAXSIZE];  //存储顺序表的数组
    int length;  //当前顺序表的长度
}Sqlist；  //定义顺序表的结构体类型
```

### 1.2单链表的结构体定义

```c++
struct ListNode {	//结构体ListNode代表链表节点
 	int val;   //结点的值
 	struct ListNode   *next; // 指向下一节点的指针；
}；
```

> [!important] 重点
> 指针next的类型是struct ListNode*，表示指向另一个ListNode结构体的指针

使用这个定义可以创建如下链表

```c++
struct ListNode* head =(struct ListNode*)malloc(sizeof(struct ListNode)); //创建头结点
head->val =1 ; //设置头结点的值
head->next= NULL;	//头结点的指针指向NULL

struct ListNode*node1 =(struct ListNode*)malloc(sizeof(struct ListNode)); //创建第一个节点
node1->val =2;	//设置第一个节点的值
node1->next = NULL;	//第一个节点的指针指向NULL
head->next=node1;	//头结点的指针指向第一个节点

struc ListNode*node3 =(struct ListNode*)malloc(sizeof(struct ListNode));  //创建第二个节点
node2->val=3; //设置第二个节点的值
node2->next= NULL;  //第二个节点的值指向NULL
node1->next =node2;  //第一个节点的指针指向第二个节点
```

> [!important] 重点
> 这是带头结点的单链表的简单创建，节点的值分别为1，2，3

### 1.3双链表的结构体定义

双链表的创建，要有两个指针，分别指向前驱和后继。但是头结点和尾结点要记住是特殊的

```c++
struct ListNode{
    int val;   //结点的值
        struct ListNode *prev;	//指向前一个结点的指针
        struct ListNode *next;	//指向后一个结点的指针
            };
```



## 2.顺序表的基本操作

### 2.1初始化顺序表

```c++
int init_list(SqList *L,int maxsize){
	L->data=(int *)malloc(maxsize * sizeof(int));    //动态分配内存；
         if(L->data==NULL){  //   !L->data等价于L->data==NULL
             	return 0; //内存分配失败
         }
         L->length = 0;//初始化长度为0
         L->maxsize=maxsize; //初始化最大长度
         return 1;  //初始化成功
}
```

> [!important] 重点
> 定义一个SqList*类型的指针L，指向一个顺序表结构体的指针。通过这个指针，函数可以直接操作外部传入的结构体变量，定义了maxsize表示顺序表的最大容量，即最多能存储多少个元素

### 2.2求指定元素位置

```c++
int locate_elem(SqList *L ,int elem){
	for(int i =0;i < L->length;i++){  //遍历顺序表中的每一个元素
		if(L->data[i]==elem){   //如果发现数据i和elem相等
			return i;   //返回i
		}
	}
	return -1; //没有找到元素
}
```

> [!important] 重点
> 这个函数接受了一个指向顺序表的指针L和一个元素elem作为参数，然后遍历顺序表中的元素，查找是否有与elem相等的元素。如果找到了，则返回该元素在顺序表中的位置，否则返回-1

### 2.3插入数据元素

```c++
int insert_elem(SqList *L,int pos ,int elem ){   //用指针L指向顺序表，pos表示要插入的位置，elem为值
    	if(pos<1 || pos > L-> length+1){   //如果插入位置超过了表的大小，则不合法
                    return 0；
        }
   		if(L->length >= L->maxsize){   //如果当前顺序表已满，无法插入
            	return 0;
        }
    	for（int i=L->length -1;i >= pos -1 ;i--){  //从表尾开始，挨个遍历pos及以后的元素
            	L->data[i+1]=L->data[i];   //将pos及后面的元素后移一位
        }
        L->data[pos -1] =elem;  //将新元素插入到pos位置
        L->length++；  //长度加1
        return 1；  //插入成功
}
```

### 2.4按元素值查找

```c++
int search_elem(SqList *L, int elem) {  
    for(int i=0;i<L->length;i++){
        if(L->data[i]==elem){
            return i+1;  //返回元素在顺序表中的位置
        }
    }
    return 0;  //没有找到元素
}
```

> [!important] 重点
> 这个函数接受一个指向顺序表的指针L和一个元素elem作为参数，遍历顺序表中的元素，找到第i个元素如果和elem相等，就返回i+1，没找到就返回0

### 2.5删除数据元素

```c
int delete_elem(SqList *L ,int pos){
	if(pos<1 || pos> L->length){
		return 0; //删除位置不合法
	}
	for(int i= pos;i<L->length;i++){  //找到pos位置了，对他后面的元素进行迁移
    	L->data[i-1]=L->data[i];//往前移位
	}
         L->length--; ///长度减一
         return 1; //删除成功
}
```

> [!important] 重点
> 该函数接受了一个指向顺序表的指针L和一个指定的删除位置参数pos，如果pos给定的位置不对，直接判不合法。合法后再谈其他的，对pos后序元素都进行前移1位的操作，这样的话也不用主动删除pos位置的元素，移位时直接被覆盖了。最后总表长度-1，返回1表示删除成功

### 2.6顺序表的元素逆置

```c++
void reverse_list(SqList *L){
     int temp;   //借助temp来辅助逆置元素
     for(int i=0l;i<L->length;i++){   //对整个表遍历
      		temp=L->data[i];  //暂存第i个元素
      		L->data[i]=L->data[L->length-i-1];  // 令逆序的第i个赋值给正序第i个
      		L->data[L->length-i-1]=temp; //把temp里的元素放在里面，完成整个对调的步骤
     }
}
```

###   2.7删除下标i-j的元素

```c++
int delete_range(SqList *L, int i, int j) {
    if (i < 1 || j > L->length || i > j) {
        return 0; // 删除范围不合法
    }
    for (int k = j + 1; k <= L->length; k++) {
        L->data[k - j + i - 2] = L->data[k - 1]; // 将j+1及其后面的元素前移j-i位
    }
    L->length -= j - i + 1; // 长度减少j-i+1
    return 1; // 删除成功
}
```

### 2.8Partition操作

```c++
int partition(SqList *L,int low,int high){
    int pivot= L->data[low]; //选取第一个元素作为枢轴
    while(low<high){
        while(low<high&&L->data[high]>=pivot){ 
            high--;
        }
        L->data[low]=L->data[high];//将小于枢轴的元素移到左边
        while(low<high&&L->data[low]<=pivot){
            low++;
        }
        L->data[high]=L->data[low];//将大于枢轴的元素移到右边
    }
    L->data[low]=pivot; //将枢轴放到最终位置
    return low;//返回枢轴位置
}
```

> [!important] 重点
> partition操作是把一个顺序表分成两个部分，使得左边的部分都小于某个值，右边的部分都大于等于某个值。

## 3.单链表的基本操作

### 3.1初始化单链表

```c++
#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int data;
    struct Node *next;
} Node, *LinkedList;

LinkedList init_list() {
    LinkedList L = (LinkedList)malloc(sizeof(Node)); // 创建头结点
    L->next = NULL; // 头结点的指针域置为空
    return L;
}

int main() {
    LinkedList L = init_list(); // 初始化单链表
    return 0;
}
```

### 3.2尾插法建立单链表

```c++
LinkedList create_list() {
    LinkedList L = (LinkedList)malloc(sizeof(Node)); // 创建头结点
    L->next = NULL; // 头结点的指针域置为空
    Node *tail = L; // 尾结点初始化为头结点
    int x;
    while (scanf("%d", &x) != EOF) { // 不断读入数据，直到输入结束
        Node *new_node = (Node*)malloc(sizeof(Node)); // 创建新结点
        new_node->data = x; // 将数据存入新结点的数据域
        new_node->next = NULL; // 新结点的指针域置为空
        tail->next = new_node; // 将新结点插入到尾结点之后
        tail = new_node; // 更新尾结点
    }
    return L;
}
```

### 3.3头插法建立单链表

```c
LinkedList creat_list(){
	LinkedList L=(LinkedList)malloc(sizeof(Node)); //创建头结点
	L-next=NULL;  //头结点的指针域置为空
	int x;
	while(scanf("%d",&x)!=EOF){//不断读入数据，直到输入结束
		Node *new_node=(Node*)malloc(sizeof(Node));//创建新节点
		new_node->data =x;//将数据存入新节点的数据域
		new_node->next =L->next; //将新节点插入到头结点之后
		L->next=new_node; //更新新节点
	}
	return L;
}
```

### 3.4合并递增单链表

```c++
struct ListNode* mergeLists(struct ListNode* l1, struct ListNode* l2) {
    struct ListNode dummy;      // 创建虚拟头节点
    struct ListNode* tail = &dummy; // 创建尾节点指针
    while (l1 != NULL && l2 != NULL) { // 遍历两个链表
        if (l1->val < l2->val) { // 如果l1的值小于l2的值
            tail->next = l1;    // 将l1添加到新链表中
            l1 = l1->next;      // 移动l1指针到下一个节点
        } else {                // 如果l2的值小于等于l1的值
            tail->next = l2;    // 将l2添加到新链表中
            l2 = l2->next;      // 移动l2指针到下一个节点
        }
        tail = tail->next;      // 移动尾节点指针到新链表的尾部
    }
    if (l1 != NULL) {           // 如果l1还有剩余节点
        tail->next = l1;        // 将剩余节点添加到新链表的尾部
    } else {                    // 如果l2还有剩余节点
        tail->next = l2;        // 将剩余节点添加到新链表的尾部
    }
    return dummy.next;          // 返回新链表的头节点
}
```

> [!important] 重点
>  
>
>  | `dummy` 是虚拟头结点的原因          |
>  | ----------------------------------- |
>  | `dummy` 不赋值 val                  |
>  | `dummy` 不作为最终返回值            |
>  | 返回的是 `dummy.next`               |
>  | `tail` 指向 `&dummy` 并进行尾插操作 |
>  | `dummy` 是局部变量                  |

### 3.5将2个递增单链表合并成递减单链表

```c
void MergeList(LinkList *La,LinkList *Lb)
{
    //合并2个递增有序链表(带头结点)，并使合并后的链表递减排列
    Linklist *r,*pa=La->next,*pb=Lb->next;//分别是表La和Lb的工作指针
    La->next=NULL;//La作为结果链表的头指针，先将结果链表初始化为空
    while(pa&&pb){ //当两链表均不空时循环
        if(pa->data<=pb->data){
            r=pa->next; //r暂存pa的后继节点指针
            pa->next=La->next;//将比较之后的较小结点的next指针指向结果链表的头结点后的第一个结点
            La->next=pa; //将结果链表的头结点的next指针指向新来的"小"结点
            pa=r;//恢复pa为当前待比较节点
        }else{
            r=pb->next; //r暂存pa的后继节点指针
            pb->next=La->next;
            La->next=pb; //将pa节点与链于结点链表中，同时逆置(头插法)
            pb=r;//恢复pa为当前待比较节点
        }
        if(pa)
            pb=pa;//通常会剩一个链表非空，处理剩下的部分
        while(pb){ //处理剩下的一个链表
            r=pb->next; //r暂存pa的后继节点指针
            pb->next=La->next;
            La->next=pb; //将pa节点与链于结点链表中，同时逆置(头插法)
            pb=r;//恢复pa为当前待比较节点
        }
    free(Lb);
}
```

> [!important] 重点
> 这个代码的作用是将两个递增有序链表合并成一个递减有序链表，并且使用头插法将新节点插入到结果链表的头部。函数的参数是两个指向链表头结点的指针，函数执行后，链表Lb将被释放，而链表La则成为合并后的递减有序链表

### 3.6查找元素并删除

```c++
void delete_node(LinkedList L,int x){
 	Node*p= L->next，*pre=L;  //分别指向第一个节点和头节点
 	while(p !=NULL){  //遍历单链表
 	 	if(P->data==x){   //如果找到了要删除的节点
 	 		pre->next=p->next; //将前驱节点的指针域指向后继节点
 	 		free(p); //释放要删除节点的内存空间
 	 		return; //结束函数
 	 	}
 	 	pre=p; //更新前驱节点
 	 	p=p->next; 指向下一个节点
 	}
 	printf("未找到要删除的节点！\n");//如果遍历完整个单链表都没有找到要删除的节点，则输出提示信息
}
```

> [!important] 重点
> 这个函数使用了头结点，分别定义了两个指针p和pre，分别指向第一个节点和头节点。然后遍历单链表，如果找到了要删除的节点，则将前驱节点的指针域指向后继节点，释放要删除的节点的内存空间，最后结束函数，如果遍历完整个单链表没有找到要删除的节点，则输出提示信息

### 3.7单链表根据位置删除

```c
struct ListNode* p = head;
struct ListNode* q = NULL;
int i = 1;
while (p != NULL && i < pos) {
    q = p;
    p = p->next;
    i++;
}
if (p == NULL) {
    printf("删除位置无效\n");
    return;
}
if (q == NULL) {
    head = p->next; // 删除头节点
} else {
    q->next = p->next; // 删除中间节点
}
free(p); // 释放要删除的节点的内存
```

> [!important] 重点
> 单链表在某处删除节点的操作需要先找到要删除位置的前一个结点，然后将前一个结点的next指针指向后一个结点，释放要删除的结点的内存

> [!important] 重点
> 问：**“为什么这个根据位置删除节点的代码也要用双指针呢？”**
>
> 答：单链表是单向的，不能从当前节点直接访问他的前驱节点，所以为了删除当前节点，必须知道他的前驱节点

### 3.8对于一个递增单链表，删除其重复的元素

```c++
void delete_dupliacte(LinkedLis L ){
 	Node*p=L->next,*q;  //定义p，q指针指向第一个节点和下一个节点（下一个节点在循环里再指向）
 	while(p!=NULL){ //遍历单链表
 		q=p->next; //指向下一个节点
 		while(q!=NULL&&q->data==p->data){ //如果下一个节点的值与当前节点的值相同，就该进行删除操作了
 			Node*temp=q; //首先定义临时变量把q给存储了，弱者标记！
 			q=q->next;//让q指针移向下一个
 			free(temp);  //把标记的重复节点空间抹杀!
 		}
 		p->next=q;//将当前节点的指针域指向下一个不重复的节点
 		p=q;//指向下一个节点
 	}
 }
```

> [!important] 重点
> 该函数使用了头结点，分别定义了两个指针p和q，分别指向第一个节点和下一个节点。然后遍历单链表，如果下一个节点的值与当前节点的值相同，则释放下一个节点的内存空间，直到找到一个不重发的节点。最后将当前节点的指针域指向下一个不重复节点，然后指向下一个节点，继续遍历单链表

### 3.9删除单链表中的最小值

```c++
void delete_min(LInkedList L){
 	Node *p=L-> next,*pre=L,*min_p=p,*min_pre=pre;//定义4个指针
 	while(P!=NULL){  //当p不为空时
 		if(p->data<min_p->data){ //如果p节点数据小于当前记录最小值
 		 	min_p=p;  //更新最小值
 		 	min_pre=pre; //更新最小值前驱
 		}
 		pre=p; //pre向后移动
 		p=p->next;  //p向后移动
 	}
 	min_pre->next=min_p->next;//将min_pre的后继改为min_p的后继，越过了要删除的节点
 	free(min_p);  //释放空间
}
```

> [!important] 重点
> 这个函数使用了头结点，分别定义了4个指针p,pre,min_p,min_pre,分别指向第一个节点、头结点、最小值节点和最小值节点的前驱节点。然后遍历单链表，如果找到了比当前最小值更小的节点，则更新最小值节点和最小值节点的前驱节点。最后将最小值节点的前驱节点的指针域指向最小值节点的后继节点，然后释放最小值节点的内存空间

### 3.10单链表的原地逆置

```c
void reverseList(struct LinstNode **head){//**head 是一个指向头指针的指针。为了在函数内部修改头指针本身
	if(*head == NULL || (*head)->next==NULL){//如果链表为空或只有头结点，直接返回
		return;
	}
	struct ListNode *prev=NULL,*curr=*head,*next=NULL;  //定义3个节点
	while(cur!= NULL){  //遍历链表，将每个节点的后继指针指向前一个结点
		next=curr->next;  
		curr->next=prev;
		prev=curr;
		curr=next;
	}
	*head=prev； //将头指针指向逆置后的链表的头结点
}
```

### 3.11判断链表是否有环（快指针和慢指针）

```c++
bool hasCycle(struct LinstNode *head){
    if(head==NULL || head->next==NULL){ 
        return false;
    }
    struct ListNode *slow =head,*fast=head->next;
    while(fast!=NULL &&fast-next!=NULL){  //循环条件仅判断fast，因为fast会先到达尾部
        if(slow==fast){
            return true;
		}
		slow=slow->next;
		fast=fast->next->next;
    }
    return false  //遍历完还没有相遇，说明无环
}
```

> [!important] 重点
> 链表中的环是一个循环
>
> **算法思想**
>
> 1. 使用快慢指针来遍历链表（环的长度要和快慢指针的相对速度互质）
> 2. fast指针每次移动2步，slow指针每次移动1步。
> 3. 在遍历过程中，如果fast和slow指针相遇，必定有环。因为正常情况下slow是不可能追得上fast的
> 4. 为了避免特殊情况，首先要判断为空或只有1个节点的情况

### 3.12逆序打印单链表中的节点

```c
void print_list_reverse(LinkedList L){
    if(L->next !=NULL){  //如果单链表不为空
        print_list_reverse(L->next)； //递归调用
        printf("%d",L->next->data); //输出当前节点的数据域
    }
}
```

> [!important] 重点
> 问：**“print 不都在 if 中吗，不进入 if 怎么打印？”**
>
> 答：虽然 `printf` 写在 `if` 语句块中，但它是在递归调用返回后才执行的。
> 当递归到达最深层（即最后一个节点）时，虽然不再进入 `if`，但前面压栈的那些 `printf` 会逐层返回并执行。

> [!important] 重点
> 该函数利用了递归的方法，先判断单链表是否为空，如果不为空，则递归输出单链表中除头结点之外的节点，然后输出当前节点的数据域

### 3.13查找链表中倒数第k个节点

```c
Node* find_kth_to_tail(LinkedList L, int k) {
    if (L->next == NULL || k <= 0) { // 如果单链表为空或 k 小于等于 0，则返回 NULL
        return NULL;
    }
    Node *p = L->next, *q = L->next; // p 和 q 都指向第一个结点
    while (k > 1 && q != NULL) { // q 先走 k - 1 步
        q = q->next;
        k--;
    }
    if (q == NULL) { // 如果 q 走到了 NULL，则说明 k 大于单链表的长度，返回 NULL
        return NULL;
    }
    while (q->next != NULL) { // p 和 q 同时向后走，直到 q 走到了最后一个结点
        p = p->next;
        q = q->next;
    }
    return p; // 返回倒数第 k 个结点
}
```

> [!important] 重点
> 核心思想我觉得还是用了快慢指针的相对速度这个概念。而且避免了单独求链表长，把O(n)化为了O(1).先让q走k-1步，然后p和q同时向后走，直到q走到了最后一个节点，此时p指向的就是倒数第k个节点。如果q走到了NULL，则说明K大于单链表的长度，返回NULL

## 4.双链表的基本操作

### 4.1双链表插入节点

```c
void insert_node(LinkedList L, int pos, int x) {
    Node *p = L->next; // 指向第一个结点
    int i = 1;
    while (p != NULL && i < pos) { // 遍历双链表，找到要插入的位置
        p = p->next;
        i++;
    }
    if (p == NULL) { // 如果插入位置无效，则输出错误信息并返回
        printf("插入位置无效\n");
        return;
    }
    Node *new_node = (Node*)malloc(sizeof(Node)); // 创建新结点
    new_node->data = x; // 将数据存入新结点的数据域
    new_node->prev = p; // 将新结点的前一个节点指针指向前一个节点
    new_node->next = p->next; // 将新结点的后一个节点指针指向后一个节点
    if (p->next != NULL) { // 如果后一个节点存在，则将后一个节点的前一个节点指针指向新节点
        p->next->prev = new_node;
    }
    p->next = new_node; // 将前一个节点的后一个节点指针指向新节点
}
```

> [!important] 重点
> 这个算法首先指向双链表的第一个节点，然后遍历双链表，找到要插入的位置。如果插入位置无效，则输出错误信息并返回。否则，创建一个新节点，将数据存入新节点的数据域，将新节点的前一个结点指针指向前一个结点，将新节点的后一个结点指针指向后一个结点，如果后一个结点存在，则将后一个结点的前一个结点指针指向新节点，最后将前一个结点的后一个结点指针指向新节点。这样就完成了向双链表中插入一个新节点的操作。

### 4.2双链表删除节点

```c
void delete_node(LinkedList L,int x){
	Node *p=L->next; //指向第一个节点
	while(p!=NULL && p->data!=x){ //遍历双链表，查找值为x的节点
		p=p->nextl
	}
	if(p==NULL){  //如果没找到该节点，则输出错误信息并返回
		printf("未找到该节点\n");
		return;
	}
	p->prev->next=p->next;//将前一个结点的后一个结点指针指向后一个结点
	if(p->next!=NULL){  //如果后一个节点存在，则将后一个节点的前一个结点指针指向前一个结点
		p->next->prev=p->prev;
	}
	free(p); //释放被删除的节点
}
```

## 5.栈和队列的结构体定义

### 5.1顺序栈的定义

```c
#define MAXSIZE 100 //定义栈的最大容量
typedef struct Stack{
	int data[MAXSIZE]; //栈的数据域，使用静态数组实现
	int top;
}Stack;
```

> [!important] 重点
> 顺序栈使用静态数组实现，因此结构体包括一个静态数组和一个栈顶指针。其中，静态数组的大小可以根据实际情况进行调整，这里定义为MAXSIZE

### 5.2链栈节点的定义

```c
typedef struct StackNode{
 	int data;
 	struct StackNode *next; //指向下一个节点的指针
}StackNode；
```

> [!important] 重点
> 链栈节点包括一个数据域和一个指向下一个节点的指针

### 5.3顺序队列的定义

```c
#define MAXSIZE 100 //定义队列的最大容量
typedef struct Queue{
	int data[MAXSIZE];//队列的数据域，使用静态数组实现
	int front; //队头指针
	int rear;  //队尾指针
}Queue；
```

> [!important] 重点
> 顺序队列使用静态数组实现，因此结构体中包含一个静态数组、一个队头指针和一个队尾指针。其中，静态数组的大小可以根据实际情况进行调整，这里定义为MAXSIZE。

### 5.4链队定义

```c
typedef struct QueueNode {
    int data; // 数据域
    struct QueueNode *next; // 指向下一个节点的指针
} QueueNode;

typedef struct Queue {
    QueueNode *front; // 队头指针
    QueueNode *rear; // 队尾指针
} Queue;
```

> [!important] 重点
> 链队使用链表实现，因此结构体中包含一个队头指针和一个队尾指针。队列中的每个元素都是一个节点，包含一个数据域和一个指向下一个节点的指针。

## 6.顺序栈的基本操作

### 6.1顺序栈的初始化

```c
#define MAXSIZE 100

typedef struct Stack{
	int data[MAXSIZE]； //栈的数据域，使用静态数组实现
	int top；  //栈顶指针
}Stack；

void init_stack(Stack *S){
	S->top=-1; //初始化栈顶指针为-1，表示栈为空
}
```

> [!important] 重点
> 这个函数接受一个指向栈的指针作为参数，将栈顶指针初始化为-1，表示栈为空

### 6.2判断栈空

```c
int is_empty(Stack *S) {
    return S->top == -1; // 如果栈顶指针为 -1，表示栈为空，返回 1，否则返回 0
}
```

### 6.3进栈出栈

```c
void push(Stack *S, int x) {
    if (S->top == MAXSIZE - 1) { // 如果栈已满，则输出错误信息并返回
        printf("栈已满\n");
        return;
    }
    S->top++; // 栈顶指针加 1
    S->data[S->top] = x; // 将数据存入栈顶指针所指向的位置
}

int pop(Stack *S) {
    if (S->top == -1) { // 如果栈为空，则输出错误信息并返回
        printf("栈为空\n");
        return -1;
    }
    int x = S->data[S->top]; // 取出栈顶元素
    S->top--; // 栈顶指针减 1
    return x; // 返回栈顶元素
}
```

> [!important] 重点
> 这个代码中，push（）函数接收一个指向栈的指针和一个整数作为参数，如果栈已满，则输出错误信息并返回，否则栈顶指针+1，将数据存入栈顶指针所指向的位置。pop（）函数接收一个指向栈的指针作为参数，如果栈为空，则输出错误信息并返回，否则取出栈顶元素，栈顶指针减1，最后返回栈顶元素。

## 7.链栈的基本操作

### 7.1链栈的初始化

```c
typedef struct StackNode {
    int data; // 数据域
    struct StackNode *next; // 指向下一个节点的指针
} StackNode;

typedef struct Stack {
    StackNode *top; // 栈顶指针
} Stack;

void init_stack(Stack *S) {
    S->top = NULL; // 初始化栈顶指针为空，表示栈为空
}
```

> [!important] 重点
> 这个函数接受一个指向栈的指针作为参数，将栈顶指针初始化为空，表示栈为空

### 7.2判断栈空

```c
typedef struct StackNode{
    int data;//数据域
    struct StackNode *next;//指向下一个节点的指针
}StackNode；
    
typedef struct Stack{
    StackNode *top； //栈顶指针
}Stack；
    
int is_empty(Stack *s){
    return S->top==NULL; //如果栈顶指针为空，表示栈为空，返回1，否则返回0
}
```

> [!important] 重点
> 这个函数接收一个指向栈的指针作为参数，如果栈顶指针为空，表示栈为空，返回1，否则返回0.

### 7.3进栈代码

```c
typedef struct StackNode {
    int data; // 数据域
    struct StackNode *next; // 指向下一个节点的指针
} StackNode;

typedef struct Stack {
    StackNode *top; // 栈顶指针
} Stack;

void push(Stack *S, int x) {
    StackNode *new_node = (StackNode*)malloc(sizeof(StackNode)); // 创建新结点
    new_node->data = x; // 将数据存入新结点的数据域
    new_node->next = S->top; // 将新结点插入到栈顶之后
    S->top = new_node; // 更新栈顶指针
}
```

### 7.4出栈代码

```c
typedef struct StackNode {
    int data; // 数据域
    struct StackNode *next; // 指向下一个节点的指针
} StackNode;

typedef struct Stack {
    StackNode *top; // 栈顶指针
} Stack;

int pop(Stack *S) {
    if (S->top == NULL) { // 如果栈为空，则输出错误信息并返回
        printf("栈为空\n");
        return -1;
    }
    int x = S->top->data; // 取出栈顶元素
    StackNode *temp = S->top; // 保存栈顶指针
    S->top = S->top->next; // 栈顶指针指向下一个节点
    free(temp); // 释放原栈顶节点的内存
    return x; // 返回栈顶元素
}
```

> [!important] 重点
> 为什么顺序栈和链栈的入栈出栈的指针移动顺序是不一样的？
>
> 因为顺序栈中，数组空间是预先分配好的，只需要移动索引top就行
>
> 在链栈中，链栈**是先创建新节点，并把它连到当前栈顶，然后再更新栈顶指针。**

## 8.顺序队列的基本操作

### 8.1顺序队列的初始化

```c
#define MAXSIZE 100 // 定义队列的最大容量

typedef struct Queue {
    int data[MAXSIZE]; // 队列的数据域，使用静态数组实现
    int front; // 队头指针
    int rear; // 队尾指针
} Queue;

void init_queue(Queue *Q) {
    Q->front = Q->rear = 0; // 初始化队头指针和队尾指针为 0，表示队列为空
}
```

> [!important] 重点
> 这个函数接收一个指向队列的指针作为参数，将队头指针和队尾指针初始化为0，表示队列为空

### 8.2判断队空

```c
#define MAXSIZE 100 //定义队列的最大容量

typedef struct Queue{
	int data[MAXSIZE]; //队列的数据域，使用静态数组实现
	int front; //队头指针
	int rear; //队尾指针
}Queue;

int is_empty(Queue *Q){
	return Q->front==Q->rear; //如果队头指针等于队尾指针，表示队列为空，返回1，否则返回0
}
```

### 8.3进队算法

```c
#define MAXSIZE 100 //定义队列的最大容量
typedef struct Queue{
	int data[MAXSIZE];  //队列的数据域，使用静态数组实现
	int	front; //队头指针
	int rear; //队尾指针
}Queue;

void enqueue(Queue *Q,int x){
	 if(Q->rear==MAXSIZE){  //如果队列已满，则输出错误信息并返回
	 	printf("队列已满\n");
	 	return;
	 }
	 Q->data[Q->rear] =x;   //将数据存入队尾指针所指向的位置
	 Q->rear++;  //队尾指针+1
}
```

> [!important] 重点
> 这个函数接收一个指向队列的指针和一个整数作为参数，如果队列已满，则输出错误信息并返回，否则将数据存入队尾指针所指向的位置，队尾指针+1.

### 8.4出队算法

```c
#define MAXSIZE 100 //	定义队列的最大容量
typedef struct Queue{  
	int data[MAXSIZE];
	int front;
	int rear;
}Queue;

void dequeue(Queue *Q,int x){
		if(Q->rear==Q->front){  //此时队空，输出错误信息并返回
			printf（"队列为空\n");
			return -1;
		}
		int x=Q->data[Q->front]; //取出队头元素
		Q->front++; //队头指针加1
		return x; //返回队头元素
}
```

> [!important] 重点
> 这个函数接收一个指向队列的指针作为参数，如果队列为空，则输出错误信息并返回，否则取出队头元素，队头指针加1，最后返回队头元素。

## 9.链队的基本操作

### 9.1链队的初始化

```c
typedef struct QueueNode{
    	int data; //数据域
    	struct QueueNode *next；//指向下一个节点的指针
}QueueNode；
typedef struct Queue{
    	QueueNode *front;
    	QueueNode *rear;
}Queue;
void init_queue(Queue *Q){
    Q->front =Q->rear =NULL; //初始化队首队尾指针为空
}
```

> [!important] 重点
> 这个函数接收一个指向队列的指针作为参数，将队头指针和队尾指针初始化为空，表示队列为空

### 9.2判断队空算法

```c
typedef struct QueueNode{
	int data;
	struct QueueNode *next; //指向下一节点的指针
}QueueNode;
typedef struct Queue{
	QueueNode *front; //队头指针
	QueueNode *rear; //队尾指针
}Queue;

int is_empty（Queue *Q）{
	return Q->front ==NULL; //如果队头指针为空，表示队列为空，返回1，否则返回0
}
```

> [!important] 重点
> 这个函数接收指向队列的指针Q，如果队头指针为空，表示队列为空返回1.否则返回0

### 9.3入队算法

```c
typedef struct QueueNode{
    int data;  //数据域
    struct QueueNode *next; //指向下一个节点的指针
}QueueNode;

typedef struct Queue{
    QueueNode *front； //队头指针
    QueueNode *rear;   //队尾指针
}Queue;

void  intqueue(Queue *Q,int x){
    QueueNode *new_node=(QueueNode*)malloc(sizeof(QueueNode));  //创建新节点
    new_node->data=x;    //将数据存入新节点的数据域
    new_node->next=NULL; //新节点的指针域置为空
    if(Q->front == NULL){ //如果队列为空，则将新节点作为队头节点
       Q->front=new_node;
    }
    else{ //否则将新节点插入到队尾节点之后
        Q->rear->next=new_node;
    }
    Q->rear=new_node; //更新队尾指针
}
```

> [!important] 重点
> 这个函数接受一个指向队列的指针和一个整数作为参数，首先创建一个新的节点，将数据存入新节点的数据域，然后判断队列是否为空，如果为空，则将新节点作为队头节点，否则将新节点插入到队尾节点之后，最后更新队尾指针

### 9.4出队算法

```c
typedef struct QueueNode {
    int data; // 数据域
    struct QueueNode *next; // 指向下一个节点的指针
} QueueNode;

typedef struct Queue {
    QueueNode *front; // 队头指针
    QueueNode *rear; // 队尾指针
} Queue;

int dequeue(Queue *Q) {
    if (Q->front == NULL) { // 如果队列为空，则输出错误信息并返回
        printf("队列为空\n");
        return -1;
    }
    QueueNode *temp = Q->front; // 保存队头结点
    int x = temp->data; // 取出队头元素
    Q->front = temp->next; // 将头结点指向下一个结点
    if (Q->front == NULL) { // 如果队列中只有一个元素，则更新队尾指针
        Q->rear = NULL;
    }
    free(temp); // 释放原队头结点的内存
    return x; // 返回队头元素
}
```

> [!important] 重点
> 这个函数接收一个指向队列的指针作为参数，如果队列为空，则输出错误信息并返回，否则保存队头节点，取出队头元素，将头结点指向下一个节点，如果队列中只有一个元素，则更新队尾指针，释放原队头节点的内存，最后返回队头元素。

## 10.串的结构体定义

### 10.1定长顺序存储表示

```c
#define MAXSIZE 100 //定义串的最大长度
typedef struct String{
	char str[MAXSIZE]; //存储串的字符数组
	int length; //串的长度
}String；
```

> [!important] 重点
> 这个结构体包括两个成员，一个是存储串的字符数组，另一个是串的长度。由于是定长顺序存储，所以需要预先定义一个最大长度。

### 10.2变长分配存储表示

```c
typedef struct String {
    char *str; // 指向存储串的字符数组的指针
    int length; // 串的长度
    int capacity; // 串的容量
} String;
```

> [!important] 重点
> 这个结构体包含三个成员，一个是指向存储串的字符数组的指针，另一个是串的长度，还有一个是串的容量。由于是变长分配存储，所以需要记录串的容量

## 11.串的模式匹配算法（没考过）

> [!important] 重点
> 简单串的简单模式匹配算法是一种基础的字符串匹配算法,它的思想是从文本串的第一个字符开始匹配，如果当前字符匹配成功，则继续匹配下一个字符，否则回溯到上一个匹配的位置，重新从下一个位置开始匹配。如果模式串已经遍历完，说明匹配成功，返回匹配的起始位置，否则匹配失败，返回-1.这个算法的时间复杂度为O（m*n），m为主串长度，n为模式串长度

```c
int simple_match(char *s, char *p) {
    int i = 0, j = 0;
    while (s[i] != '\0' && p[j] != '\0') { // 两个字符串都没有遍历完
        if (s[i] == p[j]) { // 当前字符匹配成功
            i++;
            j++;
        } else { // 当前字符匹配失败，回溯
            i = i - j + 1;
            j = 0;
        }
    }
    if (p[j] == '\0') { // 如果模式串已经遍历完，说明匹配成功
        return i - j;
    } else { // 否则匹配失败
        return -1;
    }
}
```

> [!important] 重点
> 这个函数接收两个指向字符数组的指针作为参数，分别表示文本串和模式串。算法从文本串的第一个字符开始匹配，如果当前字符匹配成功，则继续匹配下一个字符，否则回溯到上一个匹配的位置，重新从下一个位置开始匹配。如果模式串已经遍历完，说明匹配成功，返回匹配的起始位置，否则匹配失败，返回-1.

### 11.2KMP算法

**求next数组**

```c
void get_next(char *p, int *next) {
    int i = 0, j = -1;
    next[0] = -1;
    while (p[i] != '\0') {
        if (j == -1 || p[i] == p[j]) {
            i++;
            j++;
            next[i] = j;
        } else {
            j = next[j];
        }
    }
}
```

> [!important] 重点
> 该函数接收两个参数，一个是指向模式串的指针，另一个是指向next数组的指针。算法从模式串的第一个字符开始遍历，如果当前字符匹配成功，则将next数组的下一个元素的值设为j+1，否则回溯到上一个匹配的位置继续匹配。如果j=-1，或者当前字符匹配成功，则将i和j都加1，否则将j更新为next[j]。最后，函数返回next数组。

**求nextval数组**

```c
void get_nextval(char *p, int *nextval) {
    int i = 0, j = -1;
    nextval[0] = -1;
    while (p[i] != '\0') {
        if (j == -1 || p[i] == p[j]) {
            i++;
            j++;
            if (p[i] != p[j]) {
                nextval[i] = j;
            } else {
                nextval[i] = nextval[j];
            }
        } else {
            j = nextval[j];
        }
    }
}
```

> [!important] 重点
> 这个函数接受两个参数，一个是指向模式串的指针，另一个是指向nextval数组的指针。算法从模式串的第一个字符开始遍历，如果当前字符匹配成功，则将nextval数组的下一个元素的值设为j+1，否则回溯到上一个匹配的位置，继续匹配。如果j=-1，或者当前字符匹配成功，则将i和j都加1，否则将j更新为nextval[j].如果p[i]=p[j],则说明在j位置匹配失败，需要继续回溯，因此将nextval[i]的值设为nextval[j].最后，函数返回nextval数组。

**KMP算法**

```c
void get_next(char* p, int* next) {
    int len = strlen(p);
    next[0] = -1;
    int i = 0, j = -1;
    while (i < len - 1) {
        if (j == -1 || p[i] == p[j]) {
            i++;
            j++;
            next[i] = j;
        } else {
            j = next[j];
        }
    }
}

int kmp_match(char* s, char* p, int* next) {
    int i = 0, j = 0;
    int s_len = strlen(s), p_len = strlen(p);
    while (i < s_len && j < p_len) {
        if (j == -1 || s[i] == p[j]) {
            i++;
            j++;
        } else {
            j = next[j];
        }
    }
    if (p[j] == '\0') {
        return i - j;
    } else {
        return -1;
    }
}
```

> [!important] 重点
> KMP算法核心思想是利用已经匹配过的信息，尽可能减少模式串与文本串的匹配次数。具体来说，kmp算法通过预处理模式串，得到一个next数组，用于指导匹配过程中的回溯操作。next数组的含义是：当模式串中的某个字符与文本串中的某个字符匹配失败时，模式串应该回溯到哪个位置重新开始匹配，kmp算法的时间复杂度为O（m+n），其中m和n分别是模式串和文本串的长度

## 12.二叉树

### 12.1二叉树的节点定义

```c
struct TreeNode{
	int val;
	struct TreeNode *left;
	struct TreeNode *right;
};
```

> [!important] 重点
> 链式存储结构，每个节点包含：
>
> val，left，right
>
> 链式存储灵活高效，非常适合构建二叉树这种非顺序数据结构，是二叉树最常用的存储方式

### 12.2线索二叉树的节点定义

```c
typedef struct TBTNode{
	char data；
	int ltag,rtag;
	sturct TBTNode *lchild;
	struct TBTNode *rchild;
}TBTNode;
```

> [!important] 重点
> 线索二叉树是一种利用二叉树的空指针来存放指向该节点在某种遍历方式下的前驱和后继节点的指针的二叉树。ltag和rtag用于标记节点的左右子节点是否为线索，如果为1则表示是线索，否则表示是子节点

## 13.二叉树的遍历算法

### 13.1先序遍历（递归）

```c
void preorderTraversal(struct TreeNode* root) {
    if (root == NULL) {
        return;
    }
    printf("%d ", root->val); // 访问根节点
    preorderTraversal(root->left); // 递归遍历左子树
    preorderTraversal(root->right); // 递归遍历右子树
}
```

> [!important] 重点
> 思想：
>
> 1. 如果二叉树为空，直接返回
> 2. 打印根节点值
> 3. 递归遍历左子树
> 4. 递归遍历右子树
>
> 递归先序遍历的关键在于对左右子树的递归处理。具体分析：
>
> - 首先检查二叉树是否为空，如果为空直接返回
> - 然后打印根节点的值（访问根节点）
> - 对根节点的左子树递归调用先序遍历，遍历左子树
> - 对根节点的右子树递归调用先序遍历，遍历右子树
> - 左子树和右子树的遍历仍然按照打印根节点、遍历左子树、遍历右子树的顺序进行
> - 这样通过递归不断重复先序遍历的过程，实现了对整个二叉树的先序遍历

### 13.2先序遍历（非递归）

```c
void preorderN(BTNode *bt) {
    BTNode *Stack[maxSize];
    BTNode *p;
    int top = -1;  // 定义人工栈
        
    if (bt != NULL) {
        Stack[++top] = bt;  // 根节点入栈
 
        while (top != -1) {  // 判断不空
            p = Stack[top--];  // 出栈 并完成一次访问
            printf("%c\n", p->data);
            if (p->rchild != NULL)   // 记住，先序遍历一定是先右孩子，再左孩子
                Stack[++top] = p->rchild;
            if (p->lchild != NULL) 
                Stack[++top] = p->lchild;
        }
    }
}
```

> [!important] 重点
> 这是一个使用非递归方式实现二叉树的先序遍历的代码实现。使用人工栈来模拟递归调用栈，从而实现非递归遍历。
>
> 1. 定义一个大小为maxSize的人工栈Stack，以及一个指针变量p和一个整型变量top，用于记录栈顶位置
> 2. 判断二叉树是否为空，如果不为空。则将根节点入栈。
> 3. 循环执行以下操作，直到栈为空：
>    1. 出栈一个节点，并完成一次访问
>    2. 如果该节点的右子树不为空，则将右子树入栈
>    3. 如果该节点的左子树不为空，则将左子树入栈

### 13.3中序遍历（递归）

```c
void inorderTraversal(struct TreeNode* root) {
    if (root == NULL) {
        return;
    }
    inorderTraversal(root->left);
    printf("%d ", root->val);
    inorderTraversal(root->right);
}
```

> [!important] 重点
> 1. 如果二叉树为空，直接返回
> 2. 递归遍历左子树
> 3. 打印根节点值
> 4. 递归遍历右子树

### 13.4后序遍历（递归）

```c
void postorderTraversal(struct TreeNode* root){
	if(root==NULL){
		return;
	}
	postorderTraversal(root->left);
	postorderTraversal(root->right);
	printf("%d",root-val);
}
```

> [!important] 重点
> 1. 如果二叉树为空，直接返回
> 2. 递归遍历左子树
> 3. 递归遍历右子树
> 4. 打印根节点值（访问根节点）

### 13.5后序遍历（非递归）

```c
void postorderN(struct TreeNode *bt){
	struct TreeNode *stack[maxSize];
	struct TreeNode *p=bt;
	strcut TreeNode *r=NULL; //记录上一个访问的节点void postorderN(struct TreeNode *bt) {
    struct TreeNode *stack[maxSize];
    struct TreeNode *p = bt;
    struct TreeNode *r = NULL;  // 记录上一个访问的节点
    int top = -1;  // 定义人工栈

    while (p != NULL || top != -1) {
        if (p != NULL) {
            stack[++top] = p;  // 当前节点入栈
            p = p->left;  // 遍历左子树
        } else {
            p = stack[top];  // 取出栈顶元素
            if (p->right == NULL || p->right == r) {  // 如果右子树为空或者已经访问过
                printf("%d ", p->val);  // 访问当前节点
                r = p;  // 记录上一个访问的节点
                top--;  // 出栈
                p = NULL;  // 避免重复访问
            } else {
                p = p->right;  // 遍历右子树
            }
        }
    }
}
	int top=-1; //定义人工栈
}
```

> [!important] 重点
> 使用人工栈来模拟递归过程。首先将根节点入栈，然后循环执行以下操作：
>
> 1. 如果当前节点不为空，则将其入栈，并遍历其左子树
> 2. 如果当前节点为空，则取其栈顶元素，并判断其右子树是否为空或者已经访问过。如果是，则访问当前节点，并将其出栈。否则，遍历其右子树。

### 13.6层序遍历

层次遍历特殊，层次遍历使用的是队列

```c
void levelOrder(struct TreeNode *root) {
    if (root == NULL) { // 如果根节点为空，直接返回
        return;
    }
    struct TreeNode *queue[maxSize];
    int front = 0, rear = 0;
    queue[rear++] = root; // 将根节点入队
    while (front < rear) { // 当队列不为空时，执行以下操作
        struct TreeNode *node = queue[front++]; // 取出队头元素
        printf("%d ", node->val); // 访问该节点
        if (node->left != NULL) { // 如果该节点有左子节点，将其左子节点入队
            queue[rear++] = node->left;
        }
        if (node->right != NULL) { // 如果该节点有右子节点，将其右子节点入队
            queue[rear++] = node->right;
        }
    }
}
```

> [!important] 重点
> 1. 将根节点入队
> 2. 当队列不为空时，执行以下操作：
>    - 取出队头元素，访问该节点
>    - 如果该节点有左子节点，将其左子节点入队
>    - 如果该节点有右子节点，将其右子节点入队
> 3. 遍历结束

## 14.二叉树的其他算法

### 14.1递归求WPL

```c
int getWPL(struct TreeNode *root, int depth) {
    if (root == NULL) { // 如果节点为空，返回0
        return 0;
    }
    if (root->left == NULL && root->right == NULL) { // 如果节点是叶子节点，返回带权路径长度
        return depth * root->val;   //这里的*是×，别和指针搞混了
    }
    // 如果节点不是叶子节点，递归计算左子树和右子树的WPL，并相加返回
    return getWPL(root->left, depth + 1) + getWPL(root->right, depth + 1);
}
```

> [!important] 重点
> WPL：所有叶子节点的带权路径长度之和，其中带权路径长度是指从根节点到该节点的路径长度乘以该节点的权值。递归求WPL的方法是通过遍历二叉树所有节点，计算每个叶子节点的带权路径长度，然后将它们相加得到WPL。
>
> **root**为二叉树的根节点，**depth**表示当前节点的深度。调用该函数时，**初始深度为0**。

## 15.图的存储结构

### 15.1邻接矩阵的节点定义

```c
#define MAXSIZE 100 //定义邻接矩阵的最大大小

typedef struct VertexType{
    int no; //顶点编号
    char info; //顶点的其他信息，这里默认为char型。
}VertexType； //顶点类型
    
typedef struct MGraph{
    int edges[MAXSIZE][MAXSIZE]; //有权图则将int改为float
    int n，e；//定义为定点数和边数
    VertexType vex[MAXSIZE]; //存放节点信息
}MGraph； //图的邻接矩阵类型
```

> [!important] 重点
> 邻接矩阵MGraph，包含了图的节点信息和边信息
>
> - edges：二维数组，表示邻接矩阵
> - n：整数，表示图的结点数
> - e：整数，表示图的边数
> - vex：一维数组，表示图的节点信息，其中vex[i]表示节点i的信息，包括节点编号和其他信息

### 15.2邻接表的节点定义

```c
#define MAXSIZE 100 //定义邻接表的最大大小

typedef struct ArcNode{  //边表 
	int adjvex; //该边所指向的节点的位置
	struct ArcNode *nextarc； //指向下一条边的指针
	int info; //该边的相关信息(如权值)
}ArcNode;

typedef struct VNode{  //顶点表
	char data; //顶点信息
	ArcNode *firstarc； //指向第一条边的指针
}VNode；

typedef struct AGraph{
	VNode adjlist[MAXSIZE]； //邻接表
	int n,e
}AGraph; //图的邻接表类型
```

> [!important] 重点
> 这段代码定义了一个邻接表类型AGraph，包含了图的节点信息和边信息。具体来说，AGraph包含以下几个成员
>
> - adjlist:一维数组，表示图的邻接表
> - n：整数，表示图的结点数
> - e：整数，表示图的边数

## 16.图的遍历方式

### 16.1深度优先DFS

```cc
// 深度优先遍历（DFS）
void DFS(ALGraph G, int v) {
    visit(G.vertices[v].data); // 访问顶点v
    visited[v] = true; // 设已访问标记

    ArcNode* w = G.vertices[v].first; // 遍历顶点v的所有邻接顶点
    while (w != nullptr) {
        if (!visited[w->adjvex]) {
            DFS(G, w->adjvex); // 递归深度优先遍历邻接顶点
        }
        w = w->next; // 移动到下一个邻接顶点
    }
}

// 对图G进行深度优先遍历
void DFSTraverse(ALGraph G) {
    for (int i = 0; i < G.vexnum; ++i) {
        visited[i] = false; // 清除访问标记
    }
    for (int i = 0; i < G.vexnum; ++i) { // 从0号顶点开始遍历
        if (!visited[i]) {
            DFS(G, i);
        }
    }
}
```

> [!important] 重点
> 算法思想：
>
> - 递归访问起始结点的所有未访问邻接点
> - 邻接点入栈 Waiting遍历
> - 当前节点访问结束后，从栈顶取出下一个节点
> - 利用栈来维护节点顺序
> - 重复上述步骤直至所有节点访问结束
>
> 通过**栈**保持节点访问顺序，实现系统有序地深度优先遍历图的所有结点

### 16.2广度优先BFS

```c
// 广度优先遍历（BFS）
void BFS(ALGraph G, int v) {
    queue<int> Q; // 使用标准库中的队列
    visit(G.vertices[v].data); // 访问初始顶点v
    visited[v] = true; // 对v做已访问标记
    Q.push(v); // 顶点v入队

    while (!Q.empty()) {
        int u = Q.front(); // 顶点u出队
        Q.pop();

        ArcNode* w = G.vertices[u].first; // 检测u所有邻接点
        while (w != nullptr) {
            if (!visited[w->adjvex]) { // w为u的尚未访问的邻接顶点
                visit(G.vertices[w->adjvex].data); // 访问顶点w
                visited[w->adjvex] = true; // 对w做已访问标记
                Q.push(w->adjvex); // 顶点w入队
            }
            w = w->next;
        }
    }
}

// 对图G进行广度优先遍历
void BFSTraverse(ALGraph G) {
    for (int i = 0; i < G.vexnum; ++i) {
        visited[i] = false; // 清除访问标记
    }
    for (int i = 0; i < G.vexnum; ++i) { // 从0号顶点开始遍历
        if (!visited[i]) { // vi未访问过，从vi开始BFS
            BFS(G, i);
        }
    }
}

```

> [!important] 重点
> 算法思想：
>
> - 选择一个起点，访问该节点，并标记已访问
> - 遍历该节点的所有邻接点，将未访问的邻接点入队
> - 从队头取出一个节点，作为当前访问节点
> - 对当前访问节点进行步骤1和2的处理
> - 重复步骤3和4，直到队列为空
> - 如果还有未访问节点，回到步骤1，直至图中所有节点都被访问
> - 通过队列来维护访问顺序

## 17.最小生成树

### 17.1Prim算法

```c
int findMinKey(int key[], bool mstSet[], int V) {
    int min = INT_MAX, min_index;

    for (int v = 0; v < V; v++) {
        if (mstSet[v] == false && key[v] < min) {
            min = key[v];
            min_index = v;
        }
    }

    return min_index;
}

void primMST(MGraph graph) {
    int parent[graph.numVertices]; // 存储构造的MST
    int key[graph.numVertices];     // 键值用于选取最小权值边
    bool mstSet[graph.numVertices];// 表示顶点是否包含在MST中

    // 初始化所有键值为无穷大，mstSet[]为false
    for (int i = 0; i < graph.numVertices; i++) {
        key[i] = INT_MAX;
        mstSet[i] = false;
    }

    // 总是包含第一个顶点在MST中
    key[0] = 0;     
    parent[0] = -1; 

    // MST将有numVertices个顶点
    for (int count = 0; count < graph.numVertices - 1; count++) {
        // 从剩余的顶点集中选出具有最小key值的顶点u
        int u = findMinKey(key, mstSet, graph.numVertices);

        // 将选中的顶点标记为包含在MST中
        mstSet[u] = true;

        // 更新v的key值，如果v不是已经包含在MST中并且(u,v)的权重小于v的当前key值
        for (int v = 0; v < graph.numVertices; v++) {
            if (graph.Edge[u][v] && mstSet[v] == false && graph.Edge[u][v] < key[v]) {
                parent[v] = u;
                key[v] = graph.Edge[u][v];
            }
        }
    }

    // 打印构造的MST
    printMST(parent, graph);
}
```

> [!important] 重点
> 算法思想：从一个节点开始，不断添加与已访问节点集相连的最小权值的边，直到遍历完所有的节点，即可得到最小生成树

### 17.2Kruskal算法

```c
int kruskal(){
    int i,j,res= 0,count=0;
    for(i=1;i<= n;i++){
        parent[i]=i;
    }
    for(i=0;i<m;i++){
        for(j=i+1;j<m;j++){
            if(edges[i].w>edges[j].w){
                struct Edge tmp=edges[i];
                edges[i]=edges[j];
                edges[j]=tmp;
            }
        }
    }
    for(i=0;i<m;i++){
        if(find(edges[i].u)!=find(edges[i].v)){
            merge(edges[i].u,edges[i].v);
            res+=edges[i].w;
            count++;
            if(count == n-1)break;
        }
    }
    return res;
}
```

> [!important] 重点
> 算法思想：将所有边都按照权值从小到大排序，然后依次加入到生产树中，如果加入一条边会形成环，则不加入这条边。直到生成树中有n–1条边为止

## 18.最短路径算法

### 18.1Dijkstra算法

```c
const int NUMBER = 5; // 定义顶点的数量

// 邻接矩阵表示的图
int edges[NUMBER][NUMBER] = {
    {0, 10, 3, 20, 0},
    {10, 0, 5, 0, 15},
    {3, 5, 0, 2, 8},
    {20, 0, 2, 0, 6},
    {0, 15, 8, 6, 0}
};

// 数组用于存储从源顶点到每个顶点的最短距离
int time[NUMBER];

void dijkstra() {
    // 布尔数组用于记录访问过的顶点
    bool visited[NUMBER] = {false};
    
    // 初始化距离和访问状态
    for (int i = 0; i < NUMBER; i++) {
        visited[i] = false;
        // 如果存在从源顶点到顶点i的直接边，设置距离为该权重
        if (edges[0][i] >= 0)
            time[i] = edges[0][i];
        else 
            // 否则，初始化距离为无穷大（INT_MAX）
            time[i] = INT_MAX;
    }
    // 源顶点到自身的距离总是0
    time[0] = 0;
    // 标记源顶点为已访问
    visited[0] = true;
    
    // 寻找所有顶点的最短路径
    for (int i = 1; i < NUMBER; ++i) {
        int k = -1, minTime = INT_MAX;
        // 找到未访问的且距离最小的顶点
        for (int j = 0; j < NUMBER; j++) {
            if (!visited[j] && time[j] < minTime) {
                k = j;
                minTime = time[j];
            }
        }
        // 如果没有这样的顶点，退出循环
        if (k == -1) break;
        // 标记选定的顶点为已访问
        visited[k] = true;
        
        // 更新相邻顶点的距离
        for (int j = 0; j < NUMBER; j++) {
            if (!visited[j] && edges[k][j] >= 0) {
                // 如果通过顶点k找到了到达顶点j的更短路径，更新距离
                if (time[k] + edges[k][j] < time[j]) {
                    time[j] = time[k] + edges[k][j];
                }
            }
        }
    }
}
```



> [!important] 重点
> 算法思想：从源节点开始，依次找到距离源节点最近的节点，并以此更新其他节点的距离
>
> 1. 初始化距离数组dist和标记数组visited，将源节点的距离设为0，其他节点的距离设为无穷大，标记数组visited中所有元素都为0
> 2. 从未标记的节点中找到距离源节点最近的节点u，将节点u标记为已访问
> 3. 遍历节点u的所有邻居节点v，如果节点v未被访问过且从源节点到节点v的距离比当前记录的距离更短，则更新节点v的距离。
> 4. 重复步骤2和步骤3，直到所有节点都被访问过或者没有未标记的节点
>
> 在实现过程中，可以使用一个优先队列来存储未标记的节点，每次从队列中取出距离源节点最近的节点进行更新。这样可以减少遍历的节点数，提高算法效率

## 19.排序算法

### 19.1快速排序

```c
void quicksort(int arr[],int left,int right){ 
    if(left >= right){  //如果左指针大于等于右指针，返回
        return;
    }
    int i=left,j=right,pivot=arr[left]; //定义左指针、右指针和基准值
    while(i<j){
        while(i<j&&arr[j]>=pivot){ //从右往左找第一个小于基准值的元素
            j--;
        }
    	if(i<j){ //如果找到了，将元素赋值给左指针所指的元素
        	arr[i]=arr[j];
    	}
   		while(i<j&&arr[i]<=pivot){ //从左往右找第一个大于基准值的元素
        	i++;
   	 	}
    	if(i<j){ //如果找到了，将该元素赋值给右指针所指的位置
        	arr[j]=arr[i];
    	} 
	}
    arr[i]=pivot;//将基准值放在正确的位置上
    
    quicksort(arr,left,i-1);//递归排序左半部分
    quicksort(arr,i+1,right);//递归排序右半部分
}
```

> [!important] 重点
> 整体思想还是比较清晰的，取整个序列中第一个或者最后一个元素作为基准值pivot
>
> 然后小于pivot的统统放左边，大于pivot的统统放右边
>
> 然后再对pivot的两边进行递归，直到两边的数组大小只剩1时才返回

#### 两路快排

> [!important] 重点
> 快速排序的一种变体，使用两个pivot元素来将数组分成三部分，分别是小于第一个pivot元素、大于第一个pivot元素且小于第二个pivot元素、大于第二个pivot元素的部分。这个算法的时间复杂度为O(nlog~n~)，空间复杂度为O(log~n~)

#### 三路快排

> [!important] 重点
> 快速排序的一种变体，使用三个pivot元素来将数组分成四部分，分别是小于第一个pivot元素、等于第一个pivot元素、大于第一个pivot元素且小于第二个pivot元素、大于第二个pivot元素且小于等于第三个pivot元素、大于第三个pivot元素的部分。这个算法的时间复杂度为O(nlog~n~),空间复杂度为O（log~n~）

### 19.2归并排序

```c
void merge(int arr[], int left, int mid, int right) {
    int i = left, j = mid + 1, k = 0;
    int temp[right - left + 1]; // 定义临时数组

    while (i <= mid && j <= right) { // 将左右两部分按顺序合并到临时数组中
        if (arr[i] <= arr[j]) {
            temp[k++] = arr[i++];
        } else {
            temp[k++] = arr[j++];
        }
    }

    while (i <= mid) { // 将左边剩余的部分复制到临时数组中
        temp[k++] = arr[i++];
    }

    while (j <= right) { // 将右边剩余的部分复制到临时数组中
        temp[k++] = arr[j++];
    }

    for (i = left, k = 0; i <= right; i++, k++) { // 将临时数组中的元素复制回原数组中
        arr[i] = temp[k];
    }
}

void merge_sort(int arr[], int left, int right) {
    if (left >= right) { // 如果左指针大于等于右指针，直接返回
        return;
    }

    int mid = (left + right) / 2; // 计算中间位置

    merge_sort(arr, left, mid); // 递归处理左边的子数组
    merge_sort(arr, mid + 1, right); // 递归处理右边的子数组

    merge(arr, left, mid, right); // 合并左右两部分
}
```

> [!important] 重点
> 算法思想：把长度为n的输入序列分成两个长度为n/2的子序列，对两个子序列再进行归并。
>
> 不断递归，直到子序列大小为1时，这时候就进行合并了

### 19.3冒泡排序

```c
void bubble_sort(int arr[],int n){
    int i,j,temp;
    
    for(i=0;i<n-1;i++){    //外层循环控制排序轮数
        for(j=0;j<n-i-1;j++){  //内层循环控制每轮比较次数
            if(arr[j]>arr[j+1]){  //如果前一个数大于后一个数，交换它们的位置
                temp=arr[j];
                arr[j]=arr[j+1];
                arr[j+1]=temp;
            }
        }
    }
}
```

> [!important] 重点
> O(n^2^)

### 19.4折半插入排序

```c
void binary_insertion_sort(int arr[],int n){
    int i,j,left,right,mid,temp;
    
    for(i=1;i<n;i++){ //外层循环控制插入次数
        temp=arr[i]; //取出待插入的数
        left=0; //初始化左指针
        right=i-1; //初始化右指针
        
        while(left<=right){  //折半查找插入的位置
            mid=(left+right)/2;
        	if(arr[mid]>temp){
                right=mid-1;
            }
        	else{
                left=mid+1;
            }
 	    }
    
	    for(j=i-1;j>=left;j--){
	        arr[j+1]=arr[j];
    	}
    	arr[left]=temp;
	}
}
```

> [!important] 重点
> 算法思想：
>
> 定义3个指针left、mid和right,left和right之间表示的是查找的范围。通常来说left是一定小于right的，如果right<left时，则退出查找.其中mid帮助缩小left和right的范围，如果要找的数比mid大的话，就将left移动到mid右边；如果找的数比mid小的话，就将right移动到mid左边。

### 19.5直接插入排序

```c
void insertionSort(int arr[],int n){
    for(int i=1;i<n;i++){
        int key=arr[i];
        int j=i-1;
        while(j>=0&&arr[j]>key){
            arr[j+1]=arr[j];
            j--;
        }
        arr[j+1]=key;
    }
}
```

> [!important] 重点
> 直接插入排序很简单。就是将一个记录插入到已经排好序的有序表中，从而得到一个新的、记录数增加1的有序表。具体实现，我们从第二个元素开始，将其插入到前面已经排好序的子序列中，直到最后一个元素插入完成，整个序列就排好序了。直接插入排序的时间复杂度为O(n^2^),空间复杂度为O(1)

### 19.6希尔排序

```c
void shellSort(int arr[],int n){
    for(int gap=n/2;gap>0;gap/=2){
        for(int i=gap;i<n;i++){
	        int temp=arr[i];
    	    int j
        	    for(j=i;j>=gap&&arr[j-gap]>temp;j-=gap){
            	    arr[j]=arr[j-gap];
            	}
        		arr[j]=temp;
        }      
    }
}
```



> [!important] 重点
> 将整个序列按照选定的增量分成若干个子序列，对每个子序列进行直接插入排序。排完一趟后，缩小增量再来一轮，直至最后增量小到相当于对整个序列排序一次

### 19.7简单选择排序

```c
void selectionSort(int arr[], int n) {
    for (int i = 0; i < n - 1; i++) {
        int minIndex = i;
        for (int j = i + 1; j < n; j++) {
            if (arr[j] < arr[minIndex]) {
                minIndex = j;
            }
        }
        if (minIndex != i) {
            int temp = arr[i];
            arr[i] = arr[minIndex];
            arr[minIndex] = temp;
        }
    }
}
```



> [!important] 重点
> 每次从未排序的序列中选择最小的元素，将其放到已经排序的序列的末尾，直到整个序列都排好序。
>
> 具体实现：从第一个元素开始，依次找到最小的元素，将其与第一个元素交换位置，然后从第二个开始，依次找到最小的元素，将其与第二个元素交换位置，以此类推，直到最后一个元素排好序。

### 19.8堆排序

```c
#include<stdio.h>
#include<stdlib.h>

void shiftdown(int arr[],int n,inti ){
    int largest = i;
    int l=2*i+1;
    int r=2*i+2;
    if(l<n&&arr[l]>arr[largest]){
        largest=1;
    }
    if(r<N&&arr[r]>arr[largest]){
        largest =r;
    }
    if(largest !=i){
        int temp=arr[i];
        arr[i]=arr[largest];
        arr[largest]=temp;
        shiftdown(arr,n,largest);
    }
}

void heapSort(int arr[],int n){
    for(int i=n/2-1;i>=00;i--){
        shiftdown(arr,n,i);
    }
    for(int i=n-1,i>=0;i--){
        int temp=arr[0];
        arr[0]=arr[i];
        arr[i]=temp;
        shitfdown(arr,i,0);
    }
}

int main(){
    int arr[]={5,3,8,4,2,7,1,10,6,9};
    int n=sizeof(arr)/sizeof(arr[0]);
    heapSort(arr,n);
    for(int i=0;i<n;i++){
        printf("%d",arr[i]);
    }
    printf("\n");
    system("pause");
    return 0;
}
```



> [!important] 重点
> 算法思想：将待排序的序列构建成一个大根堆或小根堆，然后依次取出堆顶元素，直到堆为空，得到的序列就是排序好的序列。
>
> 重要操作：：shiftdown操作，将一个节点下移以满足堆的性质：将当前节点与左右子节点中的最大值进行比较，如果当前节点的值小于最大值，则将当前节点与最大值所在的子节点交换位置，并递归对交换后的子节点进行shiftdown操作，直到当前节点的值大于等于其左右子节点的值，或者当前节点没有子节点为止

## 20.二分查找

```c
#include<stido.h>

int binary_search(int arr[],int n,int target){
    int left=0,right=n-1,mid;
    
    while(left<=right){
        mid=(left+right)/2
            if(arr[mid]==target){
                return mid;
            
            }
        	else if(arr[mid]<target){
                left =mid+1;
               
            }
        	else if(arr[mid]>target){
                right=mid-1;
            }
    }
    return -1;
}
int main{
    int arr[]={1,2,3,4,5,6,7,8};
    int n=sizeof(arr)/sizeof(arr[0]);
    int target =5;
    int index=binary_search(arr,n,target);
    if(index==-1){
        pritnf(not found);
    }
    else print(yes!);
    
    return 0;
}
```

> [!important] 重点
> 算法思想：将有序数列分成两部分，取中间位置的数与目标数进行比较，如果中间位置的数等于目标数，返回中间位置；如果中间位置的数小于目标数，将左指针移动到中间位置的右边；如果中间位置的数大于目标数，将右指针移动到中间位置的左边。在实现过程中，可以使用一个循环来控制左右指针的移动，直到找到目标数或者左指针大于右指针为止。

## 21.二叉排序树

### 21.1二叉排序树的结点定义

```c
struct TreeNode {
    int val;
    struct TreeNode* left;
    struct TreeNode* right;
};
```

> [!important] 重点
> 二叉排序树每个节点都包含关键字和两个指针，分别指向左子树和右子树。对于任意一个节点，他的左子树的所有结点的关键字都小于他的关键字，他的右子树的所有结点的关键字都大于他的关键字

### 21.2二叉排序树的查找算法

```c
struct TreeNode {
    int val;
    struct TreeNode* left;
    struct TreeNode* right;
};

struct TreeNode* searchBST(struct TreeNode* root, int val) {
    if (root == NULL || root->val == val) {
        return root;
    }
    if (root->val > val) {
        return searchBST(root->left, val);
    } else {
        return searchBST(root->right, val);
    }
}
```

> [!important] 重点
> 算法接受二叉排序树的根节点和一个关键字，并返回二叉排序树中关键字等于val的结点。在算法的实现中，我们首先判断当前节点是否为空或者关键字等于val，如果是，则直接返回当前节点；否则如果当前节点的关键字大于val，则递归地在左子树中查找；否则递归地在右子树中查找

### 21.3二叉排序树的插入算法

```c#
struct TreeNode {
    int val;
    struct TreeNode* left;
    struct TreeNode* right;
};

struct TreeNode* insertBST(struct TreeNode* root, int val) {
    if (root == NULL) {
        root = (struct TreeNode*)malloc(sizeof(struct TreeNode));
        root->val = val;
        root->left = NULL;
        root->right = NULL;
        return root;
    }
    if (root->val > val) {
        root->left = insertBST(root->left, val);
    } else {
        root->right = insertBST(root->right, val);
    }
    return root;
}
```

> [!important] 重点
> 算法接收根节点和关键字。并返回插入关键字后的二叉排序树的根节点，在算法的实现中，首先判断当前节点是否为空，如果是则创建一个新节点，并将关键字val赋值给它；否则，如果当前节点的关键字大于val，则递归地在左子树中插入；否则。递归地在右子树中插入。最后返回根节点

### 21.4二叉排序树的构造算法

```c
struct TreeNode {
    int val;
    struct TreeNode* left;
    struct TreeNode* right;
};

struct TreeNode* insertBST(struct TreeNode* root, int val) {
    if (root == NULL) {
        root = (struct TreeNode*)malloc(sizeof(struct TreeNode));
        root->val = val;
        root->left = NULL;
        root->right = NULL;
        return root;
    }
    if (root->val > val) {
        root->left = insertBST(root->left, val);
    } else {
        root->right = insertBST(root->right, val);
    }
    return root;
}

struct TreeNode* constructBST(int arr[], int n) {
    struct TreeNode* root = NULL;
    for (int i = 0; i < n; i++) {
        root = insertBST(root, arr[i]);
    }
    return root;
}
```

> [!important] 重点
> 算法接收一个整数数组arr和数组的长度n，并返回构造出的二叉排序树的根节点。在算法的实现中，首先定义一个insertBST函数，用于向二叉排序树中插入一个关键字。然后，我们定义了一个constructBST函数用于构造二叉排序树。在这个函数中，我们首先将根节点初始化为NULL，然后依次将数组中的元素插入到二叉排序树中。最后，返回根节点 

<img src="https://fyywww.oss-cn-hangzhou.aliyuncs.com/img_for_typora/202510022055372.webp" alt="图灵 的图像结果" style="zoom:67%;" />

神庇佑我

# 1.反转链表

给你单链表的头节点 `head` ，请你反转链表，并返回反转后的链表。

## 迭代法

> [!important] 重点
> 假设链表为 1→2→3→∅，我们想要把它改成 ∅←1←2←3。
>
> 在遍历链表时，将当前节点的 next 指针改为指向前一个节点。由于节点没有引用其前一个节点，因此必须事先存储其前一个节点。在更改引用之前，还需要存储后一个节点。最后返回新的头引用。

```c++
void reverseList(struct LinstNode **head){//**head 是一个指向头指针的指针。为了在函数内部修改头指针本身
	if(*head == NULL || (*head)->next==NULL){//如果链表为空或只有头结点，直接返回
		return;
	}
	struct ListNode *prev=NULL,*curr=*head,*next=NULL;  //定义3个节点
	while(cur!= NULL){  //遍历链表，将每个节点的后继指针指向前一个结点
		next=curr->next;  
		curr->next=prev;
		prev=curr;
		curr=next;
	}
	*head=prev； //将头指针指向逆置后的链表的头结点
}
```

## 递归法

![[4b2d75706642a813bbdaeda776ccaa77_MD5.png]]

```c
struct ListNode* reverseList(struct ListNode* curr) {
    if (curr == NULL || curr->next == NULL) {
        return curr;
    }
    struct ListNode* newHead = reverseList(curr->next);
    curr->next->next = curr;
    curr->next = NULL;
    return newHead;
}
```

# 2.反转链表Ⅱ

给你单链表的头指针 `head` 和两个整数 `left` 和 `right` ，其中 `left <= right` 。请你反转从位置 `left` 到位置 `right` 的链表节点，返回 **反转后的链表** 。

 ![[b78733fedd5d6221f9866135f0e188c4_MD5.jpg]]

```c
void reverseLinkedList(struct ListNode *head) {
    // 也可以使用递归反转一个链表
    struct ListNode *pre = NULL;
    struct ListNode *cur = head;

    while (cur != NULL) {
        struct ListNode *next = cur->next;
        cur->next = pre;
        pre = cur;
        cur = next;
    }
}

struct ListNode *reverseBetween(struct ListNode *head, int left, int right) {
    // 因为头节点有可能发生变化，使用虚拟头节点可以避免复杂的分类讨论
    struct ListNode *dummyNode = malloc(sizeof(struct ListNode));
    dummyNode->val = -1;
    dummyNode->next = head;

    struct ListNode *pre = dummyNode;
    // 第 1 步：从虚拟头节点走 left - 1 步，来到 left 节点的前一个节点
    // 建议写在 for 循环里，语义清晰
    for (int i = 0; i < left - 1; i++) {
        pre = pre->next;
    }

    // 第 2 步：从 pre 再走 right - left + 1 步，来到 right 节点
    struct ListNode *rightNode = pre;
    for (int i = 0; i < right - left + 1; i++) {
        rightNode = rightNode->next;
    }

    // 第 3 步：切断出一个子链表（截取链表）
    struct ListNode *leftNode = pre->next;
    struct ListNode *curr = rightNode->next;

    // 注意：切断链接
    pre->next = NULL;
    rightNode->next = NULL;

    // 第 4 步：同第 206 题，反转链表的子区间
    reverseLinkedList(leftNode);

    // 第 5 步：接回到原来的链表中
    pre->next = rightNode;
    leftNode->next = curr;
    return dummyNode->next;
}
```

# 3.删除排序链表中的重复元素

给定一个已排序的链表的头 `head` ， *删除所有重复的元素，使每个元素只出现一次* 。返回 *已排序的链表* 

![[38e0cb86a74da6a8d74283b4b4aaefc4_MD5.jpg]]

![[993ddf70e5535bb700c208ad5c20e055_MD5.jpg]]

```c
struct ListNode* deleteDuplicates(struct ListNode* head) {
    if (!head) return head;

    struct ListNode* cur = head;
    while (cur->next) {
        if (cur->val == cur->next->val) {
            struct ListNode* temp = cur->next;  // 保存要删除的节点
            cur->next = cur->next->next;        // 跳过该节点
            free(temp);                         // 释放内存
        } else {
            cur = cur->next;
        }
    }

    return head;
}
```

# 4.K个一组翻转链表

给链表的头结点**head**，每**k**个节点为一组进行翻转，返回修改后的链表

**k**是一个正整数，他的值小于或等于链表的长度；如果节点不是**k**的整数倍那么请将最后剩余的节点保持原有顺序。

你不能只是单纯的改变节点内部的值，而是需要实际进行节点交换。

![[d85ae62f875f9cb5743a297b35339a0e_MD5.jpg]]



```c
// 翻转以 head 为头的链表
struct ListNode* reverse(struct ListNode* head) {
    struct ListNode* pre = NULL;
    struct ListNode* curr = head;
    while (curr != NULL) {
        struct ListNode* next = curr->next;
        curr->next = pre;
        pre = curr;
        curr = next;
    }
    return pre; // 返回新的头节点
}
// K 个一组翻转链表
struct ListNode* reverseKGroup(struct ListNode* head, int k) {
    // 创建虚拟头节点，统一处理逻辑
    struct ListNode* dummy = (struct ListNode*)malloc(sizeof(struct ListNode));
    dummy->next = head;
    struct ListNode* pre = dummy;
    struct ListNode* end = dummy;
    while (end->next != NULL) {
        // end 向后移动 k 个节点
        for (int i = 0; i < k && end != NULL && end->next != NULL; i++) {
            end = end->next;
        }
        // 如果 end 为 NULL，说明不足 k 个节点，结束
        if (end == NULL) break;
        // start 是当前组的头节点
        struct ListNode* start = pre->next;
        struct ListNode* next = end->next;
        // 断开当前组的链表
        end->next = NULL;
        // 翻转当前组
        pre->next = reverse(start);
        // 拼接翻转后的链表
        start->next = next;
        // 更新 pre 到当前组的尾部，作为下一组的前驱
        pre = start;
        // 重置 end 为 pre，准备下一轮循环
        end = pre;
    }
    // 保存新头节点
    struct ListNode* newHead = dummy->next;
    // 释放 dummy 节点
    free(dummy);
    return newHead;
}
```

> [!important] 重点
> ![[8382c6b745c3caac3050ef3b5222ebea_MD5.png]]![[a2e2bd4b9359e54147d2b07b8e6c9829_MD5.png]]

# 5.相交链表

![[2df991c098891f1d9b22c1aac5d10593_MD5.png]]

给两个单链表的头结点headA和headB，请你找出并返回两个单链表相交的起始节点。如果两个链表不存在相交节点，返回null。

```c
// 定义链表节点结构
struct ListNode {
    int val;
    struct ListNode *next;
};

// 查找两个链表的交点
struct ListNode *getIntersectionNode(struct ListNode *headA, struct ListNode *headB) {
    if (headA == NULL || headB == NULL)
        return NULL;

    struct ListNode *pA = headA;
    struct ListNode *pB = headB;

    while (pA != pB) {
        pA = (pA == NULL) ? headB : pA->next;
        pB = (pB == NULL) ? headA : pB->next;
    }

    return pA; // 返回交点节点，如果没有交点则返回 NULL
}
```

![[c1a87f039ae6eee215d7cf3361a13b5f_MD5.png]]

> [!important] 重点
> 根据题目意思
> 如果两个链表相交，那么相交点之后的长度是相同的
>
> 我们需要做的事情是，让两个链表从同距离末尾同等距离的位置开始遍历。这个位置只能是较短链表的头结点位置。
> 为此，我们必须消除两个链表的长度差
>
> 指针 pA 指向 A 链表，指针 pB 指向 B 链表，依次往后遍历
> 如果 pA 到了末尾，则 pA = headB 继续遍历
> 如果 pB 到了末尾，则 pB = headA 继续遍历
> 比较长的链表指针指向较短链表head时，长度差就消除了
> 如此，只需要将最短链表遍历两次即可找到位置
>
> 

# 6.合并两个有序链表

将两个升序链表合并为一个新的 **升序** 链表并返回。新链表是通过拼接给定的两个链表的所有节点组成的。 

![[5345feafcc55ef96dc65feb2d0eed537_MD5.jpg]]

```c
 ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) {
        if (l1 == nullptr) {
            return l2;
        } else if (l2 == nullptr) {
            return l1;
        } else if (l1->val < l2->val) {
            l1->next = mergeTwoLists(l1->next, l2);
            return l1;
        } else {
            l2->next = mergeTwoLists(l1, l2->next);
            return l2;
        }
    }
};

```

> [!important] 重点
> 官方答案：
>
> ```c
> int Majority(int A[lint n){
> 	int i,c,count=l;  //c用来保存候选主元素，count用来计数
> 	C=A[0];				//设置A[0]为候选主元素	
> 	for(i=l;i<n;i++)	//查找候选主元素
> 		if(A[i]==C)
> 			count++;	//对A中的候选主元素计数
> 		else
> 			if(count>0)	//处理不是候选主元素的情况
> 				count--;
> 			else {			//更换候选主元素，重新计数
> 				c=A[i];
> 				count=1;
>       }
> 	if(count>0)
> 		for(i=count=0;i<n;i++)	//统计候选主元素的实际出现次数
> 			if(A[i]==c)
>           count++;
> 	if(count>n/2)	return c;		//确认候选主元素
> else return -1;
>       }
> ```



> [!important] 重点
> 这一题我想到了两个思路
>
> 1. 首先先进行快速排序，毕竟这种划分序列问题，快排能避免很多麻烦。我想的是先进行快排把序列整理为一个从小到大的有序序列。这样再找到中位数，那么中位数左边都是比他小的，右边都是比他大的，而且个数还都比较均匀。满足了题目要求，我把两边化为一个子集，如果是偶数个，那我把中位数加到少一个那里面。如果是奇数个，中位数我放在第一个子集里。  构造两个子集  时间复杂度为O（nlogn），空间复杂度应该是O（1），因为我只是借助了一个辅助空间来存帮助快速排序。  这种方法比较好想，写起来很快
> 2. 还有种思路是我在写第一种思路时想到的。既然快速排序具有这种自动划分左右子集的效果。那我进行快排的第一轮不就可以实现小的在左边，大的在右边吗。这样|s1-s2|就已经满足了，子集内是否有序我不用管。但是两个子集的元素个数之差我还得另想办法，如果我首先找到的枢纽就是中位数就好了。那就先进行一轮，根据第一轮i的位置，如果i偏左，说明右边不是全都大，如果i偏右反之。（这个思想和王道课后题是一样的）

> [!important] 重点
> 我的丑陋的代码
>
> ```c
> void QuickSort(int arr[], int left,int right){
> if(left >= right){  //如果左指针大于等于右指针，返回
>      return;
>  } 
> int i=left,j=right, pivot=arr[left];
> while(i<j){
> 		while(i<j&&arr[j]>=pivot){
> 			j--;
> 		}
> 		if(i<j){
> 			arr[i]=arr[j];
> 		}
> 		while(i<j&&arr[i]<=pivot){
> 			i++;
> 		}
> 		if(i<j){
> 			arr[j]=arr[i];
> 		}
> }
> arr[i]=pivot;
> QuickSort(arr,left,i-1);
> QuickSort(arr,i+1，right);
> }
> // 划分函数
> int partition(int arr[], int n) {
>  // 第一步：排序（降序）
>  quickSort(arr, 0, n-1);
> 
>  // 第二步：计算划分点
>  int n1 = n / 2;
>  int n2 = n - n1;
> 
>  // 第三步：计算两个子集的和
>  int sum1 = 0, sum2 = 0;
>  for (int i = 0; i < n1; i++) sum1 += arr[i];
>  for (int i = n1; i < n; i++) sum2 += arr[i];
> 
>  return sum2 - sum1;  // 返回S2 - S1
> }
> 
> ```
>
> 王道
>
> 1. 算法的基本设计思想
>
> 2. 由题意可知，将最小的Ln/个元素放在 A~1~,中，其余的元素放在A~2~中，分组结果即可满足题目要求。仿照快速排序的思想基于枢轴将个整数划分为两个子集根据划分后枢轴所处的位置i分别处理:
>
> 3. 若i=Ln/2」，则分组完戈，算法结束若i<Ln/2」,则枢轴及之前的所有元素均属于A，继续对i之后的元素进行划分。若i>Ln/2],，则枢轴及之后的所有元素均属于 4，继续对i之前的元素进行划分。基于该设计思想实现的算去，无须对全部元素进行全排序，其平均时间复杂度是 0(n)，空间复杂度是 O(1)
>
>    ```c
>    int setPartition(int a[l, int n){
>    	int pivotkey,low=0,low0-0,high=n-1,high0-n-1,flag-1,k=n/2,i;
>    	int s1=0，s2-0;
>    	while(flag){
>    		pivotkey=a[low];
>    		while(low<high){//选择枢轴//基于枢轴对数据进行划分
>    			while(low<high && a[high]>=pivotkey)--high;
>    			if(low!=high)a[low]=a[high];
>    			while(low<high && a[low]<=pivotkey) ++low;
>    			if(low!=high)	a[high]=a[low];
>    			}		//end of while(low<high)
>    			a[low]=pivotkey;
>    			if(low=-k-1)		//若枢轴是第 n/2小的元素，划分成功
>    				flag=0;
>    			else{				//是否继续划分
>    				if(low<k-1){
>    					low0=++low;
>    					high=high0;
>    				}
>    				else{
>    					high0=--high;
>    					low=low0;
>    				}
>    			}
>    		}	
>    			for(i=0;i<k;i++)	s1+=a[i];
>     			for(i=k;i<n;i++)   s2+=a[i];
>    			return s2-s1;
>    }
>    ```
>
> 





# 10.找三元组最小距离

![[e52c7b86210233c6f421c010df393aad_MD5.png]]

> [!important] 重点
> 依旧三层循环暴力求解，大粪啊
>
> ```c
> #define INT_MAX  0x7fffffff
> int Abs(int a){
>  if (a>0)  return a;
> 	else return -a;}
> int TripleGroup(int A[],int n,int B[],int m,int C[],int p){
> int i=0,j=0,k=0,D;
> int D_min=INT_MAX;
> for(i=0;i<n;i++){
>   for(j=0;j<m;j++){
>       for(k=0;k<p;k++){
>           D=Abs(A[i]-B[j])+Abs(B[j]-C[k])+Abs(C[k]-A[i]);
>  		  if(D<D_min) D_min=D;}}}	
> return D_min;
> }
> ```
>
> 王道
>
> <img src="https://fyywww.oss-cn-hangzhou.aliyuncs.com/img_for_typora/202509271337841.jpeg" alt="bd20505af52078b8018d7f99220a8dea_720" style="zoom:50%;" />
>
> <img src="https://fyywww.oss-cn-hangzhou.aliyuncs.com/img_for_typora/202509271337539.jpeg" alt="ebce5d913cce45c228aaca9209a67a84_720" style="zoom:50%;" />
>
> ```c
> #define INT_MAX 0x7fffffff
> int abs_(int a){ //计算绝对值
>  if(a<0)  return -a;
>  else return a;
> }
> bool xls_min(int a,int b,int c){  //a是否是三个数中的最小值
>  if(a<=b&&a<=c)
>      return true;
>  return false;
> }
> int findMinofTrip(int A[],int n,int B[],int m,int C[],int p){
>  //D_min 用于记录三元组的最小距离，初值赋为INT_MAX
>  itn i=0,j=0,k=0,D_min=INT_MAX,D;
>  while(i<n&&j<m&&k<p&&D_min>0){
>      D=abs_(A[i]-B[j])+abs_(B[j]-C[k])+abs_(C[k]-A[i]); //计算D
>      if(D<D_min) D_min=D; //更新D
>      if(xls_min(A[i],B[j],C[k]))
>          i++;  //更新a
>      else if(xls_min(B[j],C[k],A[i]))
>          j++;
>      else k++;
>  }
>  return D_min;
> }
> ```
>
> 设n=（|S1|+|S2|+|S3|），时间复杂度为O（n），空间复杂度为O（1）.

# 顺序表的基本功训练

## 1.2.1.删除最小值

从顺序表中删除具有最小值的元素(假设唯一)并由函数返回被删除元素的值。空出的位置由最后一个元素填补，若顺序表为空，则显示出错信息并退出运行

```c
#define Origin 0x7fffffff
int DeleteMin(int A[],int n){
    int i;
    int position=-1;
    int Min=Origin;
    if(n<=0){
        return -1;
    }
    for(i=0;i<n;i++){
        if (A[i]< Min){
            Min=A[i];
            position=i;
        }    
    }
    A[position]=A[n-1];
   	return Min;
}
```

> [!important] 重点
> 王道答案：
>
> ```c
> bool Del Min(SqList&L,ElemType &value){
>  //删除顺序表工中最小值元素结点，并通过引用型参数value 返回其值
>  //若删除成功，则返回true;否则返回false
>   if(L.length==0)
> 		return false;		//表空，中止操作返回
>   value=L.data[0];	//假定0号元素的值最小
> 	 int pos=0;				//循环，寻找具有最小值的元素
> 	 for(int i=l;i<.length;i++)		//让value记忆当前具有最小值的元素
> 		if(L.data[i]<value){
> 			value=L.data[i];
> 			pos=i;}	
> 	 L.data[pos]=L.data[L.length-l];//空出的位置由最后一个元素填补 
>  L.length--;
>  return true;
> }	//此时，value为最小值
> 
> ```
>
> 

> [!important] 重点
> 1. 首先判定是否为空，如果为空，直接退出
> 2. 若不为空，定义一个变量min用于记录当前查到的最小值，初始为0x7fffffff，同时还得记录最小值元素的位置
> 3. 遍历顺序表，若当前元素小于min，则更新min，若大于等于min则跳过
> 4. 遍历完之后，那就直接令最后一个元素复制到最小值元素位置。

## 按值删除

对长度为n的顺序表L，编写一个时间复杂度为O（N），空间复杂度为O（1）的算法。使得算法能够将顺序表中所有值为x的元素删除

> [!important] 重点
> ```c
> int Delete_value(SqList &L ,int elem){   
>  int i=0;
>  int temp=L.length;
>  while(i<L.length){     //大前提是要删除的元素他确实在顺序表中
>      if(L.data[i]==elem){   //当找到了要删除的元素的时候
>          for(int j=i;j<L.length-1;j++){  //对后面元素进行移动，以移动代替删除
>              L.data[j]=L.data[j+1];
>          }
>          L.length--;  //在移动完一遍后，L的长度-1
>      }
>      else{   //当没找到要删除元素时
>              i++;  //向后查找
>          }
>  }    
>  if(temp ==L.length){  //如果L的长度没变，说明根本没有该元素
>      return -1;}
>  return 1;
> 
> }
> 
> ```
>
> 

## 删除区间内所有元素

从顺序表中删除其值在给定值s和t之间（包含s和t，要求s＜t）的所有元素，若s或t不合理或顺序表为空，则显示出错信息并退出运行

> [!important] 重点
> ```c
> int DeleteRealm(Sqlist &L,int s,int t){
>  if(s>=t ||L.length==0){
>      return -1;
>  }
>  int i=0,j;
>  int temp=L.length;
>  while(i<L.length){
>      if(L.data[i]>=s&&L.data[i]<=t){
>          for(j=i;j<L.length-1;j++){
>              L.data[j]=L.data[j+1];
>          }
>      L.length--;}
>      else {i++;  //如果i不符合在s和t之间，那就向后移动
>      }   
>  }，
>   if (temp==L.length)
>   {
>      return -1;}  //区间内没有s和t之间的元素
>  return 1;
> 
> }
> ```
>
> 

## 删除重复元素

从有序顺序表中删除所有重复的元素，使得表中的所有元素的值均不同

> [!important] 重点
> 我的思路是既然给定的顺序表是有序的，那么重复的元素肯定是相邻的，那也没必要搞什么计数器了，直接让当前指向元素和相邻元素比较就行了，如果相同的话，就把后面元素向前移动一位；如果不同，就去比较后面的；
>
> 1. 首先我要先判断表是否为空，如果为空，则退出
> 2. 其次用for循环对表中元素进行遍历，相同就移动，不同就掠过
>
> ```c
> int DeleteDup(SqList *L){
>     int i=0,j=0;
>     if(L->length==0){  //如果顺序表为空，直接返回
>         return -1;}
>     while(i<L->length-1){   //对每一个元素进行比较
>         if(L->data[i]==L->data[i+1]){
>             for(j=i;j<L->length-1;j++){
>                 L->data[j]=L->data[j+1];
>             }
>             L->length --;}
>         else {i++;}   //如果当前元素正常，那就直接跳过
>     }
>     return 1;
> }
> ```
>
> 

# 顺序表的逆置问题

## 顺序表元素逆置

设计一个高效算法，将顺序表L的所有元素逆置，要求算法的空间复杂度为O(1).

```c
#define ERROR -1
int Reverse(Sqlist *L){
	if(L->length==0){
        return ERROR;
    }
    int temp=0;
    for(int i=0;i<L->length/2;i++){
        temp=L->data[i];
        L->data[i]=L->data[L->length-i-1];
        L->data[L-length-i-1]=temp;
        
    }
return 1;
}
```



## 调换半拉位置

已知在一维数组A[m+n]中一次存放两个线性表A(a1,a2,..,am)和B(b1,b2,b3,…,bn).编写一个函数，将数组中两个顺序表的位置互换，即把B放在A的前面

> [!important] 重点
> 先对整个数组进行一次翻转，再对前半段和后半段分别翻转

```c
typedef int DataType;
void Reverse(DataType A[],int left,int right,int arraySize){
    //逆转
    if(left>=right||right>=arraySize){
        return；}
    int i,j;
    int temp;
    for(i=0;i<m+n;i++){ //对整个数组进行一次翻转
        temp=A[i];
        A[i]=A[m+n-i];
        A[m+n-i]=temp;
    for(left=A[0])
    
    }
}
void Quick_sort(int A[],int left,int right){
    while(left>right){
        return;
    }
    int i=left;j=right;pivot=A[left];
    while(i<j){
        if(i<j&&A[j]>=pivot){
            j++;
            
        }
        if(i<j){
            A[i]=A[j];
        }
        if(i<j&&A[i]<=pivot){
            i++;}
        if(i<j){
            A[j]=A[i];
        }
        }
    A[i]=A[left];
    Quick_sort(A,left,i);
    QUick_sort(A,i,right)
    }
}
```



## 真题调换半拉

设将n(n>1)个整数存放在一维数组R中。设计一个在时间和空间两方面都尽可能高效的算法。将R中保存的序列循环左移P(0<p<n)个位置，即将R中的数据有0到n-1变换为x~p~到x~n-1~，x~0~到x~p-1~

> [!important] 重点
> 思路：首先对整个数组进行一次翻转，得到n-1，n-2….，p…，..1，0的顺序，接着以p为截断位置，对两边进行一次独立的翻转，即可得到目标数组

```c
void Reverse(int A[],int p,int n){
    int i,j,k=0;
    int temp=0;
    if(A==NULL || n==0){//判空操作
        return;
    }
    for(i=0;i<n/2;i++){ //先对整个数组进行一次翻转
        temp=A[i];
        A[i]=A[n-1-i];
        A[n-1-i]=temp;
    }
    for(j=0;j<p/2;j++){  //对p左边进行翻转
        temp=A[j];
        A[j]=A[p-j];
        A[p-j]=temp;
    }
    for(k=0;k<(n-p)/2;k++){  //对p右边进行翻转
        temp=A[k];
        A[k]=A[n-1-k];
        A[n-1-k]=temp;
    }
    return;
}
```



# 快速排序在算法题中应用

## 整理属于自己的快排代码

> [!important] 重点
> ```c
> void quicksort(int arr[],int left,int right){ 
>     if(left >= right){  //如果左指针大于等于右指针，返回
>         return;
>     }
>     int i=left,j=right,pivot=arr[left]; //定义左指针、右指针和基准值
>     while(i<j){
>         while(i<j&&arr[j]>=pivot){ //从右往左找第一个小于基准值的元素
>             j--;
>         }
>     	if(i<j){ //如果找到了，将元素赋值给左指针所指的元素
>         	arr[i]=arr[j];
>     	}
>    		while(i<j&&arr[i]<=pivot){ //从左往右找第一个大于基准值的元素
>         	i++;
>    	 	}
>     	if(i<j){ //如果找到了，将该元素赋值给右指针所指的位置
>         	arr[j]=arr[i];
>     	} 
> 	}
>     arr[i]=pivot;//将基准值放在正确的位置上
> 
>     quicksort(arr,left,i-1);//递归排序左半部分
>     quicksort(arr,i+1,right);//递归排序右半部分
> }
> ```
>
> 

## 寻找中位数

- [ ] 三刷

一个长度为L（>=1）的升序序列S,处在L/2上取整个位置的数称为S的中位数，两个序列的中位数是含他们所有元素的升序序列的中位数。现在又两个等长升序序列A和B，试设计一个在时间和空间两方面都尽可能高效的算法，找出两个序列A和B的中位数

> [!important] 重点
>
> ```c
> #define ERROR -1 // 错误返回值
> 
> int Mid_Search(int A[], int B[], int n) {
>     int a1 = 0, b1 = 0, count = 0; // 定义指针和计数器
> 
>     // 判定输入是否合法
>     if (A == NULL || B == NULL) {
>         return ERROR;
>      }
>     while (a1 < n && b1 < n) {
>         // 处理 A[a1] >= B[b1] 的情况
>         while (a1 < n && b1 < n && A[a1] >= B[b1]) {
>                  count++;
>             if (count == n) { // 当计数器达到 n，返回 B[b1]，即中位数
>                 return B[b1];
>             }
>             b1++;  // b1 指针后移
>         }
>         // 处理 B[b1] > A[a1] 的情况
>         while (a1 < n && b1 < n && B[b1] > A[a1]) {
>             count++;
>             if (count == n) { // 当计数器达到 n，返回 A[a1]，即中位数
>                  return A[a1];
>             }
>             a1++;  // a1 指针后移
>      }
>  }
> 
>  return ERROR; // 如果未找到，返回错误
> }
> 
> ```
>
> 
>
> ```c
> int M_Search(int A[],int B[],int n){
>     int s1,d1,m1,s2,d2,m2;
>     s1=0;d1=n-1;
>     s2=0;d2=n-1;
>      while(s1!=d1||s2!=d2){
>         m1=(s1+d1)/2;
>         m2=(s2+d2)/2;
>          if(A[m1]==B[m2])
>             return A[m1];		//满足条件①
>         if(A[m1]<B[m2]){		//满足条件②
>             if((s1+d1)%2==0){  //若元素个数为奇数
>                  s1=m1;		//舍弃A中间点以后的部分，且保留中间点
>                 d2=m2;  //舍弃B中间点以后的部分，且保留中间点
>             }
>             else{  //元素个数为偶数
>                 s1=m1+1;	//舍弃A的前半部分
>                 d2=m2		//舍弃B的后半部分
>             }
>         }
>         else{  				//满足条件③
>             if(s1+d1)%2==0{		//若元素个数为奇数
>              d1=m1;			//舍弃A中间的以后的部分，且保留中间点
>              s2=m2;		//舍弃B中间点以前的部分，且保留中间点
>          }
>          esle{			//元素个数为偶数
>                 d1=m1;		//舍弃A的后半部分
>                  s2=m2+1;  //舍弃B的前半部分
>             }
>         }
>     }
>      return A[s1]<B[s2]?A[s1]:B[s2];
> }
> ```
>
> 以上算法的空间复杂度为1，时间复杂度为logn





## 寻找主元素

<img src="https://fyywww.oss-cn-hangzhou.aliyuncs.com/img_for_typora/202509271337470.png" alt="image-20250816192940976" style="zoom:67%;" />

> [!important] 重点
> ```c
> int FindMain(int A[],int n){
>  if(A==NULL ||n==0){  //如果序列A为空，则输出-1
>      return -1;
>  }
>  int count[n]; //用数组count作为计数器
>  int i,j;
>  int k=0;
>  for(i=0;i<n;i++){
>      count[i]=0;   //对计数器count进行赋值，在之后如果元素对应上可以直接在相应位置+1
> 
>  }
>  for(j=0;j<n;j++){  //遍历数组A
>      k=A[j];  //让A[j]的元素赋值给k
>      count[k]++;  //在count数组里的k位置的值同样为k，所以达成了计数器的效果。
>      if(count[k]>n/2){
>          return A[j];  //如果当前计数器记到了n/2以上，说明找到了主元素
>      }
> 
>  }
>  return -1;//如果在for循环里还没有找到主元素的话，说明查找失败，输出-1
> }
> ```
>
> 暴力解

> [!important] 重点
> 王道的思想是：标记处一个可能称为主元素的元素NUM。然后重新计数，确认Num是否为主元素
>
> 1. 出现就+1，没出现就-1.到计数减到0时，将遇到的下一个数作为num，计数重新记为1，然后循环
> 2. 判断c中元素是否为真正的主元素。再次扫描该数组，统计c中的元素出现的次数，若大于n/2，则为主元素；否则，序列中不存在主元素。
>
> ![image-20250828182139088](C:\Users\FUYAOWEN\AppData\Roaming\Typora\typora-user-images\image-20250828182139088.png)
>
> else return -1; //不存在主元素
>
> }
>
> 

## 寻找未出现的最小正整数

题目：给定一个整数数组，请设计一个在时间上尽可能高效的算法，找出数组中未出现的最小正整数。例如，数组{-5，3，2，3}中未出现的最小正整数是1；数组{1，2，3}中未出现的最小正整数是4。要求：

1. 给出算法的基本思想
2. 根据设计思想描述算法，关键处给出注释
3. 说明你所设计算法的时间复杂度和空间复杂度；

****

- [x] 一刷

```c
void QuickSort(int arr[],int left ,int right){  
     if(left>=right)   
         return;
    int i=left,j=right,pivot=arr[left];
    while(i<j){
        while(i<j&&arr[j]>=pivot)
            j--;
        if(i<j)
            arr[i]=arr[j];
        while(i<j&&arr[i]<=pivot)
            i++;
        if(i<j)
            arr[j]=arr[i];
    }
    arr[i]=pivot;
    QuickSort(arr,left,i-1);
    QuickSort(arr,i+1,right);
}
// 找出未出现的最小正整数
int findMissingPositive(int arr[], int n) {
    QuickSort(arr, 0, n - 1);

    int i = 1;  // 当前期望的最小正整数
    for (int j = 0; j < n; j++) {
        // 跳过非正数
        if (arr[j] <= 0) continue;

        // 跳过重复元素
        if (j > 0 && arr[j] == arr[j - 1]) continue;

        // 如果当前元素等于 i，则 i 可以前进
        if (arr[j] == i) {
            i++;
        }
        // 如果当前元素 > i，说明 i 缺失
        else if (arr[j] > i) {
            break;
        }
        // arr[j] < i 的情况已经通过去重和排序避免
    }

    return i;
}

```

- [x] 二刷

> [!important] 重点
> 既然只是让时间上尽可能高效的算法，那我就对空间狠狠利用；我准备一个和整数数组正数一样长的临时数组，该数组从1一直到n都有记录。对给定数组进行遍历，如果遍历到一个正整数，就对临时数组的对应元素进行标记，标为1.就这样遍历完之后。对临时数组进行遍历，找到临时数组里第一个出现的未被标记的元素，该元素就是最小正整数。

```c
int minNum(int A[], int n) {
    int i, j, k, num = 0;
    int temp[n + 1];

    // 初始化 temp 数组
    for (i = 0; i <= n; i++) {
        temp[i] = i;
    }

    // 标记出现过的正数
    for (j = 0; j < n; j++) {
        if (A[j] > 0 && A[j] <= n) { // 增加一个判断，防止数组越界
            num = A[j];
            temp[num] = 0;
        }
    }

    // 查找第一个未出现的数字
    k = 1;
    while (k <= n && temp[k] == 0) {
        k++;
    }
    // 循环结束后，如果 k 还在数组范围内，返回 k
    // 如果 k 已经超过了 n，说明 1~n 都出现了，返回 n+1
    if (k > n) {
        return n + 1;
    } else {
        return k; // 其实这里直接返回 k 就可以了，因为 temp[k] 的值就是 k
    }
```



## 集合划分优化问题

- [x] 二刷
- [ ] 三刷

已知由n（n≥2）个正整数构成的集合A={aₖ|0≤k<n}，将其划分为两个不相交的子集A₁和A₂，元素个数分别是n₁和n₂，A₁和A₂中的元素之和分别为S₁和S₂。设计一个尽可能高效的划分算法，满足|n₁-n₂|最小且|S₁-S₂|最大。即：

1. 两个子集的元素个数之差最小
2. 在满足条件1的前提下，两个子集的元素和之差最大

> [!important] 重点
> 既然要满足个数差最小，以及元素和之差最大。那么我可以选择一个中位数，以他为界限划分两个集合。不是可以完美解决问题吗？！
>
> 1. 我先对集合进行快排，排序过后，选择第n/2个元素

```c
void QuickSort(int arr[],int left,int right){
    if(left > right){
        return;
    }
    int i=left,j=right,pivot=arr[left];
    while(i<j){
        while(i<j&&arr[j]>=pivot){
            j--;
        }
        if(i<j){
            arr[i]=arr[j];
        }
        while(i<j&&arr[i]<=pivot){
            i++;
        }
        if(i<j){
            arr[j]=arr[i];
        }
    }
    arr[i]=pivot;
    QuickSort(arr,left,i-1);
    QuickSort(arr,i+1,right);
}
void Partition(int arr[],int n){
    QuickSort(arr,0,n-1);
    int temp=arr[n/2];  //找到中位数
    int S1=0,S2=0;   //S1，S2用于存储元素和
    int n1=0,n2=0;  //n1,n2用于计数
    for(int i=0;i<n/2;i++){
        n1++;
        S1+=arr[i];
    }
    for(int j=n/2;j<n;j++){
        n2++
        S2+=arr[j];
    }
    printf("%d,%d\n",n1-n2,S1-S2);  
    return;
}
```



# 快速排序的“划分”思想在算法题中的应用

## 排序后找第k小

试编写一个算法，使之能够在数组L[1…n]中找出第K小的元素（从小到大排序后处于第k个位置的元素）

> [!important] 重点
> 先快排，排完之后直接指出第k小 nlogn
>
> 如果我利用快排的思想，每次只递归一半呢，因为快速排序每一次都能划分两边比他大或者比他小的

```c
int kth_elem(int a[], int low, int high, int k) {
    if (low == high) return a[low];  // 只有一个元素时直接返回
    
    int pivot = a[low];  // 选择第一个元素作为枢纽
    int i = low, j = high;  // 使用临时变量进行分区
    
    // 分区操作
    while (i < j) {
        while (i < j && a[j] >= pivot) j--;
        a[i] = a[j];
        while (i < j && a[i] <= pivot) i++;
        a[j] = a[i];
    }
    a[i] = pivot;  // 枢纽归位
    
    // 计算枢纽在当前范围内的排名（从1开始）
    int rank = i - low + 1;
    
    // 根据排名决定递归方向
    if (rank == k) {
        return a[i];  // 找到第k小的元素
    } else if (rank > k) {
        return kth_elem(a, low, i - 1, k);  // 在前半部分查找
    } else {
        return kth_elem(a, i + 1, high, k - rank);  // 在后半部分查找，调整k值
    }
}
```



# “归并排序”在算法题中的应用

## 基本功训练

完成归并排序“基本功训练”——实现Merge函数，将有序递增的int数组 A[N]、B[M] 合并为一个更大的有序数组

```c
ElemType* Merge(int A[], int n, int B[], int m) {
    ElemType *c = (ElemType*)malloc((n + m) * sizeof(ElemType));
    if (c == NULL) return NULL;
    
    int i = 0, j = 0, k = 0;
    
    while (i < n && j < m) {
        if (A[i] <= B[j]) {
            c[k++] = A[i++];
        } else {
            c[k++] = B[j++];
        }
    }
    
    while (i < n) c[k++] = A[i++];
    while (j < m) c[k++] = B[j++];
    
    return c; // 返回合并后的数组
} 

```



## 完整二路归并排序代码

在笔记本上手写整理完整的“二路归并排序”代码（可参考王道书或基础课视频）

```c
ElemType *B=(ElemType)malloc((n+1)*sizeof(ElemType)); //辅助数组B
void Merge(int A[], int low, int mid, int high) {
    //表的两段A[low..mid]和A[mid+1...high]各自有序，将他们合并成一个有序表
    int i,j,k;
    for(k=low;k<=high;k++)
        B[k]=A[k];					//将A中所有元素复制到B中
    for(i=low,j=mid+1,k=i;i<=mid&&j<=high;k++){ 
        if(B[i]<=B[j])			//比较B的两个段中的元素
            A[k]=B[i++];		//将较小值复制到A中
        else
            A[k]=B[j++];
    }
    while(i<=mid)	A[k++]=B[i++];	//若第一个表未检测完，复制
    while(j<=high)	A[k++]=B[j++];  //若第二个表未检测完，复制
}

void MergeSort(int A[],int low,int high){
    if(low<high){
        int mid=(low+high)/2;		//从中间划分两个子序列
        MergeSort(A,low,mid);		//对左侧子序列进行递归排序
        MergeSort(A,mid+1,high);	//对右侧子序列进行递归排序
        Merge(A,low,mid,high); //归并
    }
}

```



# 基于“链表”的算法训练

<img src="https://fyywww.oss-cn-hangzhou.aliyuncs.com/img_for_typora/202509271337686.png" alt="image-20250910173647931" style="zoom: 33%;" />

- [x] 【视频】看强化课录播视频《算法2.1.1_链表的算法题备考》

## 单链表的基本功训练

1. 在带头结点的单链表L中，删除所有值为x的结点，并释放看空间，假设值为x的结点不唯一，试编写算法以实现上述操作

   1. ```c
      void delete_all_nodes(LinkedList L, int x) {
          Node *pre = L;      // 前驱指针，指向头节点
          Node *curr = L->next; // 当前指针，指向第一个数据节点
          Node *temp;         // 临时指针，用于释放内存
          
          while (curr != NULL) {
              if (curr->data == x) {
                  // 找到要删除的节点
                  temp = curr;            // 保存要删除的节点
                  pre->next = curr->next; // 前驱节点指向后继节点
                  curr = curr->next;      // curr移动到下一个节点
                  free(temp);             // 释放内存
              } else {
                  // 当前节点不需要删除
                  pre = curr;         // pre跟上curr
                  curr = curr->next;  // curr移动到下一个节点
              }
          }
      }
      
      ```

2. 试编写在带头结点的单链表L中删除一个最小值结点的高效算法（假设该节点唯一）

   1. > 定义两个指针premin和cur，cur负责遍历，min负责指向当前最小值节点的前驱（当然需要一个临时变量temp来负责比较大小）,
      >
      > 1. 初始时min和cur都指向头结点，
      > 2. 然后cur开始遍历，若cur指向（temp初始化为0xfffffff）若小于temp，则temp更新，premin指针移动到cur前一个节点
      > 3. 若大于temp，则跳过，min指针不动
      > 4. 直到cur遍历完后，修改指针，再free（temp）

   2. ```c
      void delete_min(LinkList *L) {
          if (L == NULL || L->next == NULL) return;
          
          LinkList *premin = L;          // 最小值节点的前驱
          LinkList *temp = L->next;      // 当前最小值节点
          LinkList *curr = L->next;      // 当前遍历节点
      
          while (curr->next != NULL) {   // 检查curr的下一个节点
              if (curr->next->data < temp->data) {
                  // 找到更小的值
                  temp = curr->next;     // 更新最小值节点
                  premin = curr;         // 更新前驱节点
              }
              curr = curr->next;         // 移动到下一个节点
          }
          
          premin->next = temp->next;     // 删除最小值节点
          free(temp);
      }
      ```

3. 设在一个带表头结点的单链表中，所有节点的元素值无序，试编写一个函数，删除表中所有处于给定的两个值（作为函数参数给出）之间的元素（若存在）

   1. ```c
      void Delete_size(LinkedList *L, int a, int b) {
          if (L == NULL || L->next == NULL) return;
          
          LinkedList *cur = L;   // cur 指向头节点
          while (cur->next != NULL) {
              if (cur->next->data >= a && cur->next->data <= b) {
                  LinkedList *temp = cur->next;
                  cur->next = temp->next;
                  free(temp);
                  // 这里不移动 cur，因为需要检查新的 cur->next
              } else {
                  cur = cur->next;   // 只有不删除时才移动
              }
          }
      }
      ```

4. 在一个递增有序的单链表中，存在重复的元素。设计算法删除重复的元素。例如（7，10，10，21，30，42，42，42，51，70）将变为（7，10，21，30，42，51，70）

   1. ```c
      void Delete_Duplication(LinkedList *L) {
          // 处理边界情况
          if (L == NULL || L->next == NULL) {
              return;
          }
          
          LinkedList *pre = L;        // 前驱节点
          LinkedList *cur = L->next;  // 当前节点
          
          while (cur != NULL) {
              if (pre->data == cur->data) {
                  // 发现重复，删除当前节点
                  pre->next = cur->next;
                  free(cur);
                  cur = pre->next;    // 移动到下一个节点
              } else {
                  // 没有重复，正常前进
                  pre = cur;
                  cur = cur->next;
              }
          }
      }
      ```

5. 有两个循环单链表，链表头指针分别为h1和h2，编写一个函数将链表h2链接到链表h1之后，要求连接之后的链表仍保持循环链表形式

   1. ```c
      LinkList Link(LinkList &h1,LinkList &h2){
          //将循环链表h2链接到循环链表h1之后，使之仍保持循环链表的形式
          LNode *p,*q;			//分别指向两个链表的尾结点
          p=h1;
          while(p->next!=h1)   	//寻找h1的尾结点
              p=p->next; 
          q=h2;
          while(q->next!=h2) 		//寻找h2的尾结点
              q=q->next;
          p->next=h2;				//将h2链接到h1之后
          q->next=h1;				//令h2的尾结点指向h1
          return h1;
      }
      ```

      


## 真题训练

1. <img src="https://fyywww.oss-cn-hangzhou.aliyuncs.com/img_for_typora/202509271337737.png" alt="image-20250902125438854" style="zoom: 67%;" />

   1. ```c
      ElemType SearchNumK(LinkList *list,int k){
          LinkList *slow=ist;  //定义慢指针，初始在头结点
          LinkList *fast=list;	//定义快指针，初始在头节点
          if(k<=0){return;}    //如果k不合法，直接返回
          int count=k;  		//定义计数器
          while(count!=0){	//先让快指针移动k次，和慢指针保持k个距离
              fast=fast->next;
              count--;
          }
          while(fast!=null){	//快指针和慢指针同时移动，直到快指针移动到最后一个位置
              slow=slow->next;
              fast=fast->next;
          }
          return slow->data;		//输出倒数第k个位置的值
      }
      ```

      

2. <img src="https://fyywww.oss-cn-hangzhou.aliyuncs.com/img_for_typora/202509271337880.png" alt="image-20250902125500301" style="zoom:67%;" />

   1. ```c
      LinkList FindtheSame(LinkList *str1, LinkList *str2){
          // 定义遍历指针和计数指针
          LinkList *cur1 = str1;  // 用于最终比较的指针1
          LinkList *cur2 = str2;  // 用于最终比较的指针2
          LinkList *count1 = str1; // 用于统计链表1长度的指针
          LinkList *count2 = str2; // 用于统计链表2长度的指针
          int num1 = 0, num2 = 0;  // 链表1和链表2的长度计数器
          
          // 遍历链表1，统计节点个数
          while(count1 != NULL){
              count1 = count1->next;
              num1++;
          }
          
          // 遍历链表2，统计节点个数
          while(count2 != NULL){
              count2 = count2->next;
              num2++;
          }
          
          // 计算两个链表长度的绝对差值
          int x = abs(num1 - num2);
          
          // 如果链表1更长，让cur1先移动差值个节点
          if(num1 > num2){
              while(x != 0){
                  cur1 = cur1->next;
                  x--;
              }
          }
          // 如果链表2更长或等长，让cur2先移动差值个节点
          else{
              while(x != 0){
                  cur2 = cur2->next;
                  x--;
              }
          }
          
          // 现在cur1和cur2之后的节点数相同，同步遍历比较
          while(cur1 != NULL && cur2 != NULL){
              // 找到第一个数据相同的节点，即为公共后缀起始节点
              if(cur1->data == cur2->data){
                  return cur1; // 返回找到的公共节点
              }
              // 指针同步后移
              cur1 = cur1->next;
              cur2 = cur2->next;
          }
          
          return NULL; // 遍历结束未找到相同节点，返回NULL
      }
      
      int abs(int x) {
          return x > 0 ? x : -x;
      }
      ```


> [!important] 重点
> 王道
>
> ```c
> typedef struct Node{
>  char data;
>  struct Node *next;
> }SNode;
> //求链表长度的函数
> int listlen(Snode *head){
>  int len=0;
>  while(head->next!=NULL){
>      len++;
>      head=head->next;
>  }
>  return len;
> }
> //找出共同后缀的起始地址
> SNode* find_list(Snode *str1,Snode *str2){
>  int m,n;
>  SNode *p,*q;
>  m=listlen(str1);			//求str1的长度，O(m)
>  n=listlen(str2);			//求str2的长度，O(n)
>  for(p=str1;m>n;m--) 		//若m>n,使p指向链表中的第m-n+1个节点
>      p=p->next;
>  for(q=str2;m<n;n--)			//若m<n,使q指向链表中的第n-m+1个节点
>      q=q->next;
>  while(p->next!=NULL&&p->next!=q->next){		//查找共同后缀起始点
> 		 p=p->next;				//两个指针同步向后移动
>      q=q->next;
>  }
>  return p->next;			//返回共同后缀的起始地址
> }
> 
> ```

1. <img src="https://fyywww.oss-cn-hangzhou.aliyuncs.com/img_for_typora/202509271337002.png" alt="image-20250902125515690" style="zoom:67%;" />

2. > 既然要求时间上尽量高效，那我就在空间上狠狠不高效！
   >
   > 复制一条链表，把节点都用绝对值函数给他变为正   ，再用个邻接表当作计数器！，节点长度超过1就说明有重复！
   >
   > 有点麻烦0.o
   >
   > 那就用bool函数，对每次遍历的节点进行判断，到底出没出现过啊，如果出现过，那你不能留了。没出现过，那我就无视你了捏~
   >
   > 

```c
int abs(int x){
    if(x>=0) 	return x;
    else 	return -x;
}
bool isFirstTime(int x){
    bool A=(bool)malloc((n+1)sizeof(bool));
    if(A[abs(x)]==null){
        A[abs(x)]=abs(x);
        return true;
    }
    else return false;
}
void Delete_Must(LinkList *L){
    LinkList *temp=L;
    LinkList *prev=L;
    LinkList *curr=L'
    while(curr->next!=null){
        if(isFirstTime(curr->data)==true){
            curr=curr->next;
            prev=prev->next;
        }
        else{
            temp=curr;
            prev->next=curr->next;
            curr=curr->next;
            free(temp);
        }
    }
}
```

```c
typedef struct node{
    int data;
    sturct node *link;
}NODE;
Typedef NODE *PNODE;
void func(PNODE h,int n){
    PNODE p=h,r;
    int *q,m;
    q=(int *)malloc((n+1)sizeof(int*);		//申请n+1个位置的辅助空间
    for(int i=0;i<n+1;i++)					//数组元素初值置0；
        *(q+i)=0;
    while(p->link!=NULL){
        m=p->link->data>0? p->link->data:-p->link->data;

   		 if(*(q+m)==0){			//判断该节点的data是否已出现过
       	 	 *(q+m)=1;			//首次出现
       		 p=p->link;			//保留
   		 }
    	 else{					//重复出现
        	r=p->link;			//删除
        	p->link=r->link;
        	free(r);
    		}
    }
    free(q);
}
```

> [!important] 重点
> 辅助数组q的大小为n+1，每个元素的初值均为0；依次扫描链表中的每个节点，同时检查q[|data|]的值，若为0则保留该节点，并令q[|data|]=1;否则将该节点从链表中删除

## 单链表的“原地逆置”技巧

1. 试编写算法将带头结点的单链表就地逆置，所谓“就地”是指辅助空间复杂度为O(1)

   1. ```c
      LinkList List_HeadInsert(LinkList &L){ //逆向建立单链表
          LNode *s;
          int x;		//设元素类型为整型
          L=(LNode*)malloc(sizeof(LNode));  //创建头结点
          L->next=NULL;			//初始为空链表
          scanf("%d",&x);			//输入节点的值
          while(x!=9999){			//输入9999表示结束
              s=(LNode*)malloc(sizeof(LNode));	//创建新节点
              s->data=x;
              s->next=L->next;
              L->next=s;			//将新节点插入表中，L为头指针
              scanf("%d",&x);
          }
          return L;
      }
      ```

      

2. 设C=a1,b1,a2,b2,..,an,bn为线性表，采用带头结点的单链表存放，设计一个就地算法，将其拆分为两个线性表，使得A=a1,a2,a3,an; B=bn,bn-1…,b2,b1

   1. ```c
      void LinkList Divide(LinkList *L){
          if(L==null||L->next==null){
              return;
          }//判定是否不合法
          LinkList *cur=L;
          LinkList *temp=L;
          LinkList *mama=L;
          mama=L->next->next;//干脆直接用个指针指向b1吧
          while(cur->next->next!=null){
              temp=cur; //用temp存储cur的位置，cur接下来移动的话，还用cur的话比较复杂
              cur->next->next=L->next; //cur的下一个a元素插入到头结点后面
              temp->next=L->next;
              L->next=temp;   //这样就可以任由cur自由移动了
              cur=cur->next->next;
          }//这样的话我们就只用处理a数组就好了，a划分完毕则b也自动划分完毕了
          Lnew=(LNode*)malloc(sizeof(LNode*))  //创建一个新的头结点，用于拆开两个链表
          Lnew->next=mama;  //因为cur在循环里已经到了最后一个a的节点，所以这里就直接链接他后面的节点就行
          cur->next=null;
          
      }
      ```

      

3. 设有一个长度n为偶数的不带头结点的单链表，且节点值都大于0，设计算法求这个单链表的最大孪生和。孪生和定义为一个节点值与其孪生节点值之和，对于第i个结点（从0开始），其孪生结点为第n-i-1个结点

   1. ```c
      ElemType FindType(LinkList *L,int n){
          if(n%2!=0||n==0) 	return -1;
          LinkList *tail=L;
          LinkList *mid=L;
          LinkList *cur=L;
       
          while(tail->next!=null&&tail!=null){
              tail=tail->next->next;
              mid=mid->next;
          }
          ElemType Reverse(mid,tail ,n/2);
          int sum=0;
          while(tail!=null){
              if(sum<=cur->data+tail->data){
      	        sum=cur->data+tail->data;}
              cur=cur->next;
              tail=tail->next;
          }
          return sum;
      }
      ElemType Reverse(LinkList left,LinkList right,int lengthz){
          LinkList *temp=L;
          LinkList *cur=L;
          LinkList *pre=null;
          while(cur!=null){
              temp=cur;
              cur=cur->next;
              temp->next=pre;
              pre=temp;
          }
      }
      ```

      > 王道
      >
      > ```c
      > int PairSum(LinkList L){
      >  LNode *fast=L->next,*slow=L; //利用快慢双指针找到链表的中间点
      >  while(fast!=null&&fast->next!=null){
      >      fast=fast->next->next;	//快指针每次走两步
      >      slow=slow->next; 	//慢指针每次走一步
      >  }
      >  LNode *newHead=NULL,*p=slow->next,*tmp;
      >  while(p!=null){		//反转链表后一半部分的元素，采用头插法
      >      tmp=p->next;	//p指向当前待插入节点，令tmp指向其下一节点
      >      p->next=newHead;	//将p所指节点插入到新链表的首节点之前
      >      newHead=p;			//newHead指向刚才新插入的节点，作为新的首节点
      >      p=tmp;			//当前待处理节点变为下一节点
      >  }
      >  int mx=0;p=L;
      >  LNode*q =newHead;
      >  while(q!=null){			//用p和q分别遍历两个链表
      >      if((p->data+q->data)>mx)	//用mx记录最大值
      >          mx=p->data+q->data;
      >      p=p->next;
      >      q=q->next;
      >  }
      >  return mx;
      > }
      > ```
      >
      > 

4. 设线性表L=a1,a2,a3,…an-2,an-1,an采用带头结点的单链表保持，链表中的结点定义如下

```c
typedef struct node
{	int data;
	struct node*next;
}NODE;
```

请设计一个空间复杂度为O（1）且时间上尽可能高效的算法，重新排列L中的各结点，得到线性表L’=a1,an,a2,an-1,…

```c
typedef struct node{
	int data;
	struct node*next;
}Node,*LinkList;
ElemType Reverse(LinkList a,LinkList b){
	L1=(LinkList*)malloc(sizeof(LinkList));
	while(a!=null){
		  mid->next=L->next;
         L1->next=mid;
         mid=mid->next;
	}}
void Resort(LinkList *L){
    LinkList *mid=L;
    LinkList *end=L;
    while(end->next!=null){
        mid=mid->nexxt;
        end=end->next->next;
    }
    ElemTypeReverse(mid,end);
    L2=(LinkList*)malloc(sizeof(LinkList*));
    LinkList *Arr=L;
   	LinkList *Brr=L1;
    LinkList *temp1=null;
    LinkList *temp2=null;
    while(Arr->next!=null&&Brr->next!=null){
        temp1=Arr;
        temp1->next=Brr;
        Arr=Arr->next;
        temp2=Brr;
        temp2->next=Arr;
        Brr=Brr->next;
    }
}	

```

> [!important] 重点
> 王道
>
> ```c
> void change_list(NODE*h){
>  NODE *p,*q,*r,*s
>  p=q=h;
>  while(q->next!=NULL){	//寻找中间节点
>      p=p->next;				//p走一步
>      q=q-<next;
>      if(q->next!=NULL)	q=q->next;	//q走两步
>  }
>  q=p->next;		//p所指节点为中间节点，q为后半段链表的首节点
>  p->next=NULL;
>  while(q!=NULL){		//将链表后半段逆置
>      r=q->next;
>      q->next=p->next;
>      p->next=q;
>      q=r;
>  }
>  s=h->next;		//s指向前半段的第一个数据节点，即插入点
>  q=p->next;		//q指向后半段的第一个数据节点
>  =->next=NULL;
>  while(q!=NULL){		//将链表后半段的节点插入到指定位置
>      r=q->next;		//r指向后半段的下一个节点
>      q->next=s->next;	//将q所指节点插入到s所指节点之后
>      s->next=q;
>      s=q->next;		//s指向前半段的下一个插入点
>      q=r;
>  }
> }
> ```
>
> 

## 单链表的其他训练题

1. 设A和B是两个单链表（带头结点），其中元素递增有序。设计一个算法从A和B中的公共元素产生单链表C，要求不破坏A、B的结点

   > 1. 准备两个指针指向A和B的头结点，然后先比较再移动
   >
   > 2. 其中较小元素的指针向后移动，移动后再比较，如果相等，就将该节点复制到单链表c中
   >
   > 3. 如果某个链表提前遍历完，则退出
   >
   > 4. ```c
   >    void CreateLinkC(LinkList *A,LinkList*B){
   >        if(A==null&&B==null){
   >            return;
   >        }
   >        C=(Lnode*)malloc(sizeof(Lnode));
   >        LinkList *pointA=A;
   >        LinkList *pointB=B;
   >        LinkList *pointC=C;
   >        while(pointA->next!=null&&point->next!=null){
   >            if(pointA->data>pointB->data){
   >                pointB=pointB->next;
   >            }
   >            else if(pointA->data<pointB->data){
   >                pointA=pointA->next;
   >            }
   >            else{
   >                pointC->next=pointA;
   >                pointC=point->next;
   >                pointA=pointA->next;
   >                pointB=pointB->next;
   >                                                                            
   >            }
   >        }
   >    }
   >    ```
   >
   > 5. > 王道
   >    >
   >    > ```c
   >    > void Get_Common(LinkList A,LinkList B){
   >    >  LNode *p=A->next,*q=B->next,*r,*s;
   >    >  LinkList C=(LinkList)malloc(sizeof(LNode));	//建立表c
   >    >  r=C;			//r始终指向c的尾结点
   >    >  while(p!=NULL&&q!=NULL){		//循环跳出条件
   >    >      if(p->data<q->data)
   >    >          p=p->next;		//若A的当前元素较小，后移指针
   >    >      else if(p->data>q->data)
   >    >          q=q->next;		//若B的当前元素较小，后移指针
   >    >      else{	//找到公共元素节点
   >    >          s=(LNode*)malloc(sizeof(LNode));	
   >    >          s->data=p->data;//复制产生节点*s
   >    >          r->next=s;		//将*s链接到C上（尾插法）
   >    >          r=s;
   >    >          p=p->next;		//将表A和B继续向后扫描
   >    >          q=q->next;
   >    >      }
   >    >  }
   >    >  r->next=NULL;		//置C尾结点指针为空
   >    > }
   >    > ```
   >    >
   >    > 

2. 已知两个链表A和B分别表示两个集合，其元素递增排列。编制函数，求A和B的交集，并存放于A链表中

   > 1. ```c
   >    void Recreate(LinkList *A,LinkList *B){
   >        if(A==NULL&&B==NULL){
   >            return;
   >        }
   >        LinkList *pa=A;
   >        pa=A->next;
   >        LinkList *pb=B;
   >        LinkList *temp=NULL;
   >        LinkList *prepa=A;
   >        while(pa->next!=NULL&&pb->next!=NULL){
   >            if(pb->data>pa->data){
   >                temp=pa;
   >                pa=pa->next;
   >                prepa->next=pa;
   >                free(temp);
   >            }
   >            else if(pb->data<pa->data){
   >                pb=pb->next;
   >            else if(pb->data==pa->data){
   >                pa=pa->next;
   >                pb=pb->next;
   >            }
   >            }
   >        }
   >    }
   >    ```
   >
   > 2. > 王道
   >    >
   >    > ```c
   >    > LinkList Union(LinkList *la,LinkList *lb){
   >    >  LNode *pa=La->next;				//设工作指针分别为pa和pb
   >    >  LNode *pb=lb->next;
   >    >  LNode *u,*pc=la;				//结果表中当前合并节点的前驱指针pc
   >    >  while(pa!=NULL&&pb!=NULL)
   >    >      if(pa->data==pb->data){		 //交集并入结果表中
   >    >          pc->next=pa;		//A中节点链接到结果表
   >    >          pc=pa;
   >    >          pa=pa->next;
   >    >          u=pb;		//B中节点释放
   >    >          pb=pb->next;
   >    >          free(u);	
   >    >      }
   >    >  	else if(pa->data<pb->data){		//若A中当前节点值小于B中当前节点值
   >    >          u=pa;
   >    >          pa=pa->next;		//后移指针
   >    >          free(u);		//释放A中当前节点
   >    >      }
   >    >  	else{				//若B中当前节点值小于A中当前节点值
   >    >          u=pb;			
   >    >          pb=pb->next;		//后移指针
   >    >          free(u);		//释放B中当前节点
   >    >      }
   >    >  while(pa){			//B已遍历完，A未完
   >    >      u=pa;	
   >    >      pb=pa->next;
   >    >      free(u);		//释放A中剩余节点
   >    >  }
   >    >  while(pb){			//A已遍历完，B未完
   >    >      u=pb;
   >    >      pb=pb->next;	
   >    >      free(u);		//释放B中剩余节点
   >    >  }
   >    >  pc->next=NULL;	//置结果链表指针为NULL
   >    >  free(lb);		//释放B表的头结点
   >    >  return la;
   >    > }
   >    > ```
   >    >
   >    > 

3. 两个整数序列A=a1，a2，a3，…，am和B=b1，b2，b3，…，bn已经存入两个单链表中，设计一个算法，判断序列B是否是序列A的连续子序列

   1. ```c
      Bool IsSonList(LinkList *A,LinkList *B){
          LinkList *PA=A;
          LinkList *PB=B;
          if(A==NULL || B==NULL){
              return false;
          }
          while(PA->next!=NULL&&PB->next!=NULL){
              if(PA->next->data!=PB->next->data){
                  PA=PA->next;
                  PB=B；
              }
              if(PA->next->data==PB->next->data){
                  PA=PA->next;
                  PB=PB->next;
              }
          }
          if(PA->data==PB->data)
              return true;
          else return false;
      }
      ```

      

4. 设计一个算法用于判断带头结点的循环双链表是否对称

   1. ```c
      Bool Symetry(LinkList *L){    //L为带头结点的循环双链表
      	if(L->next==NULL || L->pre==NULL){
              return false;
          }
          LinkList *head=L;
          head=head->next;
          LinkList *tail=L;
          tail=tail->pre;
          while(tail!=head){
              if(head->data==tail->data){
                  head=head->next;
                  tail=tail->pre;
              }
              if(head->data!=tail->data){
                  return false;
              }
          }
          return true;
      }
      ```

      

5. 设有一个带头结点的非循环双链表L，其每一个结点中除了有pre,data,next域外，还有一个访问频度域freq，其值均初始化为0.每当在链表中进行一次Locate（L，x）运算时，令值为x的结点中freq域的值增1，并使此链表中的结点保持按访问频度递减的顺序排列，且最近访问的结点排在频度相同的结点之前，以便使频繁访问的结点总是靠近表头。试编写符合上述要求的Locate（L，x）函数，返回找到结点的地址，类型为指针型

   1. ```c
      DLinkList Locate(DLinkList *L,ElemType x){
          DNode *p=L->next,*q; //p为工作指针，q为p的前驱，用于查找插入位置
          while(p&&p->data!=x)
              p=p->next; //查找值为x的结点
          if(!=p)
              exit(0); //不存在值为x的结点
          else{
              p->freq++;	//令元素值为x的结点的freq域加1
              if(p->pre==L||p->freq>p->freq)
                  return p; 	//p是链表首结点，或freq值小于前驱
              if(p->next!=NULL)	p->next->pre=p->pre;
              p->pre->next=p->next;	//将p结点从链表上摘下
              q=p->pre;	//	以下查找p结点的插入位置
              while(q!=L&&q->freq<=p->freq)
                  q=q->pre;
              p->next=q->next;
              if(q->next!=NULL)	q->next->pre=p;	//将p结点排在同频率的第一个
              p->pre=q;
              q->next=p;
          }
          return p;		
      }
      ```

      

6. 设将n（n>1）个整数存放在不带头结点的单链表L中，设计算法将L中保持的序列循环右移k（0<k<n）个位置。例如，若k=1，则将链表0，1，2，3变为3，0，1，2

   1. ```c
      LNOde L=(LNode*)malloc(sizeof(LNode));
      LinkList MoveRight(LinkList *L,int k,int n){
          if(L==NULL || L->next==NULL){
              return;
          }
          if(k>n)	return;
          LNode  Lnew=(LNode*)malloc(sizeof(LNode));
          LinkList *tail=L;
          tail=L->next;
          LinkList *temp;
          LinkList *head=L;
          head=L->next;
          while(K!=0){
              k--;
              tail=tail->next;
          }
          while(tail->next!=NULL){
          temp=tail;
          Lnew->next=tail;
          tail=tail->next;
          tail->next=L->next;
          L->next=tail;}
          temp->next=head;
          
          return L;
      }
      ```

      > 王道
      >
      > ```c
      > LNode *Converse(LNode *L,int k){
      >  int n=1;		//n用来保存链表的长度
      >  LNode *p=L;		//p为工作指针
      >  while(p->next!=NULL){	//计算链表的长度
      >      p=P->next;
      >      n++;		
      >  }		//循环执行完后，p指向链表尾节点
      >  p->next=L;		//将链表连成一个环
      >  for(int i=1;i<=n-k;i++)	//寻找链表的第n-k个节点
      >      p=p->next;
      >  L=p->next;		//令L指向新链表尾结点的下一个结点
      >  p->next=NULL;	//	将环断开
      >  return L;
      > }
      > ```
      >
      > 

   

# 基于树、二叉树的训练

![[938593ceaa83d0d442edec9a30a6f23a_MD5.png]]

![[1d05dc57293175fed11982d59203ad68_MD5.png]]

## 二叉树的基本训练

1. 二叉树的前中后序遍历

   1. ```c
      //先序遍历
      void PreOrder(BiTree T){
      	if(T!=NULL){
      		visit(T);
      		PreOrder(T->lchild);
      		PreOrder(T->rchild);
      	}
      }
      
      //中序遍历
      void InOrder(BiTree T){
          if(T!=NULL){
              MidOrder(T->lchild);
              visit(T);
              MidOrdeer(T->rchild);
          }
      }
      
      //后序遍历
      void  PostOrder(BiTree T){
          if(T!=NULL){
              PostOrder(T->lchild);
              PostOrder(T->rchild);
              visit(T);
          }
      }
      ```

      

2. 二叉树的层序遍历(背)

   1. 需要借助队列

   2. ```c
      void LevelOrder(BiTree T){
          InitQueue(Q);		//初始化辅助队列
          BiTree *p;			//定义一个指针指向二叉树
          EnQueue(Q,T);		//将根节点入队
          while(!IsEmpty(Q)){	//队列不空则循环
              DeQueue(Q,p);	//队头节点出队
              visit(p);		//访问出队节点
              if(p->lchild!=NULL)
                  EnQueue(Q,p->lchild);	//若左孩子不空，则左孩子入队
              if(p->rchild!=NULL)			
                  EnQueue(Q,p->rchild);	//若右孩子不空，则右孩子入队
          }
      }
      ```

      

3. 二叉树结点的定义，实现求二叉树的高度

   1. ```c
      typedef struct BiTNode{
      	ElemType data;						//数据域
      	struct BiTNode *lchild,*rchild;  //左、右孩子指针
      }BiTNode,BiTree;
      int Height(BiTree T){
          if(T == NULL) 
              return 0;              // 空树高度为 0
          int hl = Height(T->lchild);
          int hr = Height(T->rchild);
          return (hl > hr ? hl : hr) + 1;   // 取较大者 +1
      }
      
      ```

4. 求二叉树的宽度

   1. ```c
      typedef struct BiTNode{
          ElemType data;
          struct BiTNode *lchild,*rchild;
      }BiTNode,*BiTree;
      int width[MAX]; //用于统计各层的节点总数
      //先序遍历，同时统计各层节点总数
      void PreOrder(BiTree T,int level){
          if(T==NULL) return;
          width[level]++; //累加该层节点总数
          PreOrder(T->lchild,level+1);	//遍历左子树
          PreOrder(T->rchild,level+1);	//遍历右子树
      }
      //求树的宽度
      void treeWidth(BiTree T){
          for(int i=0;i<MAX;i++)
              width[i]=0;
          PreOrder(T,0);	//先序遍历二叉树，统计各层节点总数
          int maxWidth=0; 	//找到最大宽度
          for(int i=0;i<MAX;i++){
              if(width[i]>maxWidth)
                  maxWidth=width[i];
          }
          printf("树的宽度是%d",maxWidth);
      }
      
      ```
      
      

5. 求二叉树的WPL

   1. ```c
      typedef struct BiTNode{
          int weight;
          struct BiTNode *lchild,*rchild;
      }BiTNode,*BiTree;
      int WPL=0;
      int PreOrder(BiTree *T,int level){
         	if(T==NULL)	return;
          if(T->lchild==NULL&&T->rchild==NULL){
              WPL+=level*T->weight;
          }
          PreOrder(T->lchild,level+1);
          PreOrder(T->rchild,level+1);
      }
      ```

6. 判定一棵树是否为二叉排序树

   1. ```c
      //采用中序遍历，看序列是否为递增序列
      typedef struct BiTnode{
          int weight;
          struct BiTnode *lchild;
          struct BiTnode *rchild;
      }BiTnode,*BiTree;
      int PostOrder(BiTree *T){
          if(T==NULL)		return;
          PostOrder(T->lchild);
          visit(T);
          PostOrder(T->rchild);
      }
      int Account(BiTree *T){
          if(T==NULL)		return;
          return	Account(T->lchild)+Account(T->rchild)+1;
      }
      bool IsSortTree(BiTree *T){
          if(T==NULL) 	return false;
          PostOrder(T);
          int n=Account(T);
          for(int i=0;i<n;i++){
              if(BiTree[i-1]>BiTree[i])
                  return false;
          }
          return true;
      }
      ```

   2. ```c
      //王道
      typedef struct BiTNode{
          int data; 	
          struct BiTNode *lchild,*rchild;	
      }BiTNode,*BiTree;
      
      int temp=MIN_INT;		//记录当前遍历到的最小值
      bool isBST=true；	//是否为二叉排序树？
      
      void InOrder(BiTree T){
          if(T==NULL)	return;
          InOrder(T->lchild);
          if(T->data>=temp)	temp=T->data;
          else  isBist=false
          InOrder(T->rchild);
      }
      ```

7. 判定一颗二叉树是否平衡

   1. ```C
      typedef struct BiTNode{
          int data;
          struct BiTNode *lchild;
          struct BiTNode *rchild;
      }BiTNode,*BiTree;
      bool isAvL=true;
      int Heigh(BiTree *T){
          if(T==NULL) return;
          int hl,hr;
          hL=Heigh(T->lchild);
        	hr=Heigh(T->rchild);
          if(hl-hr>1||hr-hl>1)
          isAvL=false;
          return(hl>hr?hl:hr)+1;
      }
      ```

   2. ```c
      //王道
      bool isBalance=true;
      int PostOrder(BiTree T){
          if(T==NULL)
              return 0;
          int left=PostOrder(T->lchild);
          int right=PostOrder(T->rchild);
          
          if(left-right> 1) isBalance =false;
          if(left-right< -1) isBalance =false;
          //树的深度=Max（左子树深度，右子树深度）+1
          if(left>right)	return left+1;
          else 			 return right+1;
      }
      ```

      

8. 判定一棵二叉树是否为完全二叉树

   1. ```c
      typedef struct BiTNode{            
          int number;
          struct BiTNode *lchild,*rchild;
      }BiTNode,*BiTree;
      int temp=0;
      bool isTotalTree(BiTree *T){ 
          if(T==NULL)	return true;
          Queue Q;
          InitQueue(Q); //初始化序列Q
          EnQueue(Q,T);  //根节点入队
          BiTree *p;
          while(Q!=NULL){
              Dequeue(Q,p);//当队列头不为空时，头节点出队
              visit(P);
              temp++;
              p->data=temp;  //给当前节点标上序号，从1开始
              if(p->lchild!=NULL)
      	        EnQueue(Q,p->lchild);  //左孩子入队
              if(p->rchild!=NULL)
         	     	EnQueue(Q,P->rchild);	//右孩子入队
              if(p->data!=1){			//当检索元素不是初始的队头元素的时候，就要开始检查是否符号完全二叉树的性质了
                  if(p->lchild->data!=p->data*2) 		return false;
                  if(p->rchild->data!=p->data*2+1)	return false;
              }
          }
      }
      ```

   2. ```c
      //王道
      void LevelOrder(BiTree T){
          InitQueue(Q);		//初始化辅助队列
          BiTree *p;			//定义一个指针指向二叉树
          EnQueue(Q,T);		//将根节点入队
          while(!IsEmpty(Q)){	//队列不空则循环
              DeQueue(Q,p);	//队头节点出队
              visit(p);		//访问出队节点
              if(p->lchild!=NULL)
                  EnQueue(Q,p->lchild);	//若左孩子不空，则左孩子入队
              if(p->rchild!=NULL)			
                  EnQueue(Q,p->rchild);	//若右孩子不空，则右孩子入队
          }
      }
      bool isComplete=true;		//表示为完全二叉树
      boo flag=false;	//flag=true，表示层序遍历时出现过叶子或只有左孩子的分支节点
      
      void visit(BiTNode *p){
          if(p->lchild==NULL&&p->rchild==NULL) flag=true;
          if(p->lchild==NULL&&p->rchild==NULL) isComplete=false;	//不是完全二叉树
          if(p->lchild==NULL&&p->rchild==NULL){
              if(flag) isComplete=false;	//不是完全二叉树
          	 flagg=true;
          }
          if(p->lchild==NULL&&p->rchild==NULL)
              if(flag)  isComplete=false;  //	不是完全二叉树
      }
      ```

      

9. 二叉树按二叉链表形式存储，试编写一个判别给定二叉树是否是完全二叉树的算法

   1. ```c
      //层序遍历，如果某节点缺孩子，那么他的后序节点都必须是叶子节点
      typedef struct BiTNode {
          struct BiTNode *lchild, *rchild;
      } BiTNode, *BiTree;
      bool IsComplete(BiTree *T){
          if(T==NULL)  return true; //空树也符合
          InitQueue(Q);  //初始化队列Q
          EnQueue(Q,T);  //根节点入队
          BiTree *p;		//定义一个指针
          while(Q !=NULL){	//若队列不为空
              DeQueue(Q,p);  //队头节点出队
              visit(p);	//访问p节点
              if(p==NULL); //如果p节点为空，说明是叶结点了，那么后面就不应该再有非叶节点了
              EnQueue(Q,p->lchild);
              EnQueue(Q,p->rchild);
              DeQueue(Q,p);
              if(p!=NULL);  //此时队头节点后面还有非叶节点，说明此时不算完全二叉树
              	return false
          }
          return true; 	  //如果遍历完节点了还是没有出错，那么你过关！
      }
      
      
      ```

      

10. 假设二叉树采用二叉链表存储，试设计一个算法，计算一颗给定二叉树的所有双分支结点个数

    1. ```c
       typedef struct BiTNode{
           int data;
           struct BiTNode *lchild,*rchild;
       }BiTNode,BiTree;
       int count = 0;
       int  PreOrder(BiTree *T){
           if(T==NULL)	return 0;
           visit(T);
           if(T->lchild!=NULL&&T->rchild!=NULL){
               count++;
           }
           PreOrder(T->lchild);
           PreOrder(T->rchild);
           return count;
       }
       
       ```

       

11. 假设二叉树采用二叉链存储结构存储，设计一个算法，求先序遍历序列中第k（1<=k<=二叉树中结点个数）个节点的值

    1. ```c
       typedef struct BiTNode{
           int data;
           struct BiTNode *lchild;
           struct BiTNode *rchild;
       }BiTNode,BiTree;
       int count = 0;  // 计数器
       int result;     // 保存第 K 个节点
       int K;
       void PreOrder_K(BiTree T) {
           if(T == NULL) return;
       
           count++;
           if(count == K) {
               result = T->data;
               return; // 找到 K 就停止
           }
       
           PreOrder_K(T->lchild);
           if(count < K) 
           PreOrder_K(T->rchild); // 已经找到就不遍历右子树
       }
       
       ```

       


## 二叉树的真题训练

1. 二叉树的带权路径长度是二叉树中所有叶节点的带权路径长度之和。给定一颗二叉树T，采用二叉链表存储，left，weight，right，其中叶结点的weight域保存该节点非负权值。设root为指向t的根节点的指针，请设计求t的wpl的算法

   1. ```c
      typedef struct BiTNode{
          int weight;
          struct BiTNode *left;
          struct BiTNode *right;
      }BiTNOde,BiTree;
      int WPL=0;
      int LevelOrder(BiTree &T,int level){
          if(T==NULL)		return -1;  //其实这句没必要，因为AccWPL函数里已经检查了一遍
          InitQueue(Q);
          EnQueue(T,level);
          BiTree *q;
          while(IsEmpty(Q)==false){
              DeQueue(Q,q);
              visit(q);
              if(q->left!=NULL)
                  EnQueue(q->right,level+1);
              if(q->right!=NULL)
                  EnQueue(q->right,level+1)
              if(q->left==NULL&&q->right==NULL)
                  WPL+=level*p->weight;
      return WPL;
      }
      void AccWPL(BiTree &T){
          if(T==NULL)		return -1;
          LevelOrder(T,0);   //规定根节点为第0层
      }
      ```

   2. ```c
      void PreOrder(BiTree T, int level){
          if (T == NULL) return;
      
          if (T->left == NULL && T->right == NULL)
              WPL += level * T->weight;
      
          PreOrder(T->left, level + 1);
          PreOrder(T->right, level + 1);
      }
      ```

      

2. 将给定的表达式树（二叉树）转换为等价的中缀表达式（通过括号反映操作符的计算次序并输出）。例如，当下列两颗表达式树作为算法的输入时，输出的等价中缀表达式分别为(a+b)*(c*(-d))  and (a*b)+(-(c-d))       

   1. ![[0c1d558d28a8066e361d4dcb0492eca0_MD5.png]]

   2. 二叉树的节点定义如下

   3. > 可以基于中序遍历，给叶结点和根节点作特殊标记，扫描到时不需要加括号

   4. ```c
      typedef struct node{
          char data[10];
          struct node *left, *right;
      }BTree;
      ```

   5. ```c
      void BtreeToE(BTree &root){
          BtreeToExp(root,1);
      }
      void BtreeToExp(BTree &root,int deep){
          if(root==NULL)	return;
          else if(root->left==NULL&&root->right==NULL)//如果子树为空，说明为叶节点，直接输出
      			printf("%s",root->data);  //输出操作数，不加括号
      
          else{
      		if(deep=1)	printf("%s",root->data);
      
             if(deep>1) {
             print("(");				//若有子表达式则加一层括号
             BtreeToExp(root->left,deep+1);
             printf(("%s",root->data));			//输出操作符
             BtreToExp(root->right,deep+1);
             if(deep>1)	printf(")");		//	若有子表达式则加1层括号
          }
             
       }
      
      
      ```

      

3. 已知非空二叉树T的节点值均为正整数，采用顺序存储方式保存，数据结构定义如下

   1. ![[7f40081245b5ce3f306322eea96747ce_MD5.png]]

   2. ![[fe0f65920a2e9fef7a3626df536b1268_MD5.png]]

   3. ```c
      typedef struct{			//MAX_Size为已定义常量
          int SqBiTnode[MAX_SIZE];		//保存二叉树节点值的数组
          int ElemNum;			//实际占用的数组元素个数
      }SqBiTree;
      
      ```
      
      > 使用整型变量val记录中序遍历过程中已遍历节点的最大值，初值为一个负整数。若当前遍历的节点值小于或等于val，则算法返回false，否则，将val的值更新为当前节点的值。
      
      ```c
      bool isBST = true;
      int prev = INT_MIN;
      void InOrderArray(int A[], int n, int i) {
          if (i >= n || A[i] == -1 || !isBST) 
             return;
          InOrderArray(A, n, 2*i + 1); // 左子树
          if (A[i] >= prev) {
              prev = A[i];
          } else {
              isBST = false;
              return;
          }
          InOrderArray(A, n, 2*i + 2); // 右子树
      }
      ```
      
      > 设置两个数组pmax和pmin。根据二叉搜索树的定义，SqBiTNode[i]中的值应该大于以SqBiTNode[2i+1]为根的子树的最大值（保存在pmax[2i+1]中），小于以SqBiTNode[2i+2]为根的子树中的最小值（保存在pmin[2i+1]中）。初始时，用数组SqBiTNode中前ElemNum个元素的值对数组pmax和pmin初始化
      >
      > 在数组SqBiTNode中从后向前扫描，扫描过程中逐一验证节点与子树之间是否满足上述的大小关系。
      
      ```c
      #define false 0
      #define true 1
      typedef int bool;
      bool judgeBST(SqBiTree bt){
          int k,m,*pmin,*pmax;
          pmin=(int *)malloc(sizeof(int)*(bt.ElemNum));
          pmax=(int *)malloc(sizeof(int)*(bt.ElemNum));
          for(k=0;k<bt.ElemNum;k++)		//辅助数组初始化
              pmin[k]=pmax[k]=bt.SqiBiTNode[k];
          for(k=bt.ElemNum-1;k>0;k--){//从最后一个叶节点向根遍历
          	if(bt.SqBiTNode[k]!=-1){
                  m=(k-1)/2;
                  if(k%2==1&&bt.SqBiTNode>pmax[k])	//其为左孩子
                      pmin[m]=pmin[k];
                  else if(K%2==0&&bt.SqBiTNode[m]<pmin[k])	//其为右孩子
                      pmax[m]=pmax[k];
                  else return false;
              }
          }
          return true;
      }
      ```
      
      ## 二叉树的拔高训练
      
      - 设树B是一颗采用链式存储结构存储的二叉树，编写一个把树B中所有节点的左右子树进行交换的函数
      
      ```c
      typedef struct BiTNode{
          int data;
          struct BiTNode *left;
          struct BiTNode *right;
      }BiTNode,*BiTree;
      
      void Reverse(BiTree &T){
          BiTNode *temp1;
          BiTNode *temp2;
          if(T==NULL)	 return;
          if(T->left!=NULL || T->right !=NULL){
              temp1=T->left;
              temp2=T->right;
              T->left=temp2;
              T->right=temp1;
          }
          Reverse(T->left);
          Reverse(T->right);
      }
      ```
      
      - 已知二叉树以二叉链表存储，编写算法完成：对于树种每个元素值为x的节点，删除以他为根的子树，并释放相应的空间
      
      ```c
      void DeleteTree(BiTree &T) { // 删除整棵树
          if (T == NULL) return;
          DeleteTree(T->left);
          DeleteTree(T->right);
          free(T);
      }
      
      void DeleteSubtree(BiTree &T, int x) {
          if (T == NULL) return;
      
          if (T->data == x) {
              if (T->left) {
                  DeleteTree(T->left);
                  T->left = NULL;
              }
              if (T->right) {
                  DeleteTree(T->right);
                  T->right = NULL;
              }
              return; // 不动自己
          }
      
          DeleteSubtree(T->left, x);
          DeleteSubtree(T->right, x);
      }
      
      ```
      
      - 在二叉树中查找值为x的节点，试编写算法打印值为x的节点所有祖先，假设值为x的节点不多于1个
      
      ```c
      typedef struct BiTNode{
          int data;
          struct BiTNode *lchild;
          struct BiTNode *rchild;
      }BiTNode,*BiTree;
      bool PrintAncestors(BiTree &T, int x) {
          if(T == NULL) return false;
      
          if(T->data == x) return true;
      
          if(PrintAncestors(T->lchild, x) || PrintAncestors(T->rchild, x)) {
              printf("%d ", T->data);  // 递归返回时打印祖先
              return true;
          }
      
          return false;
      }
      
      ```
      
      - 设一个二叉树的节点结构为（LLINK,INFO，RLINK）,ROOT为指向该二叉树根节点的指针，p和q分别为指向该二叉树中任意两个节点的指针，试编写算法ANCESTOR(ROOT,p,q,r),找到p和q的最近公共祖先节点r
      
      - > 和上一题都可以采用非递归的后序遍历（“后人认祖先”）
        >
        > 使用一个栈，从根节点开始遍历，若当前节点非空，则入栈
        >
        > 若当前节点为空，则看栈顶元素，若栈顶元素的右子树为空或者访问过，则栈顶元素出栈。否则遍历栈顶元素的右子树
      
        当访问到某个节点时，栈中所有元素均为该节点的祖先，后序遍历必然先遍历到p（假设p在q的左边）。栈中元素此时都为p的祖先，再造一个辅助栈把原栈里的元素复制过去，后序的操作就在辅助栈里进行：继续遍历到节点q时，将栈中元素从栈顶开始逐个到辅助栈中区匹配，第一个匹配（相等）的元素就是节点p和q的最近公共祖先
      
        
      
      ![[3d7070841119514f9e2b2ba45c881f98_MD5.png]]
      
      - 设有一颗满二叉树（所有节点值均不同），已知其先序序列为pre，设计一个算法求其后序序列post
      
      ```c
      void PreToPost(int pre[], int post[], int preL, int preR, int postL, int postR) {
          if (preL > preR) return;
          if (preL == preR) { // 叶子节点
              post[postR] = pre[preL];
              return;
          }
      
          int rootVal = pre[preL];
          int n = preR - preL + 1;       // 子树节点数
          int leftSize = (n - 1) / 2;    // 左子树节点数
      
          // 左子树对应 pre[preL+1 ... preL+leftSize]
          // 右子树对应 pre[preL+leftSize+1 ... preR]
      
          // 后序中根在末尾
          post[postR] = rootVal;
      
          // 递归构造左子树
          PreToPost(pre, post,
                    preL + 1, preL + leftSize,
                    postL, postL + leftSize - 1);
      
          // 递归构造右子树
          PreToPost(pre, post,
                    preL + leftSize + 1, preR,
                    postL + leftSize, postR - 1);
      }
      
      
      ```
      
      ```c
      void PreToPost(ElemType pre[],int l1,int h1,ElemType post[],int l2,int h2){
          int half;
          if(h1>=l1){
              post[h2]=pre[l1];
              half=(h1-l1)/2;
              PreToPost(pre,l1+1,l1+half,post,12,12+half-1); //转换左子树
              PreToPost(pre,l1+half+1.h1,post,12+half,h2-1); //转换右子树
          }
      }
      ```
      
      
      
      - 试设计判断两个二叉树是否相似的算法。所谓二叉树相似，指的是都为空或者都只有一个根节点；或者他们的左子树和右子树是相似的
      
      ```c
      typedef struct BiTNode{
          int data;
          struct BiTNode *leftS;
          struct BiTNode *rightS;
      }BiTNode,*BiTree;
      bool Similar(BiTree &T1,BiTree &T2){
          if(T1==NULL&&T2==NULL)	return true;	//全为空
          else if(T1!=NULL&&T2==NULL)  return false;		
          else if(T1==NULL&&T2!=NULL)  return false;
          else{
              Similar(T1->leftS,T2->leftS);	//递归左子树
              Similar(T1->rightS,T2->rightS);	//递归右子树
              return false;  //如果能来到这一步，就说明出现问题了，按理说在上面递归的时候就会return了
          }
      }
      ```
      
      # 基于“图”的算法训练
      
      1. **邻接矩阵的数据结构定义**
      
      ```c
      #define MaxVertexNum 100;
      typedef char VertexType;
      typedef int EdgeType;
      typedef struct{
          VertexType vex[MaxVertexNum];		//顶点表
          EdgeType edge[MaxVertexNum][MaxVertexNum];  //邻接矩阵（边表）
          int vexnum,arcnum;	//图当前的顶点数，边数
      }MGraph;
      ```
      
      
      
      1. **邻接表的数据结构定义**
      2. 关键在于边表和顶点表之间，顶点表有给头指针指向
      
      ```c
      #define MaxVertexNum 100;		//图中顶点数目的最大值
      typedef struct ArcNode{			//边表节点
          int adjvex;					//该弧所指向的顶点的位置
          struct ArcNode* nextarc;	//指向下一条弧的指针
          //InfoType info;		//网的边权值
      }ArcNode;
      typedef struct VNode{		//顶点表节点
          VertexType data;		//顶点信息
          ArcNode *firstarc;		//指向第一条依附该顶点的弧的指针
      }VNode,AdjList[MaxVertexNum];		
      typedef struct{
          AdjList vertices;		//邻接表
          int vexnum,arcnum;		//图的顶点数和弧数
      }ALGraph;					//ALGraph是以邻接表存储的图类型
      ```
      
      
      
      ## 图的真题
      
      1. 已知无向连通图G由顶点集V和边集E组成，E>0,当G中度为奇数的顶点个数为不大于2的偶数时，G存在包含所有边且长度为E的路径（称为EL路径）。设图G采用邻接矩阵存储，类型定义如下：
      
      ```c
      typedef struct{		//图的定义
          int numVertices,numEdges;	//图中实际的顶点数和边数
          char VerticesList[MAXV];	//顶点表。MAXV为已定义常量
          int Edge[MAXV][MAXV]; //邻接矩阵
      }MGraph；
      ```
      
       请设计算法 int IsExistEL(MGraph G),判断G是否存在EL路径，若存在，则返回1，否则返回0
      
      ```c
      #define MaxVertexNum 100;
      typedef int EdgeType;
      typedef char VertexType;
      typedef struct{
          int arcNum,vexNum;
          VertexType Vertex[MaxVerTexNum];
          EdgeType Edge[MaxVerTexNum][MaxVerTexNum];
      }MGraph,*MGraphPTr;
      int IsExistEL(MGraph &G){
          int count=0;		//count 用来记录当前度为奇数的顶点的个数
          for(int i=0;i<G.vexNum;i++){
              int sum=0;			//sum用来记录当前节点的度
              for(int j=n-i;j<vexNum;j++){
                  if(Edge[i][j]!=0){
                      sum+=1;
                  }
              }
              if(sum%2!=0){
                  count+=1;
              }
          }
          if(count!=0&&count!=2){	//如果不等于0和2其中一个，那就不满足El要求
              return 0;
          }
          return 1;
      }
      ```
      
      > 订正：
      >
      > 1. 缺少连通性判断
      > 2. 
      
      1. 已知有向图G采用邻接矩阵存储，类型定义如下：
      
      
      
      将图中出度大于入度的顶点称为K顶点。例如，在下图中，顶点a和b为k顶点
      
      ![[779ad03e210fe20533326f26d008a90f_MD5.png]]

```c
#define MaxVertexNum 100
typedef int EdgeType;
typedef char VertexType;
typedef struct{
    int arcnum,vexnum;
    VertexType vex[MaxVertexNum];
    EdgeType edge[MaxVertexNum][MaxVertexNum];
}MGraph;

int printVertices(MGraph G){
    int sum=0;
    for(int i=0;i<G.vexnum;i++){
        int innum=0,outnum=0;
        for(int j=0;j<G.vexnum;j++){
            if(G.Edge[i][j]==1)	outnum+=1;
            if(G.Edge[j][i]==1&&j!=i)	innum+=1;
        }
        if(innum<outnum){
            printf("%d",vex[i]);
            sum++;
        }
       
    }
    return sum;
}   

   
```

