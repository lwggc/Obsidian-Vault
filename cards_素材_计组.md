# 计算机组成原理 — 卡片素材

> 使用说明：每张卡片我已取好标题和整理好内容。你需要在 Markji 中手动添加 [F##...] 挖空标记。
> 建议：每张卡片挖 2~4 个空，别挖太多，否则记不住。
> 📷 标注表示原笔记该部分有配图，请对照你的原笔记插入对应图片。

---

## 卡1 — 标题：冯诺依曼计算机

冯诺依曼计算机的特点：
- **控制流驱动方式**：按指令执行序列依次读取指令
- 五大部件：输入设备、输出设备、运算器、存储器、控制器
- 指令和数据以同等地位存于存储器，按地址访问
- 指令由操作码和地址码组成
- **存储程序**：把指令事先输入主存储器，程序开始执行后不需人为干预
- 以**运算器**为中心

> 📷 原笔记该部分有配图

---

## 卡2 — 标题：现代计算机结构

现代计算机以**存储器**为中心，优化了冯诺依曼结构以运算器为中心的弊端。

**CPU = 控制器 + 运算器**，绝大多数CPU也集成了MAR和MDR。CPU的核心单元为ALU和CU。

CPU和主存储器共同构成主机，除主机外的硬件统称为外部设备（外设）。

主存储器的工作方式是**按址存取**。指令和数据都以二进制的形式存储在存储器中。

> 📷 原笔记该部分有配图

---

## 卡3 — 标题：计算机软件

软件按功能分：**系统软件**（OS、数据库、语言处理程序）和**应用软件**。

三种语言：机器语言（唯一能被计算机直接执行）、汇编语言、高级语言。
三种翻译程序：汇编程序、解释程序（逐行翻译）、编译程序。

**ISA（指令集体系结构）**：是硬件和软件之间的界面，又叫软件可见部分。ISA定义了一台计算机可以支持哪些指令。汇编语言与ISA一一对应。

软件和硬件具有**逻辑功能等价性**。软件实现性能低成本低，硬件实现性能高成本高。

> 📷 原笔记该部分有配图

---

## 卡4 — 标题：计算机性能指标

- **时钟周期** = 1/主频，CPU中最小时钟单位
- **主频** = 1/时钟周期，单位Hz
- **CPI**：执行一条指令所需的时钟周期数（与时钟频率无关）
- **CPU执行时间** = 指令条数 × CPI × 时钟周期 = 指令条数 × CPI / 主频
- **IPS** = 主频 / 平均CPI（每秒执行多少条指令）
- **MIPS** = 指令条数/(时间×10⁶) = 主频/(CPI×10⁶)
- **FLOPS**：每秒执行多少次浮点运算

机器字长、存储字长和指令字长：
- 机器字长 = CPU内部运算器位数（与ALU有关），决定运算精度
- 存储字长 = 存储单元的位数，等于MDR位数
- 指令字长取决于指令的功能和格式
- **三者都必须是字节的整数倍**

> 📷 原笔记该部分有配图

---

## 卡5 — 标题：原码、补码、反码

**原码**：符号位+数值位
**补码**：机器唯一识别的码型。正数的补码和原码相同。负数符号位不变，数值位取反+1。
- 补码表示范围：-2^(n-1) ~ 2^(n-1)-1
- [-x]补 ↔ [x]补：**全部位取反+1**
- 补码的符号位也可参与计算，能直接求出真值

**移码**：由补码符号位取反得来。移码表示范围为 -2^(n-1)~2^(n-1)-1。
- 移码中0表示唯一
- 移码越大真值越大，移码越小真值越小

**变形补码（模4补码）**：双符号位，其余和补码相同。

> 📷 原笔记该部分有配图

---

## 卡6 — 标题：加减运算与溢出判断

补码加减：
- [x+y]补 = [x]补 + [y]补
- [x-y]补 = [x]补 + [-y]补，[-y]补由[y]补全部位取反末位加1

**三种溢出判断方法**（都需要溢出判断电路，一般用异或实现）：
1. **一位符号位**：结果符号与两个相同的操作数符号不同则溢出
2. **双符号位（模4补码）**：10负溢出，01正溢出（与左移无关）
3. **一位符号位根据进位**：C_n 和 C_(n-1)不同就溢出

双符号位只在ALU中使用，存储时只需1个符号位。高符号位表示最终结果符号，低符号位表示是否溢出。

> 📷 原笔记该部分有配图

---

## 卡7 — 标题：移位运算与乘除运算

**算术移位**（有符号数）：左移×2，右移÷2。符号位参加运算，移位后发生变化就算溢出。
**逻辑移位**（无符号数）

原码乘法：符号位与数值位分别处理。符号位单独异或，数值位当无符号数相乘。

**阵列乘法器**：并行乘法器，所有乘积在同一时间内计算出来。

**无符号除法一定不会溢出。**

> 📷 原笔记该部分有配图

---

## 卡8 — 标题：整数类型转换

**扩展**：
- 短→长：有符号数则符号扩展（高位补符号），无符号数则零扩展（高位补0）
- 仅位数相同：改变解释方式

**类型转换结论**：
- 范围：double > float > int
- 精度：double > int > float
- char的有无符号性由实现定义（x86默认signed），转换为int时：unsigned char高位补0，signed char符号扩展

> 📷 原笔记该部分有配图

---

## 卡9 — 标题：ALU与标志位

**ALU是运算器的核心，加法器是ALU的核心。** ALU的运算数、结果位数与机器字长相同。

功能：算术运算（加减乘除）、逻辑运算（与或非异或移位）、其他（求补码等）。

**标志位**（送入PSW程序状态字寄存器）：
- ZF：零标志
- OF：溢出标志（有符号数用），最高位和次高位进位异或
- SF：符号标志
- CF：进位/借位标志（无符号数用），CF = Sub ⊕ C_out

有符号运算只看 **OF、SF、ZF**，无符号运算只看 **ZF、CF**。

> 📷 原笔记该部分有配图

---

## 卡10 — 标题：浮点数基本概念

浮点数真值 = (-1)^数符 × 尾数 × 基数^阶码

- 阶码：补码或移码表示的定点整数，反映表示范围和小数点实际位置
- 尾数：原码或补码表示的定点小数，位数越小精度越低

**下溢**：运算结果绝对值小于最小绝对值
**上溢**：阶码大于最大允许值

**规格化**：
- IEEE754标准：格式为 1.xxxx（隐藏位为1）
- 左规：尾数左移一位，阶码减1（可能多次）
- 右规：尾数右移一位，阶码加1（只需一次）
- **IEEE754浮点乘法永远不用左规**（尾数范围[1,2)，乘积范围[1,4)，最多一次右规）

> 📷 原笔记该部分有配图

---

## 卡11 — 标题：IEEE754标准

| 阶码 | 尾数 | 含义 |
|:---:|:---:|:---|
| 全0 | 全0 | ±0（符号位决定正负）|
| 全0 | 非0 | 非规格化数（隐藏位为0，偏置值126）|
| 全1 | 全0 | ±∞ |
| 全1 | 非0 | NaN |

单精度：1位符号 + 8位阶码 + 23位尾数，偏置值127
双精度：1位符号 + 11位阶码 + 52位尾数，偏置值1023

**舍入**（浮点数独有概念，普通定点数没有）：
IEEE标准默认0舍1入法（向最近偶数舍入）。
舍入发生在两个时机：**对阶时**和**右规时**。左规不丢失有效位，不需要舍入。

> 📷 原笔记该部分有配图

---

## 卡12 — 标题：数据存储与边界对齐

**大端方式**：先存储高位字节，顺序与原序列相同
**小端方式**：先存储低位字节，顺序与原序列相反

**边界对齐核心规则**：
1. K字节的数据的起始地址必须是K的倍数
2. 一次存取一个机器字长

**结构体对齐**：
1. 每个成员按其类型大小对齐（char1B, short2B, int4B）
2. struct长度必须是成员中最大对齐值的整数倍
3. 变量内部受大小端影响，变量之间无影响

> 📷 原笔记该部分有配图

---

## 卡13 — 标题：主存与DRAM/SRAM

**SRAM**（双稳态触发器）：非破坏性读出，速度快，集成度低，功耗大，价格贵。一般用于Cache。
**DRAM**（栅极电容）：破坏性读出，集成度高，价格低。一般用于主存。需要刷新。

**DRAM刷新**：
- 刷新是存储器独立完成的，刷新单位是行，所有芯片同时被刷新
- 刷新周期通常为2ms
- **集中刷新**：存在死时间（访存死区）
- **分散刷新**：将存取周期翻倍，无死区
- **异步刷新**：死时间更加分散

> 现代计算机主存通常基于**SDRAM**（同步DRAM），支持突发传送。

> 📷 原笔记该部分有配图

---

## 卡14 — 标题：主存储器组成与并行主存

主存储器由**MAR（存储地址寄存器）、MDR（存储数据寄存器）、时序控制逻辑和存储体**构成。

**改善主存速度的方法**：
1. 提高DRAM芯片本身速度
2. 采用并行结构（双端口存储器、多模块存储器）
3. 在CPU和主存间增加Cache

**多模块存储器**：
- **高位多体交叉**（顺序编址）：主要目的扩充容量
- **低位多体交叉**（交叉编址）：相邻地址在不同存储体，流水线工作时连续读取m个字需 T + (m-1)r

**访存冲突判断**：出现在同一模块下并且相邻则会发生访存冲突。

> 📷 原笔记该部分有配图

---

## 卡15 — 标题：位扩展与字扩展

**位扩展**（字长扩展/数据总线扩展）：芯片数据位宽小于CPU数据位宽时采用。各芯片数据线并行连接，地址线共享。

**字扩展**（容量扩展/地址总线扩展）：芯片字数不够时采用。
- **线选法**：用高位地址线直接连芯片片选端，地址空间不连续，不需要译码器
- **译码片选法**：用高位地址线通过地址译码器产生片选信号，地址空间连续

**字位同时扩展**：先位扩展，再字扩展。

> 📷 原笔记该部分有配图

---

## 卡16 — 标题：磁盘与外存

**磁盘**：按块存取。
- 存取时间 = 平均寻道时间 + 旋转延迟时间 + 传输时间
- 延迟时间取旋转半周的时间
- 磁盘读写最小单位是扇区

**RAID**：
- RAID0：条带化，提高存取速度，无容错能力
- RAID1：磁盘镜像互为备份，容量减少一半
- RAID2-5：通过奇偶校验提高容错能力

**固态硬盘**：不需要寻道（没有磁头），由控制单元+Flash芯片组成。

> 📷 原笔记该部分有配图

---

## 卡17 — 标题：Cache基本概念

Cache一般由**SRAM**构成。主存与Cache之间以**块**为单位交换数据，Cache块和主存块大小相等。

程序访问的**局部性原理**：
- **时间局部性**：一个内存位置被重复引用（如循环）
- **空间局部性**：一个位置被引用，附近位置很快也被引用（如数组）

Cache的功能全部由**硬件**实现。Cache中保存的数据可以是多个进程的数据。

**访问方式**：
1. 先访问Cache，未命中再访问主存
2. 同时访问Cache和主存，命中则停止访问主存

**CPU访存条件**：在408真题里，永远是**Cache缺失才会去访存**。

> 📷 原笔记该部分有配图

---

## 卡18 — 标题：Cache地址映射

**全相联映射**：主存块可放到Cache任意位置。地址结构：tag+块内地址。命中率高但查找速度慢。

**直接映射**：主存块只能放到特定Cache行。Cache行号 = 主存块号 % Cache总行数。**不需要替换算法**。速度快但命中率低。

**组相联映射**：主存块可放到特定分组中的任意位置。地址结构：tag+组号+块内地址。
- n路组相联：每n个Cache行为一组
- Cache组数 = Cache总行数 / n
- 比较器个数 = n（需要并行比较组内所有行的tag）

> 📷 原笔记该部分有配图

---

## 卡19 — 标题：Cache替换算法与写策略

**替换算法**：
- RAND：随机，不稳定
- FIFO：先进先出，未考虑局部性
- **LRU**（近期最少使用）：最久没被访问的先换，实际效果最好。Cache块总数2^n时，计数器需n位
- LFU（最不经常使用）：效果不如LRU

**写策略**：
- **全写法**：每次写入主存和Cache，保持一致性，适合安全性要求高的应用
- **写回法**：仅修改Cache，换出时才写回主存，速度快，适合密集访问，有1位脏位
- 写分配法：不命中时读块入Cache（利用空间局部性）
- 非写分配法：不命中只更新主存，不入Cache

**Cache总容量** = (每行标记位数+数据位数+有效位+脏位) × 总行数

> 📷 原笔记该部分有配图

---

## 卡20 — 标题：虚拟存储器

**Cache-主存与外存对比**：
- Cache-主存：全写/回写均可，全相联/组相联/直接映射均可，关注硬件管理
- 主存-外存：只能回写（全写开销过大），只能全相联（提高命中率），关注软件管理

**TLB（快表）**：
- 用SRAM实现，加快地址转换
- TLB命中则页一定命中
- TLB保存当前进程的虚拟地址，切换进程会清空TLB
- Cache保存的可不止一个进程的物理地址

**缺页处理**：由操作系统通过缺页处理程序完成（软件完成）。
**Cache缺失处理**：完全由硬件完成。
**TLB缺失处理**：软硬件均可。

> 📷 原笔记该部分有配图

---

## 卡21 — 标题：指令系统ISA

ISA规定的内容：软件可见的部分。
指令 = **操作码 + 操作数**。操作码指出应执行什么操作，操作数给出被操作信息的地址。

指令字长指的是一个指令包含的二进制代码位数，取决于操作码长度、地址码长度和个数。

**定长指令字结构**（RISC）：所有指令长度相等，执行速度快，控制简单
**变长指令字结构**（CISC）：各指令长度不等

**指令分类**（按地址码数目）：
- 0地址：不需要操作数，或堆栈运算指令（操作栈顶和次栈顶）
- 1地址/2地址/3地址/4地址

> 📷 原笔记该部分有配图

---

## 卡22 — 标题：操作码设计

**定长操作码**：指令字最高位固定若干位表示操作码。n位可表示2^n条指令。简化硬件设计。

**扩展操作码**：操作码长度随地址码数量的减少而增加。
- **短操作码不能是长操作码的前缀**
- 设地址长度为n，上一层留出m种状态，下一层可扩展出 m×2^n 种状态
- 指令字长必须是字长（8b）的整数倍

> 📷 原笔记该部分有配图

---

## 卡23 — 标题：数据寻址方式

**采用不同寻址方式的目的是缩短指令字长、扩大寻址空间、提高编程灵活性。**

- **立即寻址**：地址字段直接是操作数本身，执行阶段不访存，速度最快
- **直接寻址**：地址字段是操作数的地址
- **间接寻址**：地址字段是操作数地址的地址。第一次访存的信息通过数据总线传送到CPU
- **寄存器寻址**：操作数在寄存器中
- **寄存器间接寻址**：寄存器中存的是操作数的地址
- **隐含寻址**：隐含某个寄存器（如ACC）

**偏移寻址**（区别在于偏移起点）：
- **基址寻址** EA=(BR)+A：BR不变，A可变（程序浮动）
- **变址寻址** EA=(IX)+A：IX可变，A不变（数组访问）
- **相对寻址** EA=(PC)+A：PC所指地址为起点（程序内部浮动）

速记：**硬隐身立即就要寄**（访存次数都为0）。

> 📷 原笔记该部分有配图

---

## 卡24 — 标题：CISC与RISC

| CISC | RISC |
|:---:|:---:|
| 指令系统复杂 | 指令系统精简 |
| 寻址方式多 | 寻址方式少 |
| 变长指令 | 定长指令 |
| 微程序控制为主 | 硬布线控制为主 |
| 强调硬件功能 | 强调编译优化 |

RISC优点：更能充分利用芯片面积、更能提高运算速度、便于设计、利于编译优化。

> 二者关系：就像餐厅做菜，CISC有各种复杂工具，RISC用20%的工具完成80%的工作。

> 📷 原笔记该部分有配图

---

## 卡25 — 标题：CPU功能与结构

**用户可见寄存器**：PC、PSW、通用寄存器、ACC
**对汇编程序员可见**：基址寄存器、变址寄存器、条件码寄存器
**用户不可见**：MAR、MDR、IR、微指令寄存器、Cache

CPU靠指令周期的不同阶段来区分指令和数据（取指阶段取出的是指令，执行阶段取出的是数据）。

**运算器组成**：ALU、通用寄存器组、暂存寄存器、ACC、PSW、移位器
**控制器组成**：PC、IR、指令译码器、时序系统、微操作信号发生器、MAR、MDR

PC→MAR（同时PC+1）→主存→MDR→IR

> 📷 原笔记该部分有配图

---

## 卡26 — 标题：指令执行过程

**指令周期**：CPU从主存取出一条指令并执行所需的全部时间。
指令周期用机器周期（CPU周期）表示，机器周期包含若干时钟周期（节拍，CPU操作最基本单位）。

每个指令周期内机器周期数可以不等，每个机器周期内的节拍数也可以不等。

**执行方案**：
- **单指令周期**：所有指令在同一时钟周期内完成，指令间串行，取决于最慢指令
- **多指令周期**：各指令执行时间不同，需要几个周期给几个
- **流水线方案**：隔一段时间启动一条指令，多条指令处于不同阶段同时进行

**取址操作自动进行，控制器不需要任何指令。**

> 📷 原笔记该部分有配图

---

## 卡27 — 标题：数据通路

数据通路：数据在功能部件之间传送的路径。

**操作元件**（组合逻辑）：输出仅取决于当前输入。加法器、ALU、译码器、多路选择器、三态门等。
**状态元件**（时序逻辑）：输出与当前输入和之前输入都有关。寄存器、存储器等。

每个微操作的执行至少消耗一个时钟周期。

**CPU内部单总线**：一个时钟内只允许传送一个数据。先将一个操作数送到暂存器Y保存，运算结果保存在暂存器Z中。
**CPU内部多总线**：可同时传送不同数据。

> 📷 原笔记该部分有配图

---

## 卡28 — 标题：硬布线控制器

硬布线控制器：纯硬件实现控制，执行速度很快。微操作控制信号由组合逻辑电路即时产生。

- 指令越多，设计和实现越复杂，一般用于RISC
- 扩充一条新指令，控制器设计要大改
- 微操作控制信号形成主要与**指令译码信号和时钟**有关

> 📷 原笔记该部分有配图

---

## 卡29 — 标题：微程序控制器

设计思想：采用"存储程序"思想，CPU出厂前将所有指令的"微程序"存入"控制存储器（CM）"中。

**基本概念**：
- **微指令**：一个时钟周期内可进行的微操作
- **微程序**：有序的微指令序列，每种指令对应一个微程序
- 控制存储器（CM）用ROM实现，集成在控制单元内部

**一条微指令包含**：
1. 操作控制字段（微操作码字段）
2. 顺序控制字段（微地址码字段）

**取指周期的微程序段通常是公用的**。n条机器指令则CM中微程序段至少n+1个。

**主存储器 vs 控制存储器**：
- 主存：存放数据和程序，在CPU外部，用RAM实现
- 控制存储器：存放微程序，在CPU内部，用ROM实现

> 📷 原笔记该部分有配图

---

## 卡30 — 标题：微指令格式与编码

**水平型微指令**：一条微指令能定义多个可并行的微命令。微程序短，执行速度快，但微指令长。
**垂直型微指令**：一条微指令只能定义一个微命令。微指令短，但微程序长，执行速度慢。
**混合型微指令**：在垂直型基础上增加一些并行操作。

**编码方式**：
1. **直接编码**：无需译码，简单直观、执行最快，但微指令字长过长
2. **字段直接编码**：需译码，将控制字段分段，互斥类放同段、相容类放不同段。可缩短指令字长
3. **字段间接编码**：进一步缩短字长，但削弱并行控制能力

> 微指令的设计从**灵活性、指令长度和执行速度**出发。

> 📷 原笔记该部分有配图

---

## 卡31 — 标题：指令流水线

**流水线技术**：时间上的并行技术
**超标量处理机**：空间上的并行技术

五段流水线：IF（取指）→ ID（译码/读寄存器）→ EX（执行）→ MEM（访存）→ WB（写回）

**性能指标**：
- 吞吐率 TP = n / (k+n-1)△t
- 加速比 S = kn / (k+n-1)
- 效率 E = 有效面积 / 总面积

**三种冲突**：
1. **结构冒险（资源冲突）**：多条指令争用同一功能部件。解决方案：指令/数据Cache分离
2. **数据冒险**：后面指令要用前面指令的结果。解决方案：插入NOP、插入气泡、数据旁路技术、编译优化
3. **控制冒险**：转移指令改变PC值。解决方案：分支预测、预取目标指令

**load-use冒险**：load指令后有数据相关，无法用转发技术解决，需要阻塞一个周期。

> 📷 原笔记该部分有配图

---

## 卡32 — 标题：高级流水线与多处理器

**多发射技术**：一个时钟周期内发射多条指令。
**超流水线技术**：增加流水线级数（提高主频）。
**超标量技术**：空间换时间，每个时钟周期可同时发送多条指令（CPI<1）。
**超长指令字**：对优化编译器要求高。

**多处理器分类**：
- SISD：单指令单数据流（常规单处理器）
- SIMD：单指令多数据流（向量处理机）
- MIMD：多指令多数据流（常规多处理机）

**共享内存多处理器SMP**：
- UMA：统一存储访问，各处理器访问时间相同。需解决Cache一致性
- NUMA：非统一存储访问，可无限扩展，访问速度取决于哪个处理器和哪个字

**硬件多线程**：在一个核中处理多个线程。必须为每线程提供单独的通用寄存器组和PC。
- 细粒度：轮流发射
- 粗粒度：遇到阻塞才切换
- 同时多线程（如超线程）：两套线程状态部件，共享Cache和功能部件

> 📷 原笔记该部分有配图

---

## 卡33 — 标题：总线

**总线**：一组能为多个部件分时和共享的公共信息传送线路。
- 分时：同一时刻只允许一个部件向总线发出信息
- 共享：总线上可挂接多个部件同时接收相同信息

**分类**：
- 按数据传输方式：串行（适合长距离）、并行（适合短距离）
- 按功能：片内总线、系统总线（数据/地址/控制）、通信总线

**总线特性**：
- **只有数据线是双向传输**，地址线、控制线均为单向
- 中断类型号通过**数据总线**传输（因为它是CPU需要读取的数据）
- 间址寻址第一次访存得到的地址也通过数据总线传输（地址线是单向的）

> 📷 原笔记该部分有配图

---

## 卡34 — 标题：总线结构与定时

**单总线结构**：结构简单、成本低，但带宽低、负载重
**双总线结构**：主存总线（支持突发传送）+ I/O总线（通过通道管理）
**三总线结构**：提高I/O性能，但任意时刻只能使用一种总线

**总线带宽** = 总线频率 × 总线宽度/8 × 每时钟周期传输次数（MB/s）

**总线定时**：
- **同步定时**：统一时钟信号。速度差距大时会产生时钟偏移问题
- **异步定时**：无统一时钟，靠握手信号。不互锁、半互锁、全互锁
- **半同步定时**：引入时钟信号的异步总线，兼顾两者优势
- **分离式定时**：从设备准备数据时释放总线，提高利用率

> 📷 原笔记该部分有配图

---

## 卡35 — 标题：I/O接口与编址

I/O接口是主机和外设之间的交接界面。

**I/O接口功能**：数据缓冲、状态监测、控制定时、数据格式转换。

**I/O端口**：接口中用于缓冲信息的寄存器。主要有数据端口、状态端口、控制端口。

**两种编址方式**：
1. **独立编址**：I/O端口和主存分开编址，需专门的I/O指令
2. **统一编址**（存储器映射）：主存分一部分给I/O端口，使用统一访存指令

> 📷 原笔记该部分有配图

---

## 卡36 — 标题：程序查询与中断方式

**程序查询方式**：
- 独占查询：CPU花100%时间在I/O操作上
- 定时查询：每隔一段时间查询，间隔内CPU可执行其他程序
- 慢速I/O采用程序查询不影响CPU性能，快速设备会大幅降低CPU性能

**程序中断方式**：
- CPU在每条指令执行末尾检查中断
- 中断响应三个条件：①有未被屏蔽的中断请求 ②CPU开中断 ③一条指令执行完毕

**中断隐指令**（硬件自动完成）：①关中断 ②保存断点和程序状态 ③发中断响应信号形成向量地址

**中断向量**：中断服务程序的入口地址
**中断向量地址**：中断服务程序入口地址的地址
**向量中断**：识别中断源获取向量地址的技术

> 📷 原笔记该部分有配图

---

## 卡37 — 标题：多重中断

CPU暂停现行中断服务程序转去处理新的中断请求。

中断优先级包括：
- **响应优先级**：由中断判优电路决定，不可动态改变
- **处理优先级**：由中断屏蔽字决定，1越多优先级越高，可灵活改变

多重中断在保护被中断进程现场时**关中断**，执行中断处理程序时**开中断**。

> 易错点：外部设备通过中断控制器向CPU发送中断请求信号（正确）。但**没有所谓的中断结束信号**（CPU全程参与，执行完自动返回）。

> 📷 原笔记该部分有配图

---

## 卡38 — 标题：DMA方式

DMA完全由硬件进行信息传送，数据不再经过CPU。

**DMA与中断对比**：
| | 中断请求 | DMA请求 |
|:---:|:---:|:---:|
| 请求 | CPU时间 | 总线使用权 |
| CPU与外设 | 并行 | 并行 |
| 传送与主程序 | 串行 | 并行 |
| 数据传送 | 软件实现 | 硬件实现 |

DMA仅在数据块传送开始和结束时才向CPU发中断（批量传输，CPU干预少）。
DMA无需保护现场。

**DMA传送方式**：停止CPU访存、交替访存、周期挪用。

**DMA控制器中必须设置的4类寄存器**：DR（数据）、MAR（主存地址）、DC（数据计数）、CR（控制）。

> CPU对DMA请求的响应时机是在一个**总线周期**结束时。

> 📷 原笔记该部分有配图

---

## 卡39 — 标题：Cache与TLB工作流程

多级存储系统的访问流程需注意：
- TLB → 页表（若缺失） → Cache（若命中则返回） → 主存（若缺失则访存）
- TLB相当于页表的副本，TLB命中则页一定命中
- Cache缺失后才会去访问主存（408真题原则）

MMU地址转换完全由硬件实现。Cache的TLB查询和地址转换协同工作。

> 页面命中的访问流程中，Cache不命中时耗费的时间比页表短很多。

> 📷 原笔记该部分有配图
