<%*
const selection = tp.file.selection();
if (selection) {
  const lines = selection.split('\n');
  const quoted = lines.map(line => `> ${line}`).join('\n');
  tR += `> [!highlight] 重点\n${quoted}\n`;
} else {
  tR += `> [!highlight] 重点\n> \n`;
}
%>