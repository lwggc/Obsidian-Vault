# 操作系统 — 卡片素材

> 使用说明：每张卡片我已取好标题和整理好内容。你需要在 Markji 中手动添加 [F##...] 挖空标记。
> 建议：每张卡片挖 2~4 个空，别挖太多，否则记不住。
> 📷 标注表示原笔记该部分有配图，请对照你的原笔记插入对应图片。

---

## 卡1 — 标题：操作系统的定义与功能

操作系统：负责管理协调硬件、软件等计算机资源的工作，为上层用户、应用程序提供简单易用的服务，是一种系统软件。

**向上层提供服务**：
- 给普通用户：GUI图形界面和命令接口（联机命令接口如cmd，脱机命令接口如批处理）
- 给软件/程序员：程序接口，由一组系统调用（广义指令）组成，只能通过程序代码间接使用

内核一般由与硬件紧密相关、运行频率较高的程序构成，包括时钟管理、中断处理、设备驱动、进程管理、驱动器管理等。

> 📷 原笔记该部分有配图

---

## 卡2 — 标题：操作系统的特征

**并发**：宏观上同时运行，微观上交替进行。单核CPU同一时刻只能执行一个程序。
**共享**：资源共享，系统中的资源可供内存多个并发执行的进程共同使用。
**虚拟**：把一个物理上的实体变为若干个逻辑上的对应物。
**异步**：资源有限，进程走走停停，以不可预知的速度推进。

**并发性和共享性是操作系统两个最基本的特征**，二者互为存在条件。

> 📷 原笔记该部分有配图

---

## 卡3 — 标题：OS发展分类

**单道批处理**：引入脱机输入输出，由监督程序控制。特点：自动性、顺序性、单道性。
**多道批处理**：提高单机资源利用率的关键技术，依赖于中断技术。资源利用率高，但不提供人机交互。
**分时操作系统**：划分很短时间片轮流分配给各用户，追求快速响应用户。提供人机交互，但不能优先处理紧急任务。
**实时操作系统**：在规定时间内处理完外部事件。硬实时（必须绝对严格），软实时（可偶尔违反）。
**网络操作系统 vs 分布式系统**：网络OS仅实现资源共享和通信；分布式OS提供单一系统映像，具有分布性、并行性、透明性。

> 📷 原笔记该部分有配图

---

## 卡4 — 标题：用户态与内核态

**特权指令**：不允许用户直接使用的指令（关中断、开中断、I/O指令、修改时钟相关等）
**非特权指令**：允许用户直接使用的指令（算术逻辑指令、数据移动指令等）

**内核态→用户态**：通过修改PSW程序状态字
**用户态→内核态**：通过中断/异常/系统调用（硬件实现）

> 📷 原笔记该部分有配图

---

## 卡5 — 标题：中断与异常

**中断是让操作系统内核强行夺回CPU控制权、使CPU从用户态变为核心态的关键机制。**

**内中断（异常）**：与当前指令相关，在指令执行期间检测到。包括故障(fault)、陷阱(trap如访管指令)、终止(abort)。都不可屏蔽。
**外中断（硬件中断）**：与当前执行的指令无关，来源于CPU外部，在一条指令结束后处理。可屏蔽（INTR）、不可屏蔽（NMI）。**外部中断又称为硬件中断。**

有了中断，才能实现多道程序的并发执行。

> 📷 原笔记该部分有配图

---

## 卡6 — 标题：系统调用

系统调用是操作系统给应用程序/程序员提供的接口。

凡是与共享资源有关的操作，都必须通过系统调用方式向操作系统提出服务请求。

**过程**：传递参数（用户态）→ 执行陷入指令（用户态）→ 执行内核程序处理（核心态）→ 返回
- 传递参数一定在执行系统调用之前
- 返回用户态一定是最后一步
- 陷入指令是使得CPU从用户态转为内核态，是执行系统调用的入口

> 📷 原笔记该部分有配图

---

## 卡7 — 标题：OS体系结构

**大内核（宏内核）**：效率高，但代码难以维护、一崩全崩。Unix属于宏内核。
**微内核**：最基本功能保留在内核，其余模块在用户态运行。需频繁切换状态，效率低。解决了扩展性、可靠性、可移植性。
**分层式**：内核分多层，只能调用相邻低层。便于调试验证，但效率低。
**模块化**：将内核划分为多个模块，任何模块可直接调用其他模块。逻辑清晰，动态加载，但一崩全崩。
**外核**：只负责硬件资源的分配、回收、保护，给用户进程直接分配不虚拟的硬件资源。

> 📷 原笔记该部分有配图

---

## 卡8 — 标题：操作系统引导

1. 激活CPU，读取ROM中的boot程序
2. 硬件自检（POST），构建中断向量表
3. 加载带有操作系统的硬盘，读取引导扇区
4. 加载主引导记录MBR
5. 扫描硬盘分区表，加载活动分区
6. 加载分区引导记录PBR
7. 加载启动管理器
8. 加载操作系统到内存

> 📷 原笔记该部分有配图

---

## 卡9 — 标题：进程的组成与PCB

**进程是进程实体的运行过程，是系统进行资源分配和调度的一个独立单位。**
**程序是静态的，进程是动态的。**

**PCB（进程控制块）**：进程存在的唯一标志。操作系统对进程进行管理工作所需的信息都存在PCB中。
- 进程描述信息：PID、UID
- 控制和管理信息：CPU使用情况、进程状态
- 资源分配清单：正在使用的文件、内存区域、I/O设备
- 进程上下文：CPU中各寄存器的值（如页表始址寄存器中的页表始址）

PCB是给操作系统使用的，程序段与数据段是给进程自己用的。

> 📷 原笔记该部分有配图

---

## 卡10 — 标题：进程的状态与控制

**五状态模型**：创建态→就绪态→运行态→阻塞态→终止态

**进程控制**：用原语实现（关中断和开中断来实现原语），具有原子性。

进程控制原语：创建、撤销、阻塞、唤醒、切换。无论哪个原语，要做的无外乎：
1. 更新PCB信息
2. 将PCB插入合适队列
3. 分配/回收资源

**进程的最大数目受内存大小限制。**

> 📷 原笔记该部分有配图

---

## 卡11 — 标题：进程通信

**共享存储**（速度最快）：在通信进程之间共享一块空间，由通信进程自己负责互斥。
- 基于数据结构（低级）、基于存储区（高级）

**消息传递**：以格式化的消息为单位，通过发送原语和接收原语进行。
- 直接通信：邮差直接把信送到对方手上
- 间接通信：邮差把信放到信箱里

**管道通信**：先进先出，半双工，只能单向传输。数据读出后彻底消失。允许多个写进程、一个读进程（408答案）。

**信号**：用于通知进程某个特定事件已经发生。

> 📷 原笔记该部分有配图

---

## 卡12 — 标题：父子进程

父子进程是两个不同的进程，拥有各自的PID。相互独立，互不影响。

父进程撤销，子进程：①一并被终止，②成为孤儿进程，被init收养。
子进程撤销，父进程没事。

创建新进程的主要原因：用户登录、高级调度、系统处理用户程序请求、用户程序的应用请求。

> 📷 原笔记该部分有配图

---

## 卡13 — 标题：线程

引入线程后：**进程是资源分配的基本单位，线程是调度的基本单位。**

进程只作为除CPU之外的系统资源的分配单元。线程间并发，同一进程内的线程切换不需要切换进程环境。

**线程的属性**：
- 每个线程有线程ID、TCB
- 有就绪、阻塞、运行三种基本状态
- 同一进程的不同线程间共享进程的资源
- 同一进程中的线程通信无需系统干预
- **每个线程有独立的栈**

> 📷 原笔记该部分有配图

---

## 卡14 — 标题：用户级线程与内核级线程

**用户级线程**：由线程库实现，操作系统不知道其存在。切换在用户空间即可完成，效率高。但一个线程被阻塞后整个进程被阻塞，不可在多核上并行。

**内核级线程**：由操作系统实现，是处理机分配的单位。一个线程被阻塞后别的线程仍可继续。但线程切换需切换到核心态，开销大。

**多线程模型**：
- 一对一：一个用户级线程映射到一个内核级线程。并发能力强，但开销大
- 多对一：多个用户级线程映射到一个内核级线程。效率高，但并发度不高
- 多对多：n个用户级映射到m个内核级（n≥m）。兼具二者所长

> 📷 原笔记该部分有配图

---

## 卡15 — 标题：调度三层次

**高级调度（作业调度）**：从外存后备队列中选作业调入内存，创建PCB。解决"要不要"的问题。

**中级调度（内存调度）**：把暂时不能运行的进程调到外存等待（挂起状态）。提高内存利用率和系统吞吐量。解决"能不能"的问题。

**低级调度（进程调度/处理机调度）**：从就绪队列中选进程分配CPU，几十ms一次。最基本调度方式，必须配置。解决"给谁"的问题。

> 挂起是调入外存，阻塞还在内存。

> 📷 原笔记该部分有配图

---

## 卡16 — 标题：调度时机与方式

**可以调度的情况**：
- 主动放弃：进程终止、异常终止、主动阻塞
- 被动放弃：时间片用完、紧急事件、更高优先级进程就绪

**不可调度的情况**：处理中断过程中、OS内核程序临界区中、原子操作过程中。

**调度方式**：
- **非剥夺（非抢占）**：只允许进程主动放弃处理机。实现简单，适合批处理
- **剥夺（抢占）**：有更紧迫进程可暂停当前进程。适合分时和实时

**闲逛进程（idle）**：无就绪进程时运行，优先级最低。

> 📷 原笔记该部分有配图

---

## 卡17 — 标题：调度算法（早期）

**FCFS**：先来先服务，非抢占。按照到达先后顺序服务。
**SJF（SPF）**：最短作业/进程优先。默认非抢占，抢占式称为最短剩余时间优先（SRTF）。
**HRRN**：高响应比优先，非抢占。响应比 = (等待时间+执行时间) / 执行时间。

**评价指标**：周转时间、带权周转时间（= 作业周转时间/实际运行时间）、等待时间、响应时间

> I/O繁忙型作业的优先级应该 > 计算繁忙型作业。

> 📷 原笔记该部分有配图

---

## 卡18 — 标题：调度算法（交互式）

**RR（时间片轮转）**：轮流让就绪队列中的进程依次执行一个时间片。每次选择队头进程。
- 时间片太大：增大响应时间；时间片太小：进程切换过于频繁

**优先级调度**：分为静态优先级和动态优先级。**优先级和作业长短没有必然联系。**

**多级反馈队列**：多个就绪队列，优先级越高时间片越小。进程可在队列间移动。
- 仅当高级队列为空时才调度次级队列
- 并发度高，只能用于进程调度

> 📷 原笔记该部分有配图

---

## 卡19 — 标题：多处理机调度

**方案一（公共就绪队列）**：所有CPU共享同一个就绪队列。
- 优点：天然实现负载均衡
- 缺点：亲和性不好（进程频繁换CPU）

**方案二（私有就绪队列）**：每个CPU有自己的就绪队列。
- 优点：天然实现处理机亲和性
- 负载均衡方式：推迁移（push）和拉迁移（pull）

> 📷 原笔记该部分有配图

---

## 卡20 — 标题：同步与互斥基础

**临界资源**：一个时间段内只允许一个进程使用的资源，属于互斥共享资源。
**临界区**：实现访问临界资源的那段代码程序。

**互斥四个准则**（前三条必须，让权等待非必须）：
1. 空闲让进
2. 忙则等待
3. 有限等待
4. **让权等待（非必须）**——Peterson算法就没有实现让权等待

**可重入代码**：可以被多个进程在任意时刻共享的代码（不可修改），减少了对换数量。

> 📷 原笔记该部分有配图

---

## 卡21 — 标题：互斥实现方法

**软件方法**：
- **单标志法**：访问完临界区后转交权限
- **双标志先检查**：标记意愿，先检查后上锁
- **双标志后检查**：先上锁后检查
- **Peterson算法**：双标志法+单标志法，礼貌谦让

**硬件方法**：
- **关中断**：只适用于单处理机，只适用于OS内核进程
- **TestAndSet（TSL）**：原子操作，适用于多核。但会忙等，可能导致饥饿
- **Swap（XCHG）**：同TSL，交换锁变量值

**互斥锁**：硬件机制实现。acquire获得锁，release释放锁。需忙等（违反让权等待）。

> 📷 原笔记该部分有配图

---

## 卡22 — 标题：信号量

**整型信号量**：只有初始化、P操作、V操作。不遵循让权等待（可能忙等）。
**记录型信号量**：引入阻塞机制，消除了不让权等待。S.value表示资源数，S.L指向等待队列。

**PV操作实现互斥**：
1. 设置互斥信号量mutex，初值为1
2. 临界区前P操作，临界区后V操作

**PV操作实现同步**：
1. 设置同步信号量，初值为0
2. 前操作后V，后操作前P

**PV操作一定能实现互斥，但不能防止死锁。**

> 📷 原笔记该部分有配图

---

## 卡23 — 标题：经典同步问题（生产者消费者）

系统中有一组生产者进程和一组消费者进程。生产者每次生产一个产品放入缓冲区，消费者每次取出一个产品。

要点：
- 实现互斥的PV在同一进程中，实现同步的PV在不同进程中
- **互斥夹在同步里面**，一般不会出错
- 改变相邻P/V操作顺序会产生**死锁**

设置信号量：mutex（互斥访问缓冲区，初值1），empty（空缓冲区数），full（满缓冲区数）。

> 📷 原笔记该部分有配图

---

## 卡24 — 标题：读者写者问题

规则：
1. 允许多个读者同时读
2. 同时只允许一个写者写
3. 写者在完成写前不允许其他读者或写者工作

**读者优先**：一旦有读者读，后续读者可立即进入，写者必须等待所有读者完成。
**写者优先**：有写者等待时，新到达的读者必须等待，可能导致读者饥饿。
**读写公平**：引入队列，读者写者公平排队。

> 📷 原笔记该部分有配图

---

## 卡25 — 标题：管程

管程实现更方便的进程同步与互斥，大大降低了死锁的可能性。

管程把实现复杂的共享资源操作封装在函数里，对外只展示特定入口。

- **进程互斥**：由编译器实现
- **进程同步**：设置条件变量和等待唤醒操作（x.wait() 和 x.signal()）
- 条件变量**不是信号量**，不能进行P/V操作

管程模型：Hoare（要求wait/signal严格配对），Hansen（最常用，仅标记）

> 📷 原笔记该部分有配图

---

## 卡26 — 标题：死锁

死锁至少1个资源、2个进程。

**死锁产生的原因**：对系统资源的竞争、进程推进顺序非法、信号量使用不当。

**必要4条件**：
1. 互斥条件
2. 请求和保持条件
3. 不可剥夺条件
4. 循环等待条件

**处理策略**：
- **预防死锁**：破坏必要条件（互斥条件有时不能破坏）
- **避免死锁**：银行家算法（找安全序列）
- **死锁检测**：资源分配图消边简化，若不可完全简化则死锁

**死锁定理**：每种资源只有1个且出现环路，必然死锁。每个进程至少有一个请求边，未必死锁。

**饥饿无法彻底杜绝。**

> 📷 原笔记该部分有配图

---

## 卡27 — 标题：内存管理基础

**内存保护**：需要硬件和软件的配合。设置上下限寄存器或利用重定位寄存器+界地址寄存器判断。

**内存共享**：允许多个进程访问内存的同一部分。

**装入方式**：
- **绝对装入**：编译时知道程序将放在内存的哪个位置
- **静态重定位**：程序装入前完成地址转换，一旦加载位置固定不能移动
- **动态重定位**：程序真正执行时才转化成物理地址，可分配到不连续存储区

**链接方式**：静态链接（装入前）、装入时动态链接、运行时动态链接。

> 每个进程都有独立的内核栈，PCB保存内核栈的指针。

> 📷 原笔记该部分有配图

---

## 卡28 — 标题：连续分配管理

**单一连续分配**：内存分系统区和用户区，只支持单道程序。无外部碎片，有内部碎片。
**固定分区分配**：分区大小预先固定。无外部碎片，有内部碎片。由装入程序或OS实现地址转换。
**动态分区分配（可变分区分配）**：根据进程大小动态建立分区。无内部碎片，有外部碎片（可用紧凑技术解决）。

**基于顺序搜索的分配算法**：首次适应、邻近适应、最佳适应、最坏适应。

**基于索引搜索的分配算法**：快速适应（预分配固定大小链表）、伙伴系统（按2^n划分）、哈希算法。

> 📷 原笔记该部分有配图

---

## 卡29 — 标题：分页存储

**页面大小 = 页框大小 = 2^页内偏移量位数**

**页表**：每个页表项大小相同，页号是隐含的。页表通常存放在内存中，查找操作由硬件执行。

**逻辑地址结构**：页号 + 页内偏移量
- 页号 = 逻辑地址 / 页大小
- 页内偏移量 = 逻辑地址 % 页大小
- 物理地址 = 页框号 × 页大小 + 页内偏移量

**两级页表**：一级页表常驻内存，二级页表不一定。若采用多级页表机制，各级页表大小不能超过一个页面。使用n级页表，访存次数为 n+1 次（无快表）。

> 📷 原笔记该部分有配图

---

## 卡30 — 标题：分段存储与段页式存储

**分段**：按照程序的逻辑关系划分为若干个段，每段从0开始编址。段内相邻，段间不相邻。
**目的**：方便编程、分段共享、分段保护、动态链接、动态增长。
**只有分段存储管理与程序的逻辑结构相关。**

**段页式**：先按逻辑分段，再每段分页。地址空间是二维的。

**分页 vs 分段**：
- 分页产生内部碎片，分段产生外部碎片
- 分页在装作业时决定，分段在编程时决定
- 分页对程序员透明，分段不透明

> 📷 原笔记该部分有配图

---

## 卡31 — 标题：虚拟内存

虚拟内存特征：**多次性、对换性、虚拟性、非连续性。**

**虚拟内存的最大容量 = CPU最大寻址范围**
**虚拟内存的实际容量 ≤ min{内外存容量之和, CPU最大寻址范围}**

虚拟内存只能基于**非连续分配技术**。

**请求分页管理方式**需要支持的方面：
1. 一定容量的内存外存
2. 页表机制（新增状态位P、访问位A、修改位M、外存地址）
3. 中断机构（缺页中断属于内中断中的故障）
4. 地址变换机构

> 📷 原笔记该部分有配图

---

## 卡32 — 标题：页面置换算法

**OPT（最佳置换）**：选择以后永不使用或最长时间内不再被访问的页面。无法实现，用于评价其他算法。

**FIFO**：根据调入先后顺序排队，换出队头。Belady异常（分配内存块增加缺页率反而上升）。

**LRU（最近最久未使用）**：选择最久没被访问的页面。实际效果好，命中率高。

**CLOCK/NRU（时钟置换）**：循环扫描，使用位为0则替换，为1则置0继续。

**缺页率影响因素**：页面置换算法、页面大小、工作集大小、进程数、程序的编址方法。

> 📷 原笔记该部分有配图

---

## 卡33 — 标题：页面分配策略

**固定分配**：内存块数量不变
**全局置换**：可改变各进程的内存分配数量

**抖动**：主要原因分配给进程的物理块不够。解决：增加为进程分配的页面或撤销部分进程。

**工作集**：在最近的时间窗口内，进程频繁访问的页面集合。工作集不一定是驻留集的子集。
**驻留集**：分配给进程的物理页面的集合。

**提高TLB命中率的方法**：增大TLB容量、提高页面大小。

> 📷 原笔记该部分有配图

---

## 卡34 — 标题：文件的基本概念

**文件**：存储在存储设备上的一组数据。
**文件系统**：提供把信息保存在磁盘等外部设备上并便于日后访问的方法。

**文件分类——目录文件**：FCB（文件控制块）的集合。引入inode前目录项就是FCB；引入inode后目录项存的是文件名+inode号。

**目录结构演进**：
- **单级目录**：只有一张目录表，不允许文件重名，查找速度慢
- **两级目录**：不同用户文件可重名，但不能对文件分类
- **树形目录**：解决重名问题，方便分类。当前Windows使用，不方便文件共享
- **无环图目录**：不同目录共享文件，设置共享计数器，计数为0才真正删除

> 📷 原笔记该部分有配图

---

## 卡35 — 标题：文件结构

**逻辑结构**（用户视角）：
- 无结构文件（流式文件）：二进制流或字符流
- 有结构文件：由记录组成，分为定长和可变长
  - 顺序文件：定长可实现随机存取，可变长无法实现
  - 索引文件：每个记录对应一个表项，支持随机存取
  - 索引顺序文件：记录分组，每组一个索引表项

**物理结构**（OS视角）：
- **连续分配**：物理块 = 起始块号 + 逻辑块号。支持顺序和随机访问，但产生碎片
- **链接分配（隐式）**：盘块存指向下一个的指针，只支持顺序访问
- **链接分配（显式）**：FAT表存链接信息，常驻内存，支持随机访问
- **索引分配**：每个文件建立一个索引表。支持直接访问大文件（多层索引/混合索引）

**索引节点**：文件名和文件描述信息分离。目录项只含文件名+索引结点指针。

> 📷 原笔记该部分有配图

---

## 卡36 — 标题：文件操作与共享

**创建文件**：分配外存空间 + 创建目录项FCB
**打开文件**：open只找到目录项读入内存，不读入文件内容
**读文件**：通过文件描述符fd访问进程打开文件表→系统打开文件表→FCB→转换为物理块→访问外存

**硬链接**：指向同一个inode的多个文件名，所有硬链接平等。只有所有链接都删除时才释放文件。
**软链接（符号链接）**：是一个独立文件，存目标文件路径。删除原文件后软链接指向不复存在的文件。

文件的访问由**用户访问权限和文件属性**共同控制，**与用户优先级没有任何关系。**

> 📷 原笔记该部分有配图

---

## 卡37 — 标题：文件存储空间管理

**空闲表法**：为磁盘创建一个空闲磁盘块位置表。类似动态分区分配。
**空闲链表法**：空闲盘块链（适合离散分配）、空闲盘区链（离散/连续都适合）。
**位示图法**：每个bit位对应一个盘块。(字号,位号) = (i,j) → 盘块号 b = n×i + j
**成组链接法**：将空闲盘块分组，每组的第一个盘块记录下一组信息。第一组保存在内存专用栈中（空闲盘块号栈）。适合大型文件系统。

> 📷 原笔记该部分有配图

---

## 卡38 — 标题：I/O管理概述

**块设备**：以数据块为单位，传输速率高，可寻址，支持随机访问（如磁盘）
**字符设备**：以字符为单位，传输速率低，不可寻址（如键盘、打印机）

**I/O控制器（设备控制器）**：CPU与设备之间的电子部件。主要功能：接收识别命令、数据交换、状态报告、地址识别、数据缓冲、差错控制。

**两种寄存器编址方式**：
- **内存映射I/O**：寄存器与内存统一编址，可用访存指令操作
- **寄存器独立编址**：寄存器独立编址，需专门I/O指令

> 📷 原笔记该部分有配图

---

## 卡39 — 标题：I/O控制方式

**程序直接控制（轮询）**：CPU不断查询设备状态。简单但效率低。可结合进程调度减少CPU占用。

**中断驱动方式**：设备准备完成后发中断，CPU响应后处理。CPU和I/O可并行。但每传送一个字/字节都需中断。

**DMA方式**：数据块为单位，数据不经过CPU，仅在开始和结束时中断。CPU介入少。

**通道方式**：通道是负责数据传输的硬件单元。CPU负责启动和管理通道，不直接参与数据传输。

四种控制方式效率递增：程序查询 < 中断 < DMA < 通道。

> 📷 原笔记该部分有配图

---

## 卡40 — 标题：SPOOLing技术与设备分配

SPOOLing（假脱机技术）：由输入输出井、输入输出缓冲区、输入输出进程、井管理程序组成。

- 提高了I/O速度（将低速I/O变为对磁盘缓冲区的存取）
- **将独占设备改造为共享设备**
- 实现了虚拟设备功能
- 以空间换时间

SPOOLing由**OS内核设备独立性软件层 + 用户态守护进程**协同实现。

**设备分配**：固有属性（独占/共享/虚拟）、分配算法（FCFS/优先级）、安全性（安全/不安全）、独立性。

> 📷 原笔记该部分有配图

---

## 卡41 — 标题：缓冲区管理

**单缓冲**：处理一块数据平均用时 max(T, C+M)
**双缓冲**：处理一块数据平均用时 max(T, C+M) 的进一步优化。支持双向发送/接收。
**循环缓冲区**：多个大小相等的缓冲区链接成循环队列。
**缓冲池**：系统共用资源，供多个进程共享，能用于输入也能用于输出。

四种工作缓冲区：收容输入（hin）、提取输入（sin）、收容输出（hout）、提取输出（sout）。

**缓冲区的数据非空时不能冲入数据。缓冲区为空时才能冲入数据。**

> 📷 原笔记该部分有配图

---

## 卡42 — 标题：磁盘调度算法

一次磁盘读写时间 = 寻道时间 + 旋转延迟时间 + 传输时间

**FCFS**：按请求到达顺序处理。公平，但分散时性能差。
**SSTF（最短寻道时间优先）**：优先处理最近的磁道。平均寻道时间短，但可能产生饥饿。
**SCAN（电梯算法）**：移动到最边缘才改变方向。性能好，不会饥饿，但响应频率不平均。
**LOOK**：磁头移动方向上没有请求就立即改变方向。
**C-SCAN（循环扫描）**：一条路走到底，各位置响应频率平均，但平均寻道时间更长。
**C-LOOK**：不需要移动到最边缘就返回，进一步缩短寻道时间。

**减少磁盘延迟的方法**：扇区交替编号、错位命名。

> 📷 原笔记该部分有配图
